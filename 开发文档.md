# 记忆管理系统 - 开发文档（四层架构版）

## 1. 文档定位

- 本文档是项目的**架构与实现规范**。
- 本项目只采用一种记忆模型：**四层架构**。
- 如与其他历史文档冲突，以本文档为准。

文档版本：v4.73  
最后更新：2026-04-01

---

## 2. 项目概述

### 2.1 项目目标

构建一个可持续演进的 CLI 记忆系统，让模型在多轮对话中实现：

1. 保持短期上下文连续性
2. 记住用户偏好与背景（可管理、可删除）
3. 在相似问题上通过语义检索复用历史经验

### 2.2 技术栈

- Java 17+
- Spring Boot 3.2.0
- LangChain4j 0.35.0
- OpenAI 格式 API
- all-MiniLM-L6-v2（本地嵌入）
- Jackson / Lombok

### 2.2.1 文件即记忆（Memory as Filesystem）定义

本项目不是“聊天+工具”的通用 Agent，而是“文件即记忆”的记忆系统：

1. 记忆载体统一为文件（`json/jsonl/md`）。
2. shell 操作是记忆操作原语：对文件记忆进行 CRUD、检索与治理（读/写/改/检索/归档）。
3. 系统核心贡献聚焦“面向文件记忆体的提取、组织、检索、治理、评测闭环”。

### 2.3 架构原则

1. 单一职责：类职责边界清晰，不做“万能类”。
2. 分层调用：`cli -> memory/rag -> llm -> prompt`，禁止反向依赖。
3. 统一写入：记忆落盘必须经过 `MemoryWriteService`。
4. 提取与通信解耦：`LlmClient` 只通信，`LlmExtractionService` 做提取业务。
5. 文件可恢复：存储文件可在启动时自动初始化与迁移。

### 2.4 毕设扩展原则

项目后续扩展以“保持 `Memory-System` 核心定位”为前提，遵循以下约束：

1. 不改动四层记忆架构基线，只在层内增强能力或补充横向治理模块。
2. 新功能优先回答“记忆如何被提取、组织、检索、验证、应用”，避免偏离成通用 Agent 平台。
3. 所有新增能力尽量落到现有模块边界：`memory / rag / task / im / prompt / tool`。
4. 论文贡献优先强调“系统性”和“可验证性”，因此评测、指标、审计链路是正式功能，不是附属脚本。
5. 文档、实现、实验计划必须同步更新，避免毕设阶段出现“代码有了但无法讲清楚”的问题。

---

## 3. 四层记忆架构

### 3.1 架构总览

```text
Layer 1: Short-term Memory
Layer 2: Metadata
Layer 3: User Insight
Layer 4: Learned Knowledge
         ├── 4a Skill (.md)
         └── 4b Example (Vector/RAG)
```

说明：

1. 四层架构是本项目唯一记忆基线。
2. 后续新增“摘要记忆、治理状态、评测、身份映射、主动服务”均视为在四层基线之上的增强能力，而不是新增第五层。

### 3.1.1 会话启动导航文件 `Agent.md`（非记忆层）

`Agent.md` 不属于四层记忆中的任何一层，它是会话启动阶段的导航文件。

职责限制：

1. 说明关键文件在哪里
2. 说明各文件的职责和加载方式
3. 标注哪些内容允许自动加载，哪些内容需要按需读取

约束：

1. 不存放大量知识正文
2. 不存放高频变动的运行态信息
3. 不存放完整用户记忆内容
4. 每次新会话启动前自动加载一次，不要求每轮重复全文注入

当前方案路径：

1. 仓库根目录 `Agent.md`：全局导航图
2. 后续如需本地覆盖，可再增加 `.memory/Agent.local.md`

---

### 3.2 Layer 1 - Short-term Memory（短期记忆）

#### 定义

当前会话的即时上下文，不作为长期画像沉淀。

#### 组成

1. 最近完整对话窗口（user + assistant）
2. 最近用户输入日志（user-only）

#### 当前实现

- 对话上下文：`conversation_history.jsonl`
- 用户输入窗口：`recent_user_messages.jsonl`
- 运行时策略：
  - messages：默认最近 10 轮完整对话
  - prompt：默认注入第 11~40 轮用户消息（连续性日志）

#### 相关代码

- `MemoryStorage.getRecentConversationTurns()`
- `MemoryStorage.getOlderUserMessages()`
- `ConversationCli.processUserMessage()`

---

### 3.3 Layer 2 - Metadata（元数据）

#### 定义

描述用户环境、助手偏好、全局控制开关的静态或半静态信息。

#### 存储

- 文件：`metadata.json`
- 典型字段：
  - `user_interaction_metadata`
  - `assistant_preferences`
  - `global_controls`（`use_saved_memories`、`use_chat_history`）
  - `ui_settings`（`cli_theme`、`show_footer`）

#### 当前实现

- 读取：构建 system prompt 前读取
- 更新：CLI 命令 `/controls` 修改 `global_controls`
- 更新：CLI 命令 `/theme`、`/footer` 修改并持久化 `ui_settings`

#### 相关代码

- `MemoryStorage.readMetadata()/writeMetadata()`
- `CliRunner.showControls()/setControl()`
- `ConversationCli.buildSystemPrompt()`

---

### 3.4 Layer 3 - User Insight（用户洞察）

#### 定义

用于提升后续回复质量的用户偏好、背景、长期习惯信息。

#### 数据模型

当前统一使用 `Memory` 对象，关键字段：

- `content`
- `memoryType`（当前仅 `USER_INSIGHT`）
- `source`（`EXPLICIT` / `IMPLICIT`）
- `confidence`
- `hitCount`
- `createdAt` / `lastAccessed`

#### 存储

1. `.memory/user-insights.md`：用户洞察单文档正文
2. `pending_explicit_memories.jsonl`：显式提取冲突队列
3. `memory_queues.json`：当前实现兼容保留，后续视单文档方案决定是否退役
4. `user_insights.json`：迁移源，完成迁移后废弃

#### 写入来源

1. 显式提取：用户消息实时检测
2. 隐式提取：夜间任务或 `/memory-update`

#### 当前实现

- 已实现显式提取 + 冲突入队
- 已实现隐式提取（用户洞察）
- 已实现 Top of Mind 访问升级与淘汰
- 已实现 `.memory/user-insights.md` 单文档画像模型
- 启动时支持 `user_insights.json -> user-insights.md` 自动迁移

#### 目标文件格式

用户洞察正文文件建议使用单个 Markdown 文档，保存稳定内容和可读描述。

```md
---
updated_at: 2026-03-18T10:00:00+08:00
style: narrative
---

用户当前的长期画像如下。

用户不爱吃鱼，在推荐菜谱、餐厅和聚餐方案时应避开鱼类。用户更偏好简洁直接的表达，不喜欢冗长说明。用户当前正在开发一个记忆系统项目，讨论重点集中在本地文件系统记忆、工具加载和分层架构收敛。
```

说明：

1. `User Insight` 作为单文档维护，不再按槽位拆分多个 Markdown 文件
2. 正文使用自然段，不使用项目符号列点表达用户画像
3. 文档面向人工阅读和整体重写，不再以单槽位增删改为主
4. 当前 `hitCount`、`lastAccessed`、队列状态仍由旧实现维护；迁移后需重新评估是否保留

#### 相关代码

- `MemoryExtractor`
- `LlmExtractionService.extractExplicitMemory()/extractUserInsights()`
- `MemoryWriteService`
- `MemoryManager`

---

### 3.5 Layer 4 - Learned Knowledge（持续学习知识）

#### 定义

沉淀“怎么做事”的知识，不是“用户是谁”。

#### 4a Skill（方法论）

- 形态：`.memory/skills/*.md`
- 作用：沉淀稳定策略、最佳实践
- 加载方式：通过固定工具 `load_skill(name)` 按需读取
- 当前状态：**已接入对话主链路，默认不再全量注入 skill 正文**

#### 4b Example（案例）

- 形态：`vector_store.json`
- 作用：通过向量语义检索召回相似历史片段
- 当前状态：**已实现基础能力**

当前实现能力：

1. 记忆向量化索引（`indexMemory`）
2. 批量重建索引（`indexAllMemories`）
3. 语义搜索（`searchMemories`）
4. 对话中构建 RAG 上下文（`buildSmartContext`）

---

## 4. 会话启动与 Prompt 组装策略

### 4.1 会话启动阶段

新会话启动前先读取 `Agent.md`，建立最小导航上下文。

启动阶段规则：

1. 自动加载 `Agent.md`
2. 在启用记忆时加载 `user-insights.md`
3. 不自动全文加载所有 `Skill` 文件
4. 需要正文时再按需读取目标文件

### 4.2 对话轮次 Prompt 组装

`SystemPromptBuilder` 当前组装顺序：

1. `Agent.md` 导航摘要或启动缓存
2. Layer 2 Metadata
3. Layer 2 Assistant Preferences
4. Layer 1 用户输入日志（older user messages）
5. Layer 3 User Insights（单文档画像正文）
6. Layer 4a Skills（按需加载）
7. Layer 4b RAG Context / Examples

同时，Layer 1 最近完整对话通过 `messages` 单独传递给模型。

说明：

1. Prompt 负责把多层记忆组织成可被模型消费的上下文。
2. 后续若新增摘要记忆、检索解释、记忆状态标签，也应优先作为 Prompt 组装策略增强，而不是破坏现有主流程。

---

## 5. 面向毕设的扩展模块设计

### 5.1 下一阶段总目标

下一阶段不采用显式“记忆路由器”方案，而是建设更高级的 `Memory Reflection` 机制。

核心目标：

1. 让 Agent 在回答前先判断“当前问题是否真的需要过去的信息”。
2. 让 Agent 明白“为什么需要记忆”，而不是因为系统里有记忆就机械调用。
3. 让每次记忆调用都带有证据目的，例如个性化、连续性、约束遵循、经验复用、行动跟进。
4. 让系统对外可展示“本轮回答依赖了哪些过去依据”，增强可解释性和答辩表现力。

系统定位保持不变：

1. 项目仍然是四层 `Memory-System`。
2. `Memory Reflection` 属于回答前的推理增强机制，不新增记忆层。
3. 它负责决定“记忆是否必要”和“记忆调用目的”，不是传统按模块分流的 Router。

### 5.2 下一阶段核心能力一：Memory Reflection（记忆反思机制）

目标：让系统在生成回答前，先进行一次面向回答目标的自我反思，判断是否需要记忆支撑。

#### 5.2.1 设计原则

1. 不是先问“查哪个模块”，而是先问“只靠当前输入是否能回答好”。
2. 如果不能回答好，再判断缺失的是哪一类“过去证据”。
3. 记忆调用应服务于回答质量提升，而不是固定流程中的强制步骤。
4. 反思结果必须可落盘、可调试、可展示。

#### 5.2.2 反思输出结构

建议新增结构化结果对象 `MemoryReflectionDecision`，至少包含：

- `needsMemory`：是否需要调用长期记忆
- `memoryPurpose`：记忆使用目的
- `reason`：自然语言解释
- `confidence`：判断置信度
- `retrievalHint`：对后续检索的提示语
- `evidenceTypes`：预期需要的证据类型

其中 `memoryPurpose` 建议枚举：

- `PERSONALIZATION`
- `CONTINUITY`
- `CONSTRAINT`
- `EXPERIENCE_REUSE`
- `ACTION_FOLLOWUP`
- `NOT_NEEDED`

其中 `evidenceTypes` 建议枚举：

- `USER_INSIGHT`
- `SESSION_SUMMARY`
- `TASK`
- `EXAMPLE`
- `SKILL`
- `RECENT_HISTORY`

#### 5.2.3 触发时机

建议流程插入到 `ConversationCli.processUserMessage()` 中，在组装 system prompt 与执行 RAG/Skill 之前：

1. 接收用户输入
2. 运行 `Memory Reflection`
3. 若 `needsMemory=false`，则尽量基于当前轮和最近上下文回答
4. 若 `needsMemory=true`，则按 `retrievalHint` 与 `evidenceTypes` 组织记忆上下文
5. 生成回复
6. 记录本轮使用了哪些记忆证据

#### 5.2.4 需要覆盖的问题类型

下一阶段至少覆盖以下六类问题：

1. 用户身份与偏好类
   - 例如“按我的习惯推荐”“我之前喜欢什么”
2. 跨会话延续类
   - 例如“继续上次的毕设规划”
3. 任务与提醒类
   - 例如“我还有哪些没做”“提醒我明天继续”
4. 历史经验复用类
   - 例如“之前类似问题是怎么解决的”
5. 方法与策略类
   - 例如“这类问题一般怎么系统处理”
6. 一般知识问答类
   - 例如普通技术概念解释，通常不需要长期记忆

#### 5.2.5 失败保护

1. 即使 `Memory Reflection` 输出异常，主对话链路仍必须可继续。
2. 当结构化结果解析失败时，回退为当前默认行为。
3. 不允许因为反思阶段失败导致整个问答不可用。

### 5.3 下一阶段核心能力二：记忆证据视图（Memory Evidence View）

目标：系统不仅回答，还能说明“这次回答为何参考了过去”。

#### 5.3.1 设计要求

每一轮回答完成后，系统内部至少应能记录：

1. 本轮是否触发了长期记忆调用
2. 触发目的是什么
3. 调用了哪些证据类型
4. 实际命中了哪些记忆内容
5. 哪些记忆最终真正参与了回答

#### 5.3.2 输出结构

建议新增 `MemoryEvidenceTrace`，包含：

- `userMessage`
- `reflectionDecision`
- `retrievedInsights`
- `usedInsights`
- `retrievedExamples`
- `usedExamples`
- `loadedSkills`
- `usedSkills`
- `retrievedTasks`
- `usedTasks`
- `usedEvidenceSummary`
- `timestamp`

#### 5.3.3 展示要求

CLI 至少提供一个调试入口，例如：

- `/memory-debug`

输出内容建议包括：

1. 本轮是否需要记忆
2. 为什么需要或为什么不需要
3. 记忆使用目的
4. 命中的记忆条目与分数
5. 最终引用的证据摘要
6. 按证据类型展示 `retrieved` 与 `used` 的 Top-N 条目，便于解释“检索到”与“真正使用”的差异

说明：

1. 该视图首先服务开发和答辩展示，不要求默认对普通用户可见。
2. 后续如接入 IM，可考虑仅在 debug 模式下返回。

### 5.4 下一阶段核心能力三：会话摘要与情节记忆

目标：让系统不仅记住“事实”，也记住“过程与脉络”。

#### 5.4.1 需求描述

1. 当会话长度达到阈值时，自动生成 `session summary`。
2. 当话题发生明显迁移时，生成 `topic summary`。
3. 当项目阶段、任务状态、用户目标发生重大变化时，生成 `milestone summary`。
4. 这些摘要优先作为连续性证据参与 `Memory Reflection` 之后的回答构建。

#### 5.4.2 存储建议

- `.memory/session_summaries.jsonl`
- `.memory/topic_summaries.jsonl`
- `.memory/milestone_summaries.jsonl`

每条摘要至少包含：

- `summaryId`
- `summaryType`
- `content`
- `sourceRange`
- `createdAt`
- `keywords`
- `canonicalUserId`（若后续接入多用户）

#### 5.4.3 使用规则

1. 当用户提问明显依赖历史延续时，优先注入摘要，而不是全文长历史。
2. 摘要不足时，再补充最近对话窗口与相关原始记录。
3. 摘要应被视为高价值连续性证据，而不是普通日志缓存。

### 5.5 下一阶段核心能力四：案例蒸馏链路

目标：让系统具备“从具体经验抽象出方法”的成长能力。

#### 5.5.1 基本链路

下一阶段建议引入三层成长对象：

1. `Example`
   - 原始问题与解决方案
2. `Pattern`
   - 多个 Example 的共性步骤、适用边界、常见失败点
3. `Skill`
   - 已验证、可复用的正式方法文档

#### 5.5.2 需求描述

1. 成功解决的问题可沉淀为 `Example`。
2. 当多个 `Example` 高度相似时，系统可尝试生成 `Pattern`。
3. 当 `Pattern` 稳定并被多次复用时，可进一步蒸馏成 `Skill`。
4. `Skill` 仍保持按需加载，不重新变回全量注入模式。

#### 5.5.3 创新点表述

这一能力的重点不是“存更多数据”，而是：

1. 让 Memory-System 从记住案例进化到总结方法。
2. 体现系统具备长期学习和知识升维能力。
3. 为毕设提供区别于普通 RAG 系统的明显创新点。

### 5.6 下一阶段核心能力五：场景化展示能力

目标：展示重点不是“有无记忆对比”，而是“同一系统在不同使用场景下如何发挥作用”。

#### 5.6.1 必须覆盖的展示场景

下一阶段至少准备以下五类演示场景：

1. 个性化建议场景
   - 展示系统如何根据长期偏好生成定制回答
2. 跨会话连续协作场景
   - 展示系统如何承接上一次项目讨论和任务状态
3. 多端统一记忆场景
   - 展示 CLI、飞书、Telegram 之间的记忆连续性
4. 主动服务场景
   - 展示系统如何基于历史任务和目标主动提醒或建议
5. 案例蒸馏场景
   - 展示系统如何从多个 Example 形成 Pattern 或 Skill

其中主动服务场景至少覆盖两类固定展示：

1. 定时提醒型
   - 例如用户说“下周提醒我提交毕设初稿”，到时间后系统主动提醒
2. 记忆驱动建议型
   - 例如系统根据近期主题和任务进展，主动生成“你最近一直在推进记忆治理模块，建议先完成数据结构设计”

#### 5.6.2 展示形态要求

即使不做 Web UI，CLI 也应具备结构化展示能力。

建议新增命令：

1. `/memory-debug`
   - 展示反思决策与证据调用链（支持 `/memory-debug [N]` 查看最近 N 轮）
   - 历史 trace 中 `needs_memory` 采用三态展示：`是/否/unknown`（缺字段不默认映射为“否”）
2. `/memory-timeline`
   - 展示偏好、任务、主题的时间演化
3. `/memory-review`
   - 展示待确认、冲突、低置信度记忆
4. `/memory-report`
   - 输出当前用户的记忆总览与统计信息
5. `/memory-scenes`
   - 输出当前可用于答辩的场景脚本和数据状态
6. `/tasks`
   - 输出待办任务、即将到期任务、最近触发任务和主动建议来源
7. `/memory-insights [limit]`
   - 输出证据检索/使用率、反思命中率和可执行优化建议

#### 5.6.3 答辩展示要求

展示时不应只停留在聊天结果，而应明确表现：

1. 用户当前处于什么场景
2. 系统为什么需要过去的信息
3. 调用了哪些过去证据
4. 最终回答如何受到这些证据影响
5. 系统有没有把新的经验再次沉淀回记忆

对于主动服务类展示，还必须明确表现：

1. 任务是如何从自然语言中被识别和创建的
2. 到时间后系统如何主动触发提醒
3. 系统主动建议依赖了哪些长期记忆、摘要或任务状态
4. 主动输出不是随机生成，而是由记忆证据驱动

### 5.7 下一阶段核心能力六：自然语言定时任务与主动服务

目标：让系统从“能保存任务”升级为“能理解自然语言中的任务需求，并在合适时机主动服务”。

#### 5.7.1 功能范围

下一阶段需要同时支持两种能力：

1. 用户通过自然语言创建定时任务
2. 系统基于记忆与任务状态主动生成提醒或建议

#### 5.7.2 自然语言定时任务

需求描述：

1. 当用户表达时间相关意图时，系统应能识别任务创建需求。
2. 允许用户使用自然语言直接下达任务，例如：
   - “下周提醒我提交毕设初稿”
   - “明天下午三点提醒我给导师发消息”
   - “周五晚上提醒我整理答辩 PPT”
3. 系统需要通过 LLM 将自然语言解析为结构化定时任务。
4. 若信息不完整，可由模型给出合理默认值，或要求补充最少必要信息。
5. 已创建任务必须可查询、可去重、可触发、可追溯来源。

建议字段：

- `title`
- `detail`
- `dueAt`
- `sourceMessage`
- `sourcePlatform`
- `sourceConversationId`
- `sourceSenderId`
- `taskIntent`
- `taskCategory`

说明：

1. 该能力已具备基础 `create_task(...)` 工具链路，下一阶段重点是把它正式纳入毕设主流程和展示脚本。
2. 自然语言建任务必须被视为 Memory-System 的组成能力，而不是孤立工具能力。

#### 5.7.3 主动提醒与主动建议

需求描述：

1. 当任务到期时，系统应主动向原会话或原平台发出提醒。
2. 除到期提醒外，系统还应支持基于长期记忆和近期摘要生成“主动建议”。
3. 主动建议必须依赖明确的记忆证据，例如：
   - 最近持续出现的项目主题
   - 长期未完成的任务
   - 即将到来的里程碑
   - 用户一贯偏好的工作方式
4. 主动建议应尽量具体、可执行，避免空泛的鸡汤式输出。

主动建议示例：

- “你最近一直在推进记忆治理模块，建议先完成数据结构设计，再补 CLI 审核命令。”
- “你本周已经多次讨论答辩材料，建议今天先整理场景化展示脚本。”

#### 5.7.4 主动服务的展示要求

主动服务场景在答辩中必须能展示三件事：

1. 任务如何由自然语言生成
2. 系统如何在正确时间主动提醒
3. 系统如何基于记忆主动给出阶段性建议

#### 5.7.5 与现有模块的关系

建议落点：

1. `task/ScheduledTaskService.java`
2. `tool/CreateTaskTool.java`
3. `llm/LlmExtractionService.java`
4. `cli/ConversationCli.java`
5. `im/ImRuntimeService.java`

说明：

1. 任务解析属于 LLM 结构化提取能力。
2. 任务触发属于调度与消息推送能力。
3. 主动建议属于 Memory Reflection 之后的主动服务扩展能力。

#### 5.7.6 飞书主动提醒增强调研（2026-03-29）

调研目标：将 Task 从“到点提醒一次”升级为“可追踪、可互动、可闭环”的飞书主动提醒能力。

当前实现现状（已具备）：

1. 已具备飞书文本消息发送能力（`im.message.create` 文本）。
2. 已具备飞书入站能力（长连接 + webhook 事件解析）。
3. 已具备任务到期触发与推送主链路（`ScheduledTaskReminderJob`）。
4. 已具备跨平台统一身份与会话绑定能力，可用于推送目标定位。

调研结论（基于飞书开放平台能力与当前工程可行性）：

1. 飞书主动提醒可分为三层能力：
   - `消息发送层`：文本/富文本/交互卡片
   - `交互回传层`：用户在卡片上点击“完成/延期”等动作
   - `状态更新层`：回写任务状态并更新后续提醒策略
2. 当前工程距离“完整主动提醒”主要缺口不在接入，而在：
   - 任务状态机不够细（仅 pending/triggered/cancelled）
   - 缺少“提前提醒/重复提醒/升级提醒”策略
   - 缺少飞书交互卡片回调处理（按钮动作无法闭环）

建议能力矩阵（优先级从高到低）：

1. P1（高优先级，1 周内可落地）— 多阶段主动提醒
   - 增加提醒时机：`T-24h`、`T-1h`、`DueNow`、`OverdueDaily`
   - 增加任务状态：`PENDING / IN_PROGRESS / DONE / SNOOZED / OVERDUE / CANCELLED`
   - 在 `/tasks` 中展示“下次提醒时间、已提醒次数、最近提醒渠道”

2. P2（高优先级，1~2 周）— 飞书交互卡片提醒
   - 任务提醒改为卡片（按钮：完成、延期 1 小时、明天提醒、忽略）
   - 卡片中展示：任务标题、截止时间、来源会话、推荐动作
   - 卡片发送失败自动降级为文本提醒（保证稳定性）

3. P3（中优先级，2 周）— 交互回传闭环
   - 新增卡片回调入口（接收按钮动作）
   - 回调后更新任务状态并写入操作审计
   - 同步更新周报统计（完成率、拖延率、平均响应时长）

推荐数据结构扩展（Task 维度）：

- `status`：扩展为 `PENDING/IN_PROGRESS/DONE/SNOOZED/OVERDUE/CANCELLED`
- `nextReminderAt`
- `lastRemindedAt`
- `reminderCount`
- `reminderPolicy`（如 `at_due`、`before_1h`、`before_24h`、`overdue_daily`）
- `feishuMessageId`（用于卡片更新）
- `lastActionBy` / `lastActionAt` / `lastActionType`（交互审计）

推荐模块落点：

1. `task/ScheduledTaskService.java`
   - 扩展状态机、提醒策略计算、延期逻辑
2. `task/ScheduledTaskReminderJob.java`
   - 从“到点触发”改为“策略触发”（支持提前/逾期提醒）
3. `im/feishu/FeishuOutboundClient.java`
   - 新增 `sendCard(...)`，保留 `sendText(...)` 兜底
4. `im/feishu/FeishuWebhookController.java`（或新增 callback controller）
   - 处理卡片按钮回调，驱动任务状态变更
5. `cli/CliRunner.java`
   - 增强 `/tasks` 输出（状态、提醒计划、交互轨迹）

验收标准（建议）：

1. 用户在飞书收到任务卡片提醒，可点击“完成/延期”。
2. 点击后任务状态在系统中实时更新，并可在 `/tasks` 查询。
3. 对逾期任务可按策略自动再次提醒，且不产生重复轰炸。
4. 周报中可看到“完成任务数、拖延任务数、提醒响应率”。

调研参考（官方入口，实施前需按最新文档复核字段）：

1. [飞书开放平台文档首页](https://open.feishu.cn/document/)
2. [IM 消息发送（im.message.create）](https://open.feishu.cn/document/server-docs/im-v1/message/create)
3. [事件订阅指南](https://open.feishu.cn/document/server-docs/event-subscription-guide/overview)

### 5.8 下一阶段核心能力七：记忆治理与可信度增强

目标：保证被系统调用的记忆足够可信、可审计、可修正。

建议扩展字段：

- `status`：`ACTIVE / PENDING / ARCHIVED / FORGOTTEN`
- `sourcePlatform`
- `sourceMessageId`
- `lastVerifiedAt`
- `conflictWith`
- `verificationNote`
- `confidenceDecayScore`

需求描述：

1. 冲突记忆进入审核队列，而不是直接覆盖。
2. 低置信度记忆应允许进入待验证状态。
3. 同一记忆被多次证实时，可提升稳定度。
4. 长期未被证实的记忆可逐步衰减。

说明：

1. 该模块为 `Memory Reflection` 提供可信记忆基础。
2. 没有治理能力的记忆系统，后续反思与主动服务都容易失真。

### 5.9 下一阶段核心能力八：评测与可观测

目标：为毕设提供可量化、可复现的实验支撑。

建议评测维度：

1. `Memory Reflection` 判断准确率
2. 个性化问题上的记忆调用收益
3. 连续性场景中的摘要有效性
4. 案例蒸馏质量
5. 主动提醒有效性
6. 记忆证据解释完整性

建议产物：

1. `scripts/` 下场景化回放脚本
2. `src/test/` 下固定问答样例与期望输出
3. CLI 证据洞察命令（建议：`/memory-insights [limit]`）
4. 可直接用于论文的统计口径和实验说明

#### 5.9.1 实用增强：`/memory-insights`（证据质量洞察）

设计目标：

1. 把 `memory_evidence_traces.jsonl` 从“日志”提升为“可运营数据”
2. 直接回答“检索到了什么、真正用了什么、为什么没用上”
3. 为 prompt 调优与检索策略调整提供可执行建议

设计思路（搜索与收敛）：

1. 先复用已有 `MemoryTraceInsightService` 聚合能力，避免重复实现统计逻辑
2. 在 `CliRunner` 暴露 `/memory-insights [limit]`，提供近 N 条窗口分析
3. 输出统一四类证据的 `used/retrieved` 使用率：`insights/examples/skills/tasks`
4. 增加用途诊断（按 `evidence_purpose` 分组），定位“哪类场景记忆效果差”
5. 增加窗口趋势对比（前半窗口 vs 后半窗口），直接识别“最近效果变好/变差”
6. 同时输出高频 `skill/purpose/reason` 和建议列表，形成“指标 -> 诊断 -> 改进”闭环

输出建议（CLI）：

1. 样本窗口：`requestedLimit` 与 `sampleSize`
2. 反思命中：`needs_memory` 比例、`memory_loaded` 比例、`unknown` 比例
3. 证据使用率：四类证据的 `used/retrieved` 与百分比
4. 用途诊断：按 `purpose` 输出样本数、加载率、四类证据使用率
5. 趋势对比：前半窗口 vs 后半窗口（`memory_loaded` 与四类证据使用率）
6. 高频模式：Top `skill/purpose/reason`
7. 优化建议：按当前使用率阈值与数据质量信号输出下一步动作

### 5.10 下一阶段核心能力九：内容化产出与知识运营（Step 6/6 调研结论）

目标：将“记忆系统运行数据”持续转化为“可复现内容资产”，支持开发迭代、论文写作、答辩演示三条线并行推进。

调研结论（围绕当前代码与数据资产）：

1. 内容原材料已齐备，短板在生产机制而非数据来源。
2. 内容生产应坚持“命令驱动 + 模板固化 + 版本留痕”，避免人工自由发挥导致口径漂移。
3. 最低成本路径是先做 CLI 侧模板化与目录规范，再决定是否补充导出脚本或 UI。

内容支柱（建议长期固定）：

1. 质量支柱（Quality）
   - 核心输入：`/memory-insights`、`/memory-review`
   - 核心问题：记忆是否被有效检索、有效使用、持续变好
2. 场景支柱（Scene）
   - 核心输入：`/memory-scenes`、`/memory-debug [N]`
   - 核心问题：某类场景下系统如何调用证据、失败点在哪里
3. 闭环支柱（Task Loop）
   - 核心输入：`/tasks`、主动提醒日志、IM 对话记录
   - 核心问题：从任务创建到提醒完成是否形成可解释闭环
4. 研究支柱（Research）
   - 核心输入：上述三类资产
   - 核心问题：如何沉淀成论文图表、实验口径、附录案例

建议新增内容资产池（优先级）：

1. 周复盘卡（P1）
2. 场景案例卡（P1）
3. 任务闭环卡（P1）
4. 回归告警卡（P2，面向趋势变差）
5. 论文图表卡（P2，面向图/表/结论口径）
6. 发布简报卡（P3，面向周报/社媒/项目日志）

模板统一字段（所有卡片最少包含）：

1. 时间窗口（`from/to`）
2. 数据来源（命令与文件路径）
3. 关键指标（最多 5 项）
4. 证据片段（trace id / 轮次 / 场景名）
5. 结论（仅 1-3 条）
6. 下一步动作（带 owner 与截止日期）

建议目录规范（新增）：

1. `.memory/content-assets/weekly/`：周复盘卡
2. `.memory/content-assets/scenes/`：场景案例卡
3. `.memory/content-assets/tasks/`：任务闭环卡
4. `.memory/content-assets/regressions/`：回归告警卡
5. `.memory/content-assets/paper/`：论文图表卡

推荐实施原则：

1. 可复现：每条结论均可回放到命令输出与原始文件证据。
2. 可对比：必须包含“优化前后”或“有记忆/无记忆”对照。
3. 可追责：每个改进动作必须绑定 owner、时间和验收指标。
4. 可复用：模板字段稳定，命名稳定，便于长期累计与检索。

#### 5.10.1 内容选题矩阵（围绕记忆系统主线）

建议将高频内容选题固定为以下 10 类，避免“每周重新想题”导致效率损失：

1. 反思准确性（`needs_memory` 判定正确率、误判样例）
2. 证据利用率（`retrieved -> used` 转化差距）
3. 个性化质量（同问题“有画像/无画像”输出差异）
4. 连续性质量（摘要命中后跨会话承接效果）
5. 治理健康度（冲突、待审核、低置信度占比变化）
6. 任务闭环效率（创建到完成链路时长、延期率）
7. 主动服务有效性（提醒触达率、建议采纳率）
8. 场景失败复盘（失败触发条件、修复动作、复测结果）
9. 结构化兼容性（legacy trace 解析成功率）
10. 论文口径沉淀（图表口径与 CLI 指标一致性）

#### 5.10.2 命令驱动内容流水线（SOP）

建议固化“采集 -> 草稿 -> 复核 -> 入库”的最小流程：

1. 采集（5 分钟）
   - 执行 `/memory-insights [limit]`、`/memory-review`、`/memory-scenes`、`/tasks`
2. 草稿（3 分钟）
   - 按模板填充“时间窗口、指标、证据片段、结论、动作”
3. 复核（2 分钟）
   - 检查每条结论是否存在可回放证据（trace / 摘要 / 任务）
4. 入库（1 分钟）
   - 写入 `.memory/content-assets/**` 并更新索引文件

索引建议：新增 `.memory/content-assets/index.md`，记录每张卡的文件名、类型、时间窗口、关键结论、关联命令。

#### 5.10.3 周执行节奏（可持续运营）

建议固定周节奏，保证“持续产出”而非临时突击：

1. 周一：产出 1 份周复盘卡（质量支柱）
2. 周三：产出 1 份场景案例卡（场景支柱）
3. 周五：产出 1 份任务闭环卡或回归告警卡（闭环支柱）
4. 每两周：产出 1 份论文图表卡（研究支柱）

最低产能基线（建议）：

1. 每周至少 2 份内容卡（其中至少 1 份为场景案例卡）
2. 每月累计至少 8 份可复现内容卡
3. 每月至少覆盖 5 个不同场景主题（避免内容同质化）

#### 5.10.4 对外内容产品化（30 天执行版）

目标：把“内部复盘卡”升级为“可发布内容”，形成稳定的内容漏斗：`数据证据 -> 案例脚本 -> 对外内容 -> 用户反馈 -> 反向优化`。

内容产品分层（建议）：

1. L1 短内容（每周 2-3 条）
   - 形态：进展快报/指标截图解读/单条失败复盘
   - 来源：周复盘卡、回归告警卡
2. L2 中内容（每周 1 条）
   - 形态：场景拆解文（问题 -> 证据 -> 修复 -> 结果）
   - 来源：场景案例卡、任务闭环卡
3. L3 长内容（每两周 1 条）
   - 形态：技术长文或答辩演示稿（方法论 + 数据口径 + 对照实验）
   - 来源：论文图表卡与多期复盘卡聚合

30 天选题发布排期（建议）：

1. 第 1 周：记忆反思与证据视图
   - 主题 A：`needs_memory` 误判案例与修复前后对比
   - 主题 B：`retrieved -> used` 转化率提升实践
2. 第 2 周：连续性与任务闭环
   - 主题 C：跨会话承接案例（有摘要 vs 无摘要）
   - 主题 D：自然语言建任务到到期提醒的全链路
3. 第 3 周：治理与可观测
   - 主题 E：冲突记忆治理案例（冲突发现 -> 人工裁决 -> 生效）
   - 主题 F：`/memory-insights` 趋势诊断如何驱动 prompt 优化
4. 第 4 周：主动服务与答辩素材
   - 主题 G：主动建议命中案例与采纳率复盘
   - 主题 H：五分钟答辩演示脚本（固定输入、固定观测点、固定结论）

统一发布模板（新增约束）：

1. 开头必须给出“场景问题 + 数据窗口 + 对照口径”
2. 中段必须给出“证据链路”（命令、trace、关键字段）
3. 结尾必须给出“改进动作 + 下一次验证时间”
4. 禁止无数据结论；每条结论至少绑定一个可回放证据

内容效果 KPI（最低门槛）：

1. 产出 KPI：每月 >= 8 条可发布内容（含 >= 2 条长内容）
2. 质量 KPI：每条内容至少 1 个可复现实验或回放脚本
3. 闭环 KPI：每月 >= 3 条内容反向进入开发 backlog 并完成验证
4. 复用 KPI：答辩与论文材料中 >= 70% 图表可直接复用内容资产

#### 5.10.5 内容增长飞轮（调研深化）

目标：让“内容产出”不只服务记录，还能持续反哺系统优化与对外影响力。

飞轮结构（固定链路）：

1. 采样：从 `trace/insights/tasks/reminders` 抽取本周高信号样本
2. 归因：定位“为什么成功/失败”（reflection、evidence、治理状态）
3. 产品化：拆分为 L1/L2/L3 内容并绑定受众
4. 反馈：收集评论/私信/复现实验反馈，沉淀可执行问题
5. 回流：转成 backlog，进入修复和复测，再产生下一批样本

内容来源映射（建议固定）：

1. `/memory-insights` -> 趋势和回归 -> 周复盘卡 / 指标解读短内容
2. `/memory-debug [N]` -> 单轮证据链 -> 失败复盘卡 / 场景拆解文
3. `/memory-scenes` -> 跨轮场景承接 -> 连续性案例卡 / 演示脚本
4. `/tasks` + 主动提醒日志 -> 闭环效率 -> 任务闭环卡 / 主动服务案例
5. `src/test/**` + 回放脚本 -> 可复现实验 -> 论文图表卡 / 方法说明文

#### 5.10.6 内容实验框架（确保“可增长”）

建议围绕内容本身做轻量实验，避免只追求产量：

1. 标题实验：同一结论采用“问题导向/结果导向”两种标题，比较点击与互动
2. 结构实验：同一案例比较“先结论后证据”与“先问题后修复”留存差异
3. 证据密度实验：比较“1 个证据点”与“3 个证据点”对复现率的影响
4. 受众实验：同一素材分别面向开发者/答辩老师两类叙述，比较理解成本

实验记录最小字段（建议）：

1. `experiment_id`
2. `hypothesis`
3. `variant_a / variant_b`
4. `success_metric`（点击率、收藏率、复现成功率、反馈转 backlog 数）
5. `result`（保留/淘汰/继续观察）

#### 5.10.7 内容资产治理补充（避免失控）

建议在内容资产层补三条治理约束：

1. 新鲜度约束：超过 30 天未复测的结论标记 `stale`，发布前必须复测
2. 去重约束：同主题 14 天内不得重复发布“同证据同结论”内容
3. 撤回约束：发现结论不可复现时，72 小时内补“更正卡”或撤回原内容

建议在 `.memory/content-assets/index.md` 增加字段：

1. `content_id`
2. `pillar`（quality/scene/task/research）
3. `publish_level`（L1/L2/L3）
4. `evidence_refs`
5. `verification_status`（verified/stale/retracted）
6. `backlog_refs`

#### 5.10.8 记忆系统内容产品矩阵（调研补充）

目标：把“可复盘素材”进一步升级为“可持续栏目”，避免内容只停留在零散案例。

建议固定四条内容产品线（按受众区分）：

1. 开发者线（Builder）
   - 核心产物：实现拆解、指标诊断、失败复盘
   - 优先来源：`/memory-insights`、`/memory-debug [N]`、`src/test/**`
2. 用户线（User）
   - 核心产物：真实场景前后对比、使用说明、注意事项
   - 优先来源：`/memory-scenes`、`/tasks`、主动提醒日志
3. 答辩线（Defense）
   - 核心产物：5 分钟演示脚本、固定输入输出、观测点说明
   - 优先来源：场景案例卡、论文图表卡、回放脚本
4. 研究线（Research）
   - 核心产物：实验口径、图表、统计结论、威胁与局限
   - 优先来源：周复盘卡聚合、内容实验记录、评测脚本输出

栏目化建议（每月固定）：

1. `记忆反思误判周报`（每周 1 篇，开发者线）
2. `证据利用率优化日志`（每周 1 篇，开发者线）
3. `场景闭环案例`（每周 1 篇，用户线）
4. `主动服务复盘`（每两周 1 篇，用户线）
5. `答辩演示快照`（每两周 1 篇，答辩线）
6. `实验与图表更新`（每两周 1 篇，研究线）

内容命名建议（统一检索前缀）：

1. `builder_YYYY-MM-DD_topic.md`
2. `user_YYYY-MM-DD_topic.md`
3. `defense_YYYY-MM-DD_topic.md`
4. `research_YYYY-MM-DD_topic.md`

#### 5.10.9 12 周内容执行路线（Step 6/6 落地版）

目标：在 12 周内形成“稳定产出 + 可验证增长 + 反哺开发”的闭环，而非一次性冲刺。

阶段一（第 1-4 周）：建立稳定产能

1. 每周固定产出 3 份内容卡：周复盘卡 + 场景案例卡 + 任务闭环卡
2. 每周至少 1 篇可发布内容（L1/L2 任一）
3. 每周至少 1 条内容反馈进入 backlog

阶段二（第 5-8 周）：提升复用与转化

1. 每周固定产出 1 篇 L2 场景拆解文
2. 每两周固定产出 1 篇 L3 长内容（技术长文或演示稿）
3. 累计形成至少 10 条“可复现改进结论”（可回放到 trace 或测试）

阶段三（第 9-12 周）：答辩与论文收敛

1. 固定输出答辩素材包：脚本、关键截图、指标表、风险说明
2. 论文图表卡覆盖核心章节：反思、证据、闭环、治理、实验
3. 对全部高频结论完成一次“30 天内复测”，剔除过期结论

12 周验收指标（建议）：

1. 内容资产总量 >= 24 份（含 >= 8 份场景案例卡）
2. 可发布内容 >= 12 条（含 >= 3 条 L3）
3. 内容反馈转 backlog >= 12 条，完成验证 >= 8 条
4. 关键答辩图表中 >= 80% 可追溯到 `.memory/content-assets/index.md`

#### 5.10.10 围绕记忆系统扩展“更多内容”的方向池（调研补充）

目标：在不偏离 Memory-System 主线的前提下，把内容从“复盘记录”扩成“可持续栏目”。

建议新增 8 类内容资产（与现有五类卡片互补）：

1. 记忆对照实验卡（P1）
   - 主题：`无记忆 vs 四层记忆 vs 四层+治理`
   - 证据入口：`/memory-insights` + `src/test/**` 回放结果
2. 误判剖析卡（P1）
   - 主题：`needs_memory` 误判样本、根因、修复
   - 证据入口：`/memory-debug [N]`
3. 证据利用率优化卡（P1）
   - 主题：`retrieved -> used` 低转化问题与优化动作
   - 证据入口：`/memory-insights`
4. 治理决策卡（P2）
   - 主题：冲突记忆裁决、置信度变化、状态迁移
   - 证据入口：`/memory-review`
5. 主动服务命中卡（P2）
   - 主题：提醒触达、建议采纳、失败场景
   - 证据入口：`/tasks` + 主动提醒日志
6. 里程碑时间线卡（P2）
   - 主题：某阶段从问题到修复的连续演进
   - 证据入口：`/memory-timeline` + 迭代记录
7. 答辩演示快照卡（P1）
   - 主题：固定输入、固定观测点、固定结论
   - 证据入口：`/memory-scenes` + 回放脚本
8. 论文威胁与局限卡（P3）
   - 主题：失效边界、外部有效性、复现实验限制
   - 证据入口：论文图表卡 + 回归记录

扩展原则：

1. 每新增一类内容，必须绑定至少一个固定命令入口。
2. 每条内容必须可回放，不允许“纯主观经验总结”。
3. 每周新增内容中至少 50% 来自“失败/回归/修复”而非仅成功案例。

#### 5.10.11 Step 6/6 的 7 天落地方案（可立即执行）

Day 1（目录与模板）：

1. 初始化 `.memory/content-assets/**` 子目录与 `index.md`
2. 建立 8 类新增卡片模板（含统一字段）

Day 2（首批采样）：

1. 执行 `/memory-insights 30`、`/memory-debug 10`、`/memory-review`
2. 产出 1 份误判剖析卡 + 1 份证据利用率优化卡

Day 3（场景化）：

1. 执行 `/memory-scenes`、`/tasks`
2. 产出 1 份场景案例卡 + 1 份主动服务命中卡

Day 4（对照实验）：

1. 回放固定测试集，补齐“有/无记忆对照”
2. 产出 1 份记忆对照实验卡

Day 5（治理与时间线）：

1. 执行 `/memory-review`、`/memory-timeline`
2. 产出 1 份治理决策卡 + 1 份里程碑时间线卡

Day 6（发布与回流）：

1. 从 6-8 份卡中提炼 2 条 L1 + 1 条 L2 可发布内容
2. 48 小时内沉淀反馈并转 backlog

Day 7（周盘点）：

1. 做一次去重与复测，标记 `verified/stale/retracted`
2. 更新 `index.md` 的内容线覆盖率与回流率

7 天验收门槛（最低）：

1. 新增内容卡 >= 6 份，且覆盖 >= 4 个主题方向
2. 至少 2 份卡片直接转化为开发任务并完成验证计划
3. 100% 新卡片具备可回放证据引用（命令输出或文件证据）

#### 5.10.12 Step 6/6 内容扩展蓝图（第二层：栏目化与系列化）

目标：在已有“卡片化产出”基础上，继续扩成“可持续栏目 + 可复用素材包”，让内容不仅能复盘，还能连续输出并反哺开发。

建议新增 6 条系列栏目（围绕记忆系统主线）：

1. `Memory Weekly Benchmark`（每周）
   - 关注：`needs_memory` 判定、`retrieved->used` 转化、场景通过率
   - 证据入口：`/memory-insights` + 固定回放测试
2. `Memory Failure Library`（每周）
   - 关注：误判、证据浪费、治理冲突、主动服务失效
   - 证据入口：`/memory-debug [N]` + `/memory-review`
3. `Scene Replay Pack`（每两周）
   - 关注：同一场景跨版本表现变化（输入脚本固定）
   - 证据入口：`/memory-scenes` + 回放脚本 + 测试输出
4. `Governance Decision Log`（每两周）
   - 关注：冲突裁决、置信度变化、状态迁移是否合理
   - 证据入口：`/memory-governance` + `pending_explicit_memories.jsonl`
5. `Proactive Hit Report`（每两周）
   - 关注：提醒触发率、建议采纳率、误触达场景
   - 证据入口：`/tasks` + `/proactive-reminders`
6. `Defense Snapshot`（每周）
   - 关注：固定 5 分钟演示素材是否 `demo_ready`
   - 证据入口：`/memory-report` + `/memory-debug` + 固定输入脚本

系列化模板建议新增字段：

1. `series_name`：所属栏目名
2. `episode_no`：期号（如 `2026W14`）
3. `baseline_ref`：对照基线（上期/上版本）
4. `delta_summary`：本期变化（变好/变差/持平）
5. `decision`：保留/修复/下线

系列化验收门槛（建议）：

1. 连续 4 周每周至少产出 1 条系列栏目内容
2. 系列内容中 >= 70% 含明确“对照基线 + 变化结论”
3. 每月至少 3 条系列结论转化为 backlog 并进入验证
4. 答辩素材池中 100% 条目具备 `series_name + episode_no + demo_ready`

#### 5.10.13 Step 6/6 内容扩展蓝图（第三层：资产包化与回放即内容）

目标：把“栏目化内容”进一步升级为“可直接复用的资产包”，让每条内容都能被快速复述、复测、复演示。

建议新增 5 类资产包（与卡片/栏目互补）：

1. 演示剧本包（P1）
   - 组成：固定输入脚本、预期观测点、常见异常分支、5 分钟讲解口径
   - 证据入口：`/memory-scenes`、`/memory-debug [N]`、回放脚本
2. 诊断问答包（P1）
   - 组成：高频故障问答（症状 -> 排查命令 -> 根因 -> 修复动作）
   - 证据入口：`/memory-insights`、`/memory-review`
3. 对照实验包（P1）
   - 组成：无记忆/有记忆/有治理三组固定样本与评分口径
   - 证据入口：`src/test/**`、评测脚本、`/memory-insights`
4. 论文附录包（P2）
   - 组成：图表原始数据、统计口径、威胁与局限、复现实验说明
   - 证据入口：论文图表卡、回归告警卡、实验记录
5. 发布素材包（P2）
   - 组成：L1/L2/L3 内容草稿、封面句式、证据截图位、反馈回流入口
   - 证据入口：各内容卡与 `index.md`

资产包最小字段（建议强制）：

1. `asset_pack_id`：唯一 ID（如 `pack_2026W14_demo_01`）
2. `source_content_ids`：来源内容卡/系列条目 ID 列表
3. `command_bundle`：回放命令序列（可直接执行）
4. `expected_observations`：预期观测点（至少 3 条）
5. `evidence_snapshot`：关键证据快照引用（trace/文件/测试输出）
6. `owner` + `next_review_at`：责任人与下次复测时间

执行约束（建议）：

1. 每周至少新增 1 个资产包，其中至少每两周 1 个演示剧本包。
2. 每个资产包必须绑定至少 1 个“失败样本”或“回归样本”。
3. 每个资产包必须在 7 天内完成一次回放验证并更新状态。
4. 未通过回放验证的资产包不得进入答辩或对外发布池。

#### 5.10.14 Step 6/6 内容扩展蓝图（第四层：问题库化与专题化）

目标：在“内容卡 + 系列栏目 + 资产包”之上，再建立“可长期复用的问题库与专题季”，让内容生产从周度产出升级为季度级知识产品。

建议新增 6 类内容方向（围绕记忆系统主线）：

1. `Memory-System 100 问`（P1）
   - 形态：高频问题短答（概念/调试/治理/实验）
   - 证据入口：`/memory-insights`、`/memory-debug [N]`、`开发文档.md`
2. `反例与失效边界库`（P1）
   - 形态：失败样本卡（触发条件 -> 错误行为 -> 修复状态）
   - 证据入口：`/memory-review`、`/memory-debug [N]`、回归测试
3. `模块深潜专栏`（P1）
   - 形态：按模块拆解（memory/rag/task/im/prompt/tool）实现与演进
   - 证据入口：`src/main/**` + `/memory-insights` 趋势结论
4. `对照实验周报`（P2）
   - 形态：固定样本跨版本对照（基线/改动/结果/结论）
   - 证据入口：`src/test/**`、评测脚本、`/memory-insights`
5. `答辩问答弹药包`（P1）
   - 形态：高频评审问题 + 证据回答 + 演示入口
   - 证据入口：演示剧本包、论文附录包、`/memory-scenes`
6. `论文章节素材池`（P2）
   - 形态：章节级素材列表（图表/案例/局限/威胁）
   - 证据入口：论文图表卡、里程碑时间线卡、资产包索引

专题化执行建议（14 天冲刺）：

1. Day 1-2：从现有内容资产中筛出 20 个高频问题，初始化 `100 问` 索引。
2. Day 3-4：补齐 5 条失败样本，形成最小 `反例与失效边界库`。
3. Day 5-7：按模块产出 2 篇深潜专栏（优先 memory 与 task）。
4. Day 8-10：完成 1 份跨版本对照实验周报并回流至少 1 条 backlog。
5. Day 11-12：整理 1 套答辩问答弹药包（至少 15 问）。
6. Day 13-14：将素材映射到论文章节池，标记 `ready/draft/missing`。

新增索引字段（建议）：

1. `topic_cluster`：选题簇（reflection/evidence/governance/task/proactive/defense/research）
2. `content_form`：内容形态（qa/case/deep_dive/benchmark/snapshot）
3. `reuse_target`：复用目标（dev/defense/paper/public）
4. `proof_level`：证据等级（L1 命令输出 / L2 trace+测试 / L3 跨版本复测）
5. `next_refresh_at`：下次更新截止时间

验收门槛（建议）：

1. 连续 2 周每周至少新增 1 类“问题库/专题化”内容。
2. `100 问` 中 >= 80% 条目具备命令证据引用与回放入口。
3. 每两周至少 1 条专题结论转化为开发任务并进入验证。
4. 答辩问答弹药包中 100% 条目可追溯到内容卡或资产包。

#### 5.10.15 Step 6/6 内容扩展蓝图（第五层：渠道化分发与自动化生成）

目标：在“内容卡 -> 系列栏目 -> 资产包 -> 问题库/专题”基础上，进一步补齐“多渠道分发 + 半自动生成”，把内容体系升级为可规模化运营。

建议新增 8 类内容形态（围绕记忆系统主线）：

1. `Memory 日报快照`（P1）
   - 形态：当天关键指标与异常一页摘要
   - 证据入口：`/memory-insights 20`、`/memory-review`
2. `Memory 周报简报`（P1）
   - 形态：本周趋势、关键修复、下周动作
   - 证据入口：`/memory-insights`、`/memory-timeline`、`开发实现process.md`
3. `修复公告卡`（P1）
   - 形态：问题症状、根因、修复、复测结果
   - 证据入口：`/memory-debug [N]`、`src/test/**`、提交记录
4. `基准榜单卡`（P2）
   - 形态：固定场景通过率与版本对照榜单
   - 证据入口：回放脚本、`/memory-insights`、评测输出
5. `谣言澄清卡`（P2）
   - 形态：常见误解 -> 证据反驳 -> 正确口径
   - 证据入口：`Memory-System 100 问`、失败边界库
6. `成本效率账本卡`（P2）
   - 形态：token/时延/成功率三指标联动复盘
   - 证据入口：日志统计、`/memory-report`、评测脚本
7. `IM 周度推送包`（P2）
   - 形态：飞书/Telegram 周度更新摘要模板
   - 证据入口：内容资产索引、IM 对话与反馈数据
8. `答辩冲刺提纲包`（P1）
   - 形态：7 分钟讲解结构、Q&A、演示切换点
   - 证据入口：演示剧本包、答辩问答弹药包

自动化最小流水线（建议）：

1. 采集层：固定命令批量采集（`/memory-insights`、`/memory-review`、`/memory-scenes`、`/tasks`）
2. 组装层：按模板自动填充“指标块 + 证据块 + 结论草稿”
3. 审核层：人工仅审三项（证据可回放、口径一致、结论可复测）
4. 分发层：按渠道产出 L1/L2/L3 或 IM 摘要包
5. 回流层：渠道反馈入 `index.md` 并回写 backlog

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `distribution_channel`（repo/im/social/defense/paper）
2. `audience_stage`（cold/warm/hot/defense）
3. `render_template`（daily/weekly/fix-note/benchmark/qa）
4. `auto_generated`（true/false）
5. `publish_url`（可为空）
6. `feedback_score`（0-5）
7. `feedback_volume`（互动或反馈数量）
8. `next_distribution_at`（下次分发时间）

渠道化执行节奏（建议）：

1. 每日 1 条日报快照（内部）
2. 每周 1 份周报简报 + 1 份修复公告卡
3. 每两周 1 份基准榜单卡 + 1 份成本效率账本卡
4. 每周至少 1 次 IM 周度推送

验收门槛（建议）：

1. 连续 2 周满足“每周 >= 3 条可分发内容 + >= 1 条反馈回流 backlog”。
2. 渠道内容中 100% 条目具备 `distribution_channel + evidence_refs + verification_status`。
3. 自动组装内容占比 >= 50%，且人工复核后可发布率 >= 80%。
4. 每月至少 2 条渠道反馈转化为开发优化并完成验证。

#### 5.10.16 Step 6/6 内容扩展蓝图（第六层：专题季化与对照评测产品化）

目标：在“渠道化分发”基础上，进一步把内容升级为“可长期复用的专题季资产”，形成面向开发、答辩、论文三线统一复用的高密度内容产品。

建议新增 8 类专题季资产（围绕记忆系统主线）：

1. `Memory-System 季度状态报告`（P1）
   - 形态：季度版本变化、关键指标对照、风险与改进路线
   - 证据入口：`/memory-insights`、`/memory-report`、`开发实现process.md`
2. `跨版本对照评测集`（P1）
   - 形态：同一脚本在不同版本的结果对照与结论归因
   - 证据入口：回放脚本、`src/test/**`、评测输出
3. `失败模式周历`（P1）
   - 形态：按周沉淀失败模式、触发条件、修复状态
   - 证据入口：`/memory-debug [N]`、回归卡、测试记录
4. `Evidence 质量榜单`（P2）
   - 形态：各证据类型利用率与浪费率排名
   - 证据入口：`/memory-debug [N]`、`/memory-insights`
5. `主动服务命中季报`（P2）
   - 形态：提醒/建议命中率、误触发分析、优化动作
   - 证据入口：`/proactive-reminders`、`/tasks`、IM 反馈
6. `论文图表季更包`（P1）
   - 形态：可直接复用的图表结论与口径说明
   - 证据入口：内容资产索引、评测脚本、命令截图位
7. `答辩演示季度脚本包`（P1）
   - 形态：固定开场、场景切换、故障兜底话术与回放命令
   - 证据入口：资产包目录、答辩快照卡、问题库
8. `Memory-System 术语与口径手册`（P2）
   - 形态：统一定义、反例说明、禁用表述
   - 证据入口：`开发文档.md`、问题库、澄清卡

专题季执行机制（建议）：

1. 周层：每周至少新增 1 条“失败模式”与 1 条“对照评测”素材。
2. 双周层：每两周产出 1 份小结（含 baseline 对照与回流任务）。
3. 月层：每月产出 1 份月报（开发线 + 内容线 + 证据线）。
4. 季层：每季度合并为 1 份“季度状态报告 + 资产包目录快照”。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `season_id`（如 `2026Q2`）
2. `season_track`（engineering/defense/paper/ops）
3. `baseline_version`（基线版本号或 commit）
4. `compare_version`（对照版本号或 commit）
5. `metric_delta`（关键指标变化摘要）
6. `evidence_density`（高/中/低）
7. `reusability_level`（single/reusable/packaged）
8. `season_review_status`（draft/reviewed/published）

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 2 条专题季素材 + >= 1 条回流 backlog”。
2. `season_id` 覆盖的条目中 100% 具备 `baseline_version + compare_version + evidence_refs`。
3. 每季度至少完成 1 份跨版本对照评测集，且至少 2 条结论完成复测验证。
4. 答辩与论文使用的核心结论中 >= 80% 可直接映射到专题季资产。

#### 5.10.17 Step 6/6 内容扩展蓝图（第七层：场景赛题化与公开挑战集）

目标：在“专题季资产化”基础上继续升级为“可对外复测的场景挑战集”，让内容不只可读，还可被第三方复现、对照和质疑，进一步放大 Memory-System 的可验证价值。

建议新增 8 类挑战型内容资产（围绕记忆系统主线）：

1. `场景挑战卡`（P1）
   - 形态：固定输入脚本、期望行为、失败判定标准
   - 证据入口：`/memory-scenes`、`/memory-debug [N]`、回放脚本
2. `回归闯关榜`（P1）
   - 形态：按版本统计挑战场景通过率与退化点
   - 证据入口：`src/test/**`、评测输出、`/memory-insights`
3. `误判样本周榜`（P1）
   - 形态：每周 Top N 误判样本与修复状态
   - 证据入口：失败模式周历、trace、日志片段
4. `挑战脚本包`（P1）
   - 形态：可一键执行的输入脚本与观测点模板
   - 证据入口：`scripts/**`、场景案例卡、任务闭环卡
5. `修复前后对照卡`（P1）
   - 形态：同场景“修复前 vs 修复后”证据对照
   - 证据入口：commit、测试结果、命令回放输出
6. `挑战讲解稿`（P2）
   - 形态：用于答辩/分享的 3~5 分钟场景讲解
   - 证据入口：挑战卡、季度脚本包、问题库
7. `外部复测回执卡`（P2）
   - 形态：外部同学/老师复测反馈与偏差记录
   - 证据入口：反馈登记表、复测截图位、IM 反馈
8. `挑战赛季总览`（P2）
   - 形态：月度/季度挑战通过率、退化率、修复效率总览
   - 证据入口：挑战索引、回归榜、专题季报告

挑战集执行机制（建议）：

1. 每周固定新增 >= 2 个挑战场景（至少 1 个失败样本）。
2. 每周固定执行 1 次挑战回放，输出“通过/失败/待复测”三态。
3. 每两周固定沉淀 1 份“修复前后对照卡”，禁止只讲修复结果不讲证据。
4. 每月固定输出 1 份挑战赛季总览，回流 backlog 并标注验证状态。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `challenge_id`（如 `CH-2026-031`）
2. `challenge_scene`（continuity/personalization/followup/retrieval/governance）
3. `expected_outcome`（预期行为摘要）
4. `failure_signature`（失败特征）
5. `fix_commit_ref`（修复提交或 PR 引用）
6. `replay_script_ref`（回放脚本）
7. `external_retest_status`（none/pending/passed/failed）
8. `challenge_score`（0-100）

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 2 个挑战场景 + >= 1 条回流任务”。
2. 挑战索引中 100% 条目具备 `challenge_id + replay_script_ref + evidence_refs`。
3. 每月至少 4 条挑战结论形成开发任务，其中至少 2 条完成验证闭环。
4. 对外展示内容中 >= 70% 可映射到挑战卡或修复前后对照卡。

#### 5.10.18 Step 6/6 内容扩展蓝图（第八层：生态协作化与共创回流）

目标：在“公开挑战集”基础上，继续升级为“可协作、可复测、可回流”的内容生态，让更多外部参与者为 Memory-System 提供真实反馈与高价值样本。

建议新增 8 类生态协作型内容资产（围绕记忆系统主线）：

1. `复测任务单`（P1）
   - 形态：外部参与者可直接执行的复测任务（输入、步骤、预期、提交格式）
   - 证据入口：挑战脚本包、`/memory-debug [N]`、固定回放命令
2. `外部误判回收卡`（P1）
   - 形态：外部复测发现的误判样本、触发条件、重现证据
   - 证据入口：外部复测回执卡、trace、回归测试
3. `修复收益对照卡`（P1）
   - 形态：修复前后在真实复测样本上的收益对照（通过率/误判率/证据利用率）
   - 证据入口：回归闯关榜、`/memory-insights`、测试输出
4. `协作专题共创卡`（P2）
   - 形态：围绕单一主题的多角色共创（开发/使用/评测三视角）
   - 证据入口：专题季资产、问题库、分发反馈
5. `复测贡献榜`（P2）
   - 形态：按周期统计复测样本数量、有效问题数、修复命中数
   - 证据入口：复测任务单、外部回执、backlog 映射
6. `答辩外部证明卡`（P1）
   - 形态：答辩演示可直接引用的“外部复测通过/失败”证据摘要
   - 证据入口：外部复测回执卡、挑战赛季总览
7. `论文外部有效性卡`（P1）
   - 形态：外部复测样本对论文“外部有效性”章节的支持证据
   - 证据入口：专题季报告、复测贡献榜、失败边界库
8. `共创回流追踪卡`（P1）
   - 形态：外部反馈 -> backlog -> 修复 -> 复测通过的闭环追踪
   - 证据入口：`开发实现process.md`、提交记录、挑战卡

生态协作执行机制（建议）：

1. 每周至少发布 1 批复测任务单（不少于 3 个场景）。
2. 每周至少沉淀 1 份外部误判回收卡，禁止只收集“通过样本”。
3. 每两周至少完成 1 份修复收益对照卡，必须包含修复前后对照。
4. 每月输出 1 份复测贡献榜与 1 份共创回流追踪卡，明确“反馈价值 -> 修复收益”。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `collab_batch_id`（如 `CB-2026W14`）
2. `contributor_type`（internal/student/reviewer/user）
3. `retest_case_count`
4. `valid_issue_count`
5. `fix_adoption_count`
6. `external_proof_level`（L1 单次回执 / L2 多次复测 / L3 跨版本稳定）
7. `closure_status`（open/in_progress/verified/archived）
8. `impact_summary`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 1 批复测任务 + >= 1 条外部反馈回流任务”。
2. 协作内容索引中 100% 条目具备 `collab_batch_id + evidence_refs + closure_status`。
3. 每月至少 3 条外部反馈形成开发任务，其中至少 2 条完成验证闭环。
4. 答辩与论文中的关键“可验证性结论”中 >= 60% 可追溯到外部复测或共创回流证据。

#### 5.10.19 Step 6/6 内容扩展蓝图（第九层：基准化资产与公开评测包）

目标：在“生态协作共创”基础上，继续升级为“可版本化、可对照、可复测”的基准化内容资产，让 Memory-System 的内容输出具备长期可比较价值。

建议新增 8 类基准化内容资产（围绕记忆系统主线）：

1. `Memory 基准数据集卡`（P1）
   - 形态：固定场景集、输入模板、标签说明、更新日志
   - 证据入口：挑战卡、复测任务单、误判回收卡
2. `场景标注规范卡`（P1）
   - 形态：连续性/个性化/任务跟进/证据利用等标签定义
   - 证据入口：`/memory-scenes`、`/memory-debug [N]`、历史 trace
3. `评分协议卡`（P1）
   - 形态：通过率、误判率、证据命中率、回放成功率计算口径
   - 证据入口：评测脚本输出、`/memory-insights`、测试报告
4. `回放评测包`（P1）
   - 形态：可重复执行的脚本集合（输入、命令、预期观测点）
   - 证据入口：`scripts/**`、`src/test/**`、内容资产索引
5. `月度基准公报`（P1）
   - 形态：月内版本对照、指标变化、退化警报与修复进度
   - 证据入口：基准运行结果、提交记录、任务闭环卡
6. `回归红榜`（P2）
   - 形态：高频退化样本、复现方式、修复状态
   - 证据入口：失败样本周榜、挑战赛季总览、回归测试
7. `模型/策略对照表`（P2）
   - 形态：不同模型配置或检索策略在同基准上的差异
   - 证据入口：评测脚本、配置快照、指标对照卡
8. `可复现认证卡`（P2）
   - 形态：对外说明“已可复测”的资产批次、环境与结果摘要
   - 证据入口：回放评测包、外部回执、协作追踪卡

基准化执行机制（建议）：

1. 每周至少新增 1 批可评测样本，并完成标签复核。
2. 每两周至少执行 1 轮固定基准回放，产出“通过/退化/待验证”三态结果。
3. 每月固定发布 1 份月度基准公报，明确变化来源与待修复项。
4. 每次公报后 72 小时内完成“结论回流判断”，标注是否进入 backlog 与验证状态。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `benchmark_id`（如 `MB-2026-04`）
2. `dataset_version`（如 `ds-v1.2`）
3. `protocol_version`（如 `proto-v1`）
4. `runner_env`（本地/JDK/模型配置摘要）
5. `model_variant`（模型与关键参数）
6. `score_summary`（核心指标摘要）
7. `reproducibility_badge`（none/internal/external）
8. `benchmark_status`（draft/running/published/retired）

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 1 批样本入库 + >= 1 次基准回放登记”。
2. 基准索引中 100% 条目具备 `benchmark_id + dataset_version + protocol_version + evidence_refs`。
3. 每月至少 3 条基准结论形成开发任务，其中至少 2 条完成验证闭环。
4. 答辩和论文中引用的关键指标中 >= 70% 可映射到月度基准公报或回放评测包。

#### 5.10.20 Step 6/6 内容扩展蓝图（第十层：记忆产品化内容与演示资产库）

目标：在“基准化资产”基础上，进一步形成“可直接交付给不同受众”的记忆系统内容产品，让开发、答辩、论文和对外交流共享同一证据底盘。

建议新增 8 类产品化内容资产（围绕记忆系统主线）：

1. `记忆能力说明卡`（P1）
   - 形态：按能力拆解系统做什么/不做什么（连续性、个性化、任务跟进、证据回放）
   - 证据入口：`开发文档.md`、`/memory-report`、`/memory-insights`
2. `一分钟场景演示卡`（P1）
   - 形态：固定输入脚本 + 关键观测点 + 预期输出摘要，面向快演示
   - 证据入口：`/memory-scenes`、`/memory-debug [N]`、回放脚本
3. `失败到修复故事卡`（P1）
   - 形态：失败样例、定位线索、修复动作、修复后对照
   - 证据入口：挑战卡、回归红榜、提交记录、测试报告
4. `指标解读讲义卡`（P1）
   - 形态：核心指标定义、波动解释、风险阈值、下一步动作
   - 证据入口：月度基准公报、`/memory-insights`、trace 趋势
5. `答辩问答速查卡`（P2）
   - 形态：高频问题 -> 证据回答 -> 可复测命令，避免临场口径漂移
   - 证据入口：问题库、专题稿、回放评测包
6. `论文图表说明卡`（P2）
   - 形态：图表结论、样本边界、复现实验入口、失效条件
   - 证据入口：基准数据集卡、评分协议卡、图表季更包
7. `版本差异发布卡`（P2）
   - 形态：本版本改进点、潜在退化点、回滚策略、验证结果
   - 证据入口：`开发实现process.md`、测试日志、基准对照结果
8. `对外协作邀请卡`（P2）
   - 形态：可复测入口、参与方式、反馈模板、回流规则
   - 证据入口：协作任务单、公开挑战集、共创回流追踪卡

产品化执行机制（建议）：

1. 每周固定产出 1 份“快演示资产”（一分钟场景演示卡或能力说明卡）。
2. 每两周固定产出 1 份“问题解答资产”（失败到修复故事卡或答辩问答速查卡）。
3. 每月固定产出 1 份“对外发布资产”（版本差异发布卡或对外协作邀请卡）。
4. 所有产品化内容必须绑定至少 1 个可复测命令或脚本，不允许只给结论截图。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `product_card_id`（如 `PC-2026W14-01`）
2. `audience_type`（builder/reviewer/defense/research/public）
3. `scenario_script_ref`
4. `qa_bundle_ref`
5. `demo_ready`（true/false）
6. `proof_level`（draft/internal/public）
7. `last_replay_at`
8. `publish_window`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 1 份产品化内容 + >= 1 次回放校验记录”。
2. 产品化索引中 100% 条目具备 `product_card_id + audience_type + evidence_refs + demo_ready`。
3. 每月至少 2 条产品化内容反馈形成开发任务，其中至少 1 条完成验证闭环。
4. 答辩和论文演示池中 100% 条目可映射到“可复测命令/脚本 + 证据来源”。

#### 5.10.21 Step 6/6 内容扩展蓝图（第十一层：选题智能编排与质量闸门）

目标：在“产品化内容资产库”基础上，继续升级为“选题可计算、发布可闸门、回流可量化”的运营层，让内容生产从“有素材”进化为“可持续提效”。

建议新增 8 类运营型内容资产（围绕记忆系统主线）：

1. `选题优先级榜单卡`（P1）
   - 形态：按 `价值/证据完整度/回放成本` 排序的周选题榜单
   - 证据入口：`/memory-insights`、`/memory-review`、`index.md`
2. `内容债清单卡`（P1）
   - 形态：未复测、证据缺失、结论过期条目列表与处理计划
   - 证据入口：`index.md`、`/memory-debug [N]`
3. `复测排班卡`（P1）
   - 形态：本周待复测资产与命令包（按风险优先）
   - 证据入口：`/memory-scenes`、回放脚本、资产包索引
4. `证据缺口告警卡`（P1）
   - 形态：高价值内容但证据不足的缺口清单（缺命令/缺 trace/缺对照）
   - 证据入口：`/memory-insights`、`/memory-report`
5. `结论寿命跟踪卡`（P2）
   - 形态：关键结论从首次发布到失效/复证的生命周期轨迹
   - 证据入口：月度公报、`index.md`、回归记录
6. `回流收益归因卡`（P2）
   - 形态：内容反馈转 backlog 后的修复收益归因（通过率/误判率变化）
   - 证据入口：`开发实现process.md`、测试输出、基准对照
7. `自动周刊草稿包`（P2）
   - 形态：自动拼装“指标块 + 证据块 + 结论草稿”，人工只做质量闸门
   - 证据入口：`/memory-insights`、`/memory-timeline`、`/tasks`
8. `发布后质量回执卡`（P2）
   - 形态：发布后 48 小时质量回执（复现率、疑义点、是否需撤回）
   - 证据入口：渠道反馈、复测任务单、回放结果

执行机制（建议）：

1. 每周一先跑“选题评分”：按 `topic_score` 自动产出 Top 5 选题。
2. 每周三执行“质量闸门”：未通过 `qa_gate_status` 的内容禁止进入发布池。
3. 每周五固定完成“内容债清理”：至少关闭 2 条高风险 `content_debt`。
4. 每两周复核一次“回流收益归因”：确认哪些内容真的推动了系统指标改善。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `topic_score`（0-100）
2. `evidence_completeness`（0-100）
3. `replay_cost`（low/medium/high）
4. `freshness_risk`（low/medium/high）
5. `content_debt_level`（none/minor/major/critical）
6. `qa_gate_status`（pending/passed/blocked）
7. `automation_run_id`
8. `roi_tag`（quality/efficiency/defense/paper/public）

验收门槛（建议）：

1. 连续 4 周满足“每周 Top 5 选题均有评分与证据引用”。
2. 发布池中 100% 条目具备 `qa_gate_status=passed` 与最近回放记录。
3. 每周至少关闭 2 条 `major/critical` 内容债，且有处理留痕。
4. 每月至少 2 条“内容回流收益”完成归因并形成验证结论。

#### 5.10.22 Step 6/6 内容扩展蓝图（第十二层：课程化训练与认证化输出）

目标：在“选题编排 + 质量闸门”基础上，把内容体系进一步升级为“可训练、可考核、可认证”的学习型资产，让 Memory-System 内容不仅能看和复盘，还能用于稳定培养新参与者并放大复用价值。

建议新增 8 类课程化内容资产（围绕记忆系统主线）：

1. `十分钟上手实验卡`（P1）
   - 形态：最小环境、固定命令、预期观测、常见失败点
   - 证据入口：`README.md`、`/memory-report`、`scripts/run-tests.sh`
2. `场景训练题单卡`（P1）
   - 形态：按 continuity/personalization/followup 分类的训练题与标准答案要点
   - 证据入口：`/memory-scenes`、`/memory-debug [N]`、回放脚本
3. `误判纠偏练习卡`（P1）
   - 形态：失败 trace -> 根因假设 -> 修复动作 -> 复测结论
   - 证据入口：`/memory-insights`、回归红榜、测试输出
4. `助教讲解脚本卡`（P2）
   - 形态：每个命令/模块的讲解顺序、关键截图位、易错提醒
   - 证据入口：`开发文档.md`、`开发实现process.md`、CLI 命令输出
5. `评分量表卡`（P2）
   - 形态：训练任务评分维度（证据完整度、复测成功率、结论边界清晰度）
   - 证据入口：内容索引、回放结果、任务闭环卡
6. `训练营周报卡`（P2）
   - 形态：本周训练完成率、常见故障 Top、修复采纳率
   - 证据入口：`/tasks`、内容债清单、回流归因卡
7. `能力认证回执卡`（P2）
   - 形态：通过标准、复测记录、有效期与再认证时间
   - 证据入口：评分量表、复测排班、基准评测包
8. `新成员入门路径图`（P2）
   - 形态：Day1/Day3/Day7 路线、必做任务、达标检查点
   - 证据入口：训练题单、上手实验卡、答辩速查卡

课程化执行机制（建议）：

1. 每周至少产出 1 份训练资产（题单/误判纠偏/讲解脚本三选一）。
2. 每两周至少执行 1 次“训练回放 + 评分量表”校验，产出通过/补训结论。
3. 每月固定产出 1 份训练营周报卡，并给出“下月训练重点”。
4. 对外宣称“可复现能力”的条目必须绑定认证回执卡与最近复测时间。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `training_asset_id`（如 `TA-2026W14-01`）
2. `skill_track`（continuity/personalization/followup/governance）
3. `difficulty_level`（L1/L2/L3）
4. `rubric_ref`
5. `pass_threshold`
6. `cert_status`（not_started/in_training/certified/expired）
7. `cert_valid_until`
8. `mentor_feedback_ref`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 1 份课程化训练资产 + >= 1 次回放评分登记”。
2. 课程化索引中 100% 条目具备 `training_asset_id + skill_track + evidence_refs + cert_status`。
3. 每月至少 2 条训练反馈进入 backlog，且至少 1 条完成验证闭环。
4. 答辩或对外展示中用于“可复现能力”证明的内容，100% 可追溯到训练题单、评分量表与回放记录。

#### 5.10.23 Step 6/6 内容扩展蓝图（第十三层：周主题策展与双轨内容编排）

目标：在“课程化训练与认证化”基础上，进一步补齐“稳定产能 + 主题聚焦 + 发布复用”机制，让围绕 Memory-System 的内容不再随机产出，而是按周主题持续沉淀。

建议新增 8 类策展型内容资产（围绕记忆系统主线）：

1. `周主题策展卡`（P1）
   - 形态：本周唯一主主题、目标受众、关键问题、证据入口、预期产出
   - 证据入口：`/memory-insights`、`/memory-review`、`.memory/content-assets/index.md`
2. `主线结论卡`（P1）
   - 形态：围绕周主题的 1 条核心结论（含边界与失败条件）
   - 证据入口：`/memory-debug [N]`、`/memory-scenes`
3. `快报短内容卡`（P1）
   - 形态：面向日更/周更的 L1 输出（结论 + 一条关键证据 + 一条行动建议）
   - 证据入口：`/memory-insights`、`/tasks`
4. `深度长内容卡`（P1）
   - 形态：面向 L2/L3 的对照分析稿（基线、变化、解释、复测计划）
   - 证据入口：基准包、挑战卡、专题卡
5. `主题问答包卡`（P2）
   - 形态：围绕周主题整理 5-10 个高频问答（用于答辩/演示/对外沟通）
   - 证据入口：`question-bank`、`/memory-scenes`、回放脚本
6. `反例补洞卡`（P2）
   - 形态：主线结论的反例、失效边界与修正建议
   - 证据入口：失败剖析卡、回归告警卡、挑战集
7. `跨周复用卡`（P2）
   - 形态：上周内容复用到本周主题的映射记录（保留/淘汰/重写）
   - 证据入口：历史 `index.md`、版本对照、复测结果
8. `主题收官卡`（P2）
   - 形态：周末收官总结（产量、质量、回流任务、下周建议）
   - 证据入口：周看板、回流台账、测试与回放记录

双轨编排机制（建议）：

1. 生产轨：每周固定“1 条主线结论 + 2 条快报短内容 + 1 条深度长内容”。
2. 复核轨：每条主线内容必须有 1 份反例补洞卡或失败边界说明。
3. 回流轨：每周至少 1 条内容反馈进入 backlog，并在下周主题策展卡中标注处理状态。
4. 复用轨：每周至少复用 2 份历史内容资产，避免重复造轮子。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `theme_week`（如 `2026-W14`）
2. `theme_name`（如 `evidence-reliability`）
3. `track_type`（production/review/reflow/reuse）
4. `content_tier`（L1/L2/L3）
5. `anchor_conclusion_id`
6. `counterexample_ref`
7. `reuse_from_ids`
8. `weekly_close_status`（open/closing/closed）

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 4 条主题内容（含 >= 1 条 L2/L3）+ >= 1 条反馈回流任务”。
2. 主题索引中 100% 条目具备 `theme_week + theme_name + track_type + evidence_refs`。
3. 主线结论条目中 >= 80% 具备 `counterexample_ref` 或明确失败边界说明。
4. 每周至少复用 2 份历史资产，且复用条目均有“保留/淘汰/重写”决策留痕。

#### 5.10.24 Step 6/6 内容扩展蓝图（第十四层：证据图谱叙事与多受众改写）

目标：在“周主题策展”基础上，继续把同一份记忆证据转化为“开发可执行、答辩可讲清、论文可引用、对外可传播”的多版本内容，减少重复写作与口径漂移。

建议新增 8 类图谱叙事型内容资产（围绕记忆系统主线）：

1. `证据图谱卡`（P1）
   - 形态：把一条结论拆成“输入问题 -> 反思决策 -> 检索证据 -> 实际使用 -> 输出结论”的链路图
   - 证据入口：`/memory-debug [N]`、`/memory-insights`
2. `一证多稿卡`（P1）
   - 形态：同一证据产出开发版/答辩版/论文版三套表达（结论一致、语言风格不同）
   - 证据入口：`/memory-scenes`、`.memory/content-assets/index.md`
3. `指标故事卡`（P1）
   - 形态：围绕单个指标变化（如 retrieved->used）输出“异常 -> 根因 -> 修复 -> 复测”故事线
   - 证据入口：`/memory-insights`、回归测试输出
4. `反例辩论卡`（P1）
   - 形态：同主题下“支持证据 vs 反例证据”双列对照，给出适用边界
   - 证据入口：`/memory-review`、失败复盘卡
5. `术语统一卡`（P2）
   - 形态：记忆系统核心术语（reflection/evidence/governance 等）统一定义与禁用表述
   - 证据入口：`开发文档.md`、`README.md`
6. `图表口播卡`（P2）
   - 形态：每张论文或答辩图表对应 30 秒讲解稿 + 追问应答点
   - 证据入口：论文图表卡、问答包卡
7. `受众反馈回执卡`（P2）
   - 形态：按受众（开发者/老师/同学/潜在用户）记录“最有价值结论 + 疑问 + 误解点”
   - 证据入口：发布回执、问答记录、评论摘要
8. `口径漂移告警卡`（P2）
   - 形态：发现“同一结论多版本冲突”时的修正单（冲突点、统一口径、影响范围）
   - 证据入口：一证多稿卡、术语统一卡、发布后回执

多受众改写机制（建议）：

1. 同一核心结论必须至少产出 2 个受众版本（开发版 + 答辩版），再按优先级补论文版。
2. 每条对外内容发布前必须经过“口径对齐检查”：结论、指标口径、时间窗口三项一致。
3. 每周至少 1 次“图谱回放审查”，抽查 3 条内容是否可从源证据回放到结论。
4. 发现口径冲突时 48 小时内补发“口径漂移告警卡”，并更新历史内容引用。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `evidence_graph_id`（如 `EG-2026W14-01`）
2. `source_trace_ids`
3. `audience_variant`（dev/defense/paper/public）
4. `claim_id`
5. `metric_window`
6. `consistency_status`（aligned/diverged/pending）
7. `narrative_template`
8. `drift_ticket_ref`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条一证多稿内容 + >= 1 条证据图谱卡”。
2. 多受众条目中 100% 具备 `claim_id + source_trace_ids + audience_variant + consistency_status`。
3. 发布内容抽检中 >= 90% 可从证据链回放到结论，且时间窗口一致。
4. 口径漂移问题在发现后 48 小时内修正闭环率 >= 95%。

#### 5.10.25 Step 6/6 内容扩展蓝图（第十五层：场景剧本化与复盘栏目闭环）

目标：在“证据图谱 + 多受众改写”基础上，把内容进一步升级为“可连续追更的场景栏目”，让同一类问题可以按周迭代、按版本对照、按结果回流。

建议新增 8 类栏目化内容资产（围绕记忆系统主线）：

1. `场景剧本母版卡`（P1）
   - 形态：固定场景的标准输入、扰动输入、观测点、失败判定线
   - 证据入口：`/memory-scenes`、`/memory-debug [N]`
2. `周更场景回放卡`（P1）
   - 形态：同一剧本按周回放的结果差异（本周 vs 上周）
   - 证据入口：回放记录、`/memory-insights`
3. `修复收益账本卡`（P1）
   - 形态：内容反馈驱动修复后的收益归因（命中率/误判率/回放通过率）
   - 证据入口：backlog 记录、测试结果、trace
4. `失败样本精选卡`（P1）
   - 形态：本周最有价值失败样本与失效边界说明
   - 证据入口：回归告警卡、`/memory-review`
5. `版本差异速报卡`（P2）
   - 形态：同场景在不同版本/策略下的最小对照结论
   - 证据入口：版本对照卡、评测脚本
6. `栏目问答追更卡`（P2）
   - 形态：围绕某栏目的新增高频问题与标准答法更新
   - 证据入口：问答包、受众反馈回执
7. `证据缺口补齐卡`（P2）
   - 形态：高价值结论但证据不足的补齐计划与完成状态
   - 证据入口：内容债清单、图谱回放审查
8. `月度栏目收官卡`（P2）
   - 形态：按栏目输出月度产量、质量、回流与下月策略
   - 证据入口：内容索引、回流台账、测试与回放记录

栏目闭环机制（建议）：

1. 每周固定选择 2 条核心场景栏目，执行“回放 -> 对照 -> 发布 -> 回流”四步。
2. 每条栏目内容必须包含“本次变化点 + 对照基线 + 下一次验证点”。
3. 每周至少 1 条失败样本进入公开复盘，禁止全部内容只呈现成功案例。
4. 每周至少 1 条栏目反馈进入 backlog，并在下一次周更中标注处理状态。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `column_id`（如 `COL-MEM-2026W14-01`）
2. `scenario_script_id`
3. `baseline_run_ref`
4. `current_run_ref`
5. `delta_signal`（improved/regressed/flat）
6. `failure_case_ref`
7. `feedback_to_backlog_ref`
8. `next_validation_due`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 2 条栏目更新 + >= 1 条失败样本复盘 + >= 1 条反馈回流任务”。
2. 栏目索引中 100% 条目具备 `column_id + scenario_script_id + baseline_run_ref + current_run_ref + delta_signal`。
3. 栏目内容中 >= 80% 具备“对照基线 + 下一次验证点”。
4. 回流任务中 >= 70% 在两周内完成“已验证/已归档”闭环。

#### 5.10.26 Step 6/6 内容扩展蓝图（第十六层：选题评分引擎与发布回流实验）

目标：把“围绕记忆系统做更多内容”从人工灵感升级为“可量化选题 + 可重复实验 + 可追踪回流”的内容工程引擎。

建议新增 8 类引擎化内容资产（围绕记忆系统主线）：

1. `选题候选池卡`（P1）
   - 形态：每周候选选题清单，附来源证据与受众假设
   - 证据入口：`/memory-insights`、trace、历史栏目反馈
2. `选题评分卡`（P1）
   - 形态：按统一维度给选题打分并排序（价值/证据完备/实现成本/复用潜力）
   - 证据入口：内容索引、backlog、测试覆盖
3. `周排期编排卡`（P1）
   - 形态：将高分选题映射到本周发布节奏（短内容/长内容/演示内容）
   - 证据入口：周主题策展卡、渠道计划
4. `实验对照卡`（P1）
   - 形态：同选题的 A/B 内容版本（标题、结构、证据呈现方式）与结果对照
   - 证据入口：渠道反馈、互动指标、回放记录
5. `发布回执卡`（P1）
   - 形态：每条发布内容的发布时间、渠道、反馈摘要、问题回流
   - 证据入口：发布日志、评论问题、私聊追问
6. `回流任务卡`（P2）
   - 形态：把反馈拆成可执行任务（文档修订/代码改进/案例补齐）
   - 证据入口：`开发实现process.md`、任务系统
7. `复用模板卡`（P2）
   - 形态：高表现内容沉淀为模板（开头结构、证据排布、结尾 CTA）
   - 证据入口：历史高分内容、A/B 胜出版本
8. `月度增长复盘卡`（P2）
   - 形态：按月汇总内容产量、有效反馈、回流完成率与下月策略
   - 证据入口：内容索引、实验账本、回流任务台账

选题评分机制（建议）：

1. 评分维度固定为：`problem_value`、`evidence_readiness`、`build_cost`、`reuse_potential`、`debate_value`。
2. 评分总分采用加权模型（建议）：`0.30*problem_value + 0.25*evidence_readiness + 0.20*reuse_potential + 0.15*debate_value - 0.10*build_cost`。
3. 每周至少选取 Top 3 进入排期，其中至少 1 条来自“失败样本/反例补洞”。
4. 同一主题连续两周低于阈值（建议 60 分）时自动降级为“观察池”，避免挤占主线产能。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `topic_id`（如 `TOPIC-MEM-2026W14-03`）
2. `topic_score`
3. `score_breakdown`
4. `experiment_variant`（A/B）
5. `publish_channel`
6. `feedback_signal`（positive/neutral/negative）
7. `feedback_to_task_ref`
8. `template_reuse_ref`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 10 条候选选题 + >= 3 条入排期 + >= 2 条实验对照内容”。
2. 发布回执中 100% 条目具备 `topic_id + topic_score + publish_channel + feedback_signal + feedback_to_task_ref`。
3. 回流任务中 >= 70% 在两周内完成“verified 或 archived”闭环。
4. 月度复盘中至少输出 3 条“可复用模板”并在下月复用验证。

#### 5.10.27 Step 6/6 内容扩展蓝图（第十七层：长期记忆演化年表与纵向研究资产）

目标：把“围绕记忆系统做更多内容”从周级运营升级到“月/季级纵向追踪”，让系统不仅能展示当前能力，还能证明记忆能力如何随时间持续演化。

建议新增 8 类纵向研究型内容资产（围绕记忆系统主线）：

1. `周度记忆快照卡`（P1）
   - 形态：固定窗口（7 天）记录记忆总量、命中率、未使用率、反思分布
   - 证据入口：`/memory-insights`、`/memory-debug [N]`
2. `主题连续性追踪卡`（P1）
   - 形态：按主题记录跨周连续对话中的上下文保持与断裂点
   - 证据入口：`/memory-scenes`、`session_summaries.jsonl`
3. `记忆漂移观察卡`（P1）
   - 形态：记录记忆内容与新输入冲突、降级、修正全过程
   - 证据入口：`/memory-governance`、`pending_explicit_memories.jsonl`
4. `个性化收益对照卡`（P1）
   - 形态：对比“有记忆/无记忆”在相关性、个性化、准确性上的差异
   - 证据入口：评测结果、`EvalService` 输出
5. `任务履约趋势卡`（P2）
   - 形态：按周统计任务创建、到期触发、完成与逾期趋势
   - 证据入口：`/tasks`、`scheduled_tasks.json`
6. `主动服务命中卡`（P2）
   - 形态：统计主动提醒触发率、采纳反馈与过度打扰率
   - 证据入口：`/proactive-reminders`、`proactive_reminders.jsonl`
7. `证据健康体检卡`（P2）
   - 形态：按月审计 trace 质量（空字段、噪声字段、结构兼容占比）
   - 证据入口：`memory_evidence_traces.jsonl`、`/memory-insights`
8. `论文图表素材卡`（P2）
   - 形态：把月/季纵向数据固化为可直接用于论文和答辩的图表口径卡
   - 证据入口：内容索引、评测与回放记录

纵向研究机制（建议）：

1. 固定三层时间窗口：`周（7d）/月（30d）/季（90d）`，每条结论必须标注窗口。
2. 每周至少完成 1 份快照卡；每月至少完成 1 份纵向趋势小结；每季度至少完成 1 份演化报告。
3. 所有纵向结论必须包含“起点基线 + 当前状态 + 变化原因 + 下周期验证点”。
4. 每月至少 1 条“纵向观察结论”进入 backlog 并完成验证计划登记。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `evolution_track_id`（如 `EVO-MEM-2026M03-01`）
2. `time_window`（7d/30d/90d）
3. `baseline_ref`
4. `current_ref`
5. `delta_summary`
6. `drift_signal`（stable/improving/regressing/drifting）
7. `backlog_link`
8. `next_checkpoint`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 1 份快照卡 + >= 1 条纵向结论留痕”。
2. 纵向索引中 100% 条目具备 `evolution_track_id + time_window + baseline_ref + current_ref + delta_summary`。
3. 每月至少 2 条纵向结论形成开发任务，其中至少 1 条完成验证闭环。
4. 答辩关键图表中 >= 70% 可追溯到纵向资产卡与原始命令输出。

#### 5.10.28 Step 6/6 内容扩展蓝图（第十八层：证据驱动内容实验室与发布流水线）

目标：在“长期记忆演化年表”基础上，进一步把内容体系升级为“可实验、可复测、可分发”的证据驱动流水线，让每条内容都能回答“为何有效、对谁有效、是否稳定有效”。

调研结论（2026-03-31）：

1. `Generative Agents` 给出的 `observation -> reflection -> planning` 架构说明，内容不应只记录结果，还应记录“观察证据”和“反思决策”。
2. `MemGPT` 的分层记忆思路说明，内容也应分层交付：快报层、解释层、方法层，避免单一篇幅承载所有信息。
3. `LangSmith` 的评测工作流强调数据集、实验、对比与追踪闭环，适合作为“内容是否可信”的最小校验框架。
4. `Letta` 的 stateful agent 实践强调跨会话状态连续性，说明你的内容应该天然支持“连续更新同一主题”，而不是一次性文章。

建议新增 8 类实验室内容资产（围绕 Memory-System 主线）：

1. `证据实验设计卡`（P0）
   - 形态：实验目标、假设、变量、评测口径、样本窗口
   - 证据入口：`/memory-insights`、`/memory-debug [N]`、`/memory-report`
2. `基线对照运行卡`（P0）
   - 形态：无记忆/默认记忆/增强策略三路对照运行结果
   - 证据入口：`/eval-*`、`src/test/**`、运行日志
3. `失败样本解剖卡`（P0）
   - 形态：误判样本、证据缺失点、修复动作、复测结果
   - 证据入口：trace 历史、回归测试、任务闭环记录
4. `受众分发改写卡`（P1）
   - 形态：同一证据结论改写为开发版/答辩版/论文版/对外版
   - 证据入口：`content_id` 与 `audience_variant` 关联索引
5. `发布回执实验卡`（P1）
   - 形态：发布时间、渠道、反馈信号、回流任务
   - 证据入口：IM 反馈、评论摘要、backlog 链接
6. `稳定性周报卡`（P1）
   - 形态：本周命中率、使用率、回归风险、下周实验计划
   - 证据入口：`/memory-insights` 周窗口统计
7. `复现实验脚本卡`（P1）
   - 形态：一键复现实验命令、输入样本、预期输出、验收阈值
   - 证据入口：`scripts/**`、`README` 命令、测试命令
8. `季度内容结论公报卡`（P2）
   - 形态：季度级结论、跨月趋势、争议点、下一季度假设
   - 证据入口：纵向资产（5.10.27）+ 本节实验资产聚合

固定执行机制（建议）：

1. 每周至少 1 个“有对照变量”的内容实验，不允许只有单样本结论。
2. 每条发布内容必须绑定 `baseline_ref + experiment_ref + evidence_refs` 三要素。
3. 失败样本占每周新增内容 >= 40%，避免只产出成功案例。
4. 每周至少 1 条发布反馈回流为可执行任务，并在下周复测。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `experiment_id`
2. `hypothesis`
3. `baseline_ref`
4. `variant_ref`
5. `metric_guardrail`
6. `audience_variant`
7. `distribution_channel`
8. `feedback_to_backlog_ref`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 1 个对照实验 + >= 3 条可分发内容 + >= 1 条反馈回流任务”。
2. 发布索引中 100% 条目具备 `experiment_id + baseline_ref + evidence_refs + distribution_channel`。
3. 每月至少 4 条失败样本完成“问题 -> 修复 -> 复测”闭环。
4. 季度结论中 >= 80% 可回放到具体实验记录，而非仅经验叙述。

#### 5.10.29 Step 6/6 内容扩展蓝图（第十九层：记忆场景基准集与内容复用引擎）

目标：在“证据驱动内容实验室”基础上，再补齐“可复现题单 + 跨内容复用”机制，让围绕 Memory-System 的内容从单条产出升级为可反复调用的基准资产。

调研结论（2026-03-31）：

1. 当前系统已具备 `/memory-debug [N]`、`/memory-scenes`、`/memory-insights`、`/tasks` 四类稳定证据入口，适合直接抽象为“场景基准题单”。
2. 现有内容流程已覆盖“实验与发布”，但“同类场景跨周对照”口径仍分散，导致复用成本偏高。
3. `memory_evidence_traces.jsonl` 与任务闭环记录具备天然的“问题-证据-修复-复测”链路，可直接沉淀为复现实验单元。
4. 论文与答辩材料的高频问题可被前置为固定题单，减少临时组织演示路径的波动。

建议新增 8 类基准集内容资产（围绕 Memory-System 主线）：

1. `场景基准题单卡`（P0）
   - 形态：按主题沉淀固定题单（输入脚本、预期行为、失败判定）
   - 证据入口：`/memory-scenes`、`.memory/content-assets/index.md`
2. `复现命令序列卡`（P0）
   - 形态：每个题单对应可执行命令序列与最小数据窗口
   - 证据入口：`scripts/**`、CLI 命令组合、测试命令
3. `跨周对照结果卡`（P0）
   - 形态：同题单在不同周版本的结果差异（通过/失败/漂移）
   - 证据入口：`/memory-debug [N]`、trace 历史
4. `失败到修复映射卡`（P1）
   - 形态：失败样本到修复提交、复测结论的一对一映射
   - 证据入口：`开发实现process.md`、测试结果、任务记录
5. `答辩演示路径卡`（P1）
   - 形态：固定 10 分钟演示脚本（开场问题、关键命令、预期输出）
   - 证据入口：题单卡 + 复现命令序列卡
6. `论文实证引用卡`（P1）
   - 形态：论文结论与题单/实验/trace 的映射关系
   - 证据入口：题单索引、实验索引、图表素材
7. `内容复用模板卡`（P2）
   - 形态：把高复用内容沉淀为统一模板（短文/长文/答辩稿）
   - 证据入口：历史高复用内容、A/B 胜出结构
8. `季度基准稳定性公报卡`（P2）
   - 形态：季度统计基准题单稳定通过率、漂移率与高风险主题
   - 证据入口：跨周对照结果 + 内容实验室资产

固定执行机制（建议）：

1. 每周至少新增 5 条候选题单，并选出 2 条进入基准池（`benchmark_ready=true`）。
2. 每条进入基准池的题单必须绑定 `input_script + command_seq + expected_signal + evidence_refs`。
3. 每周至少复测 3 条历史题单，记录 `stable/improving/regressing` 状态。
4. 失败题单 72 小时内必须进入“修复任务或降级说明”二选一流程。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `benchmark_id`
2. `benchmark_topic`
3. `input_script_ref`
4. `command_seq_ref`
5. `expected_signal`
6. `benchmark_ready`
7. `stability_state`
8. `repair_task_ref`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 2 条基准入池 + >= 3 条历史题单复测 + >= 1 条失败回流任务”。
2. 基准索引中 100% 条目具备 `benchmark_id + input_script_ref + command_seq_ref + evidence_refs`。
3. 每月至少 6 条题单完成“问题 -> 修复/降级 -> 复测”闭环并留痕。
4. 答辩演示素材中 >= 80% 可直接映射到基准题单与复现命令序列。

#### 5.10.30 Step 6/6 内容扩展蓝图（第二十层：跨作用域连续性叙事与身份链路内容化）

目标：在“场景基准集与复用引擎”基础上，补齐“个人记忆 -> 团队记忆 -> 跨平台身份 -> 主动服务”的连续叙事资产，让内容能够稳定回答“同一用户在不同作用域和渠道下是否保持一致记忆行为”。

调研结论（2026-03-31）：

1. 系统已具备 `personal/team:*` 作用域隔离与切换能力，但当前内容资产对“跨作用域差异与收益”覆盖不足。
2. 统一身份映射（`platform+senderId -> unified_id`）已落地，适合沉淀“跨平台连续性”专题内容，减少“单渠道演示偏差”。
3. 主动提醒、任务闭环和记忆证据追踪已形成可回放链路，可进一步打通“行为证据 -> 决策解释 -> 用户收益”叙事。
4. 对答辩和论文而言，跨作用域一致性与跨平台连续性是高价值问题，需有固定模板与验收口径。

建议新增 8 类连续性内容资产（围绕 Memory-System 主线）：

1. `作用域差异对照卡`（P0）
   - 形态：同问题在 `personal` 与 `team:*` 下的记忆调用差异、结论差异与风险说明
   - 证据入口：`/scope`、`/memory-debug`、`/memory-insights`
2. `跨平台身份连续性卡`（P0）
   - 形态：CLI/飞书/Telegram 的同一 `unified_id` 会话连续性与记忆继承轨迹
   - 证据入口：`/identity`、`identity_mappings.json`、IM 交互记录
3. `作用域冲突裁决卡`（P0）
   - 形态：个人偏好与团队共识冲突时的裁决策略、治理状态与复测结果
   - 证据入口：`/memory-governance`、冲突队列、trace 历史
4. `跨作用域任务履约卡`（P1）
   - 形态：个人任务与团队任务在创建、提醒、完成上的闭环对照
   - 证据入口：`/tasks`、`scheduled_tasks.json`
5. `主动提醒一致性卡`（P1）
   - 形态：不同作用域与平台下主动提醒命中率、打扰率与采纳率对照
   - 证据入口：`/proactive-reminders`、`proactive_reminders.jsonl`
6. `连续性回归告警卡`（P1）
   - 形态：跨作用域/跨平台出现记忆断裂时的告警、定位与修复动作
   - 证据入口：`/memory-insights` 趋势、回归测试、任务台账
7. `答辩跨渠道剧本卡`（P2）
   - 形态：同一主题在 CLI 与 IM 双轨演示的固定剧本与预期信号
   - 证据入口：基准题单、命令序列、IM 演示脚本
8. `论文一致性实证卡`（P2）
   - 形态：论文章节中“跨作用域一致性”结论与证据映射
   - 证据入口：内容索引、trace 抽样、评测结果

固定执行机制（建议）：

1. 每周至少完成 1 组“个人 vs 团队”作用域对照内容，且必须绑定同一输入脚本。
2. 每周至少抽查 2 条跨平台身份连续性链路，并登记 `consistent/partial/broken` 状态。
3. 每周至少 1 条连续性异常进入 backlog，并在 7 天内给出修复或降级结论。
4. 每两周至少更新 1 份“跨渠道演示剧本”，确保答辩路径可复现。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `continuity_case_id`
2. `scope_pair`
3. `unified_id_ref`
4. `channel_set`
5. `consistency_state`
6. `governance_decision_ref`
7. `continuity_risk_level`
8. `retest_due`

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 1 组作用域对照 + >= 2 条身份连续性抽查 + >= 1 条异常回流任务”。
2. 连续性索引中 100% 条目具备 `continuity_case_id + scope_pair + unified_id_ref + evidence_refs + consistency_state`。
3. 每月至少 4 条跨作用域/跨平台异常完成“发现 -> 修复/降级 -> 复测”闭环。
4. 答辩跨渠道剧本中 >= 80% 场景可回放到同一 `unified_id` 的连续证据链。

调研参考（实施前按最新版本复核）：

1. Generative Agents: https://arxiv.org/abs/2304.03442
2. MemGPT: https://arxiv.org/abs/2310.08560
3. LangSmith Evaluation Docs: https://docs.langchain.com/langsmith/evaluate-llm-application
4. Letta Stateful Agents Docs: https://docs.letta.com/guides/core-concepts/stateful-agents/

#### 5.10.31 Step 6/6 内容扩展蓝图（第二十一层：记忆资产叙事引擎与季度主题计划）

目标：把现有“内容卡/栏目/资产包”进一步升级为“可连续讲述的记忆系统叙事引擎”，让开发记录、评测结果、答辩素材与对外输出共享同一条证据主线。

调研结论（2026-03-31）：

1. 当前内容池“素材很多”，但“跨周期叙事连贯性”仍弱，难以快速回答“这个系统这三个月具体变好了什么”。
2. 现有卡片大多是单点复盘，缺少“主题链路”组织（同一主题跨周追踪、跨版本对照、跨渠道复用）。
3. Step 6/6 下一步的关键不是再加新命令，而是把已有证据流转成“季度主题计划 + 叙事主线索引”。

建议新增 6 类主题化内容资产（围绕 Memory-System 主线）：

1. `主题演进链路卡`（P0）
   - 形态：同一主题（如 `reflection`、`governance`）按周记录“问题 -> 动作 -> 结果”
   - 证据入口：`/memory-insights`、`/memory-debug [N]`、`开发实现process.md`
2. `跨版本收益账本卡`（P0）
   - 形态：按版本追踪关键指标净变化（提升/回退/持平）
   - 证据入口：回归测试输出、`/memory-insights` 趋势窗口
3. `叙事证据锚点卡`（P1）
   - 形态：为每个高频结论绑定固定证据锚（trace id、测试名、命令序列）
   - 证据入口：`memory_evidence_traces.jsonl`、`src/test/**`
4. `季度主题总览卡`（P1）
   - 形态：季度维度展示四条主线（反思、证据、治理、主动服务）的状态和里程碑
   - 证据入口：内容资产索引 + 周复盘卡
5. `答辩高频问题映射卡`（P1）
   - 形态：评审问题 -> 证据入口 -> 30 秒回答模板
   - 证据入口：`/memory-report`、演示剧本包、论文图表卡
6. `风险与退化追踪卡`（P1）
   - 形态：记录“已知风险、触发信号、兜底策略、复测时间”
   - 证据入口：回归告警卡、治理记录、测试失败样本

季度执行机制（建议）：

1. 每周固定 1 个“主题周”，只围绕一个主题生产 2-3 张关联内容卡，避免分散。
2. 每两周做一次主题复测，更新“演进状态：improving/stable/regressing”。
3. 每月产出 1 份“主题月报”，统一回答“本月最有效改动”和“下月优先修复项”。
4. 每季度产出 1 份“叙事总览”，作为论文和答辩主脚本基线。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `theme_id`
2. `theme_name`
3. `narrative_stage`（problem/action/result/retest）
4. `version_window`
5. `delta_metric_refs`
6. `anchor_evidence_refs`
7. `quarter_tag`
8. `defense_ready`

验收门槛（建议）：

1. 连续 8 周每周至少新增 1 条 `主题演进链路卡`，且必须包含“动作前后对照”。
2. 每月至少 2 条高频结论升级为“叙事证据锚点卡”，避免口径漂移。
3. 每季度至少完成 1 次“主题总览复测”，并回流 >= 3 条开发任务。
4. 答辩主脚本中 >= 80% 结论可追溯到 `theme_id + anchor_evidence_refs`。

#### 5.10.32 Step 6/6 内容扩展蓝图（第二十二层：记忆场景节目化与发布编排台）

目标：在“季度主题叙事”之上继续升级为“可连续发布、可对照复盘、可反馈回流”的节目化内容体系，让围绕 Memory-System 的输出形成稳定栏目和节奏。

调研结论（2026-04-01）：

1. 当前内容体系已能回答“做了什么”，但对外表达仍缺“固定栏目 + 发布时间表 + 反馈归档”三件套。
2. 多数内容仍以单篇交付为主，缺少“同栏目连续 N 期”的跟踪上下文，不利于展示长期改进。
3. Step 6/6 下一步应把“证据资产”映射为“节目单资产”，把发布动作与回流动作绑定到同一索引链路。

建议新增 8 类节目化内容资产（围绕 Memory-System 主线）：

1. `周主题节目单卡`（P0）
   - 形态：定义本周栏目主题、目标受众、计划发布频次与证据入口
   - 证据入口：`theme_id`、`anchor_evidence_refs`、`开发实现process.md`
2. `三分钟演示脚本卡`（P0）
   - 形态：固定“问题背景 -> 命令回放 -> 指标变化 -> 结论”四段式短演示脚本
   - 证据入口：`/memory-debug [N]`、`/memory-insights`、回放命令序列
3. `失败复盘专栏卡`（P0）
   - 形态：每周固定发布 1 条失败样本，包含触发条件、误判路径与修复状态
   - 证据入口：失败 trace、测试回归记录、修复任务单
4. `指标快讯卡`（P1）
   - 形态：面向开发节奏的短更新（本周命中率/回流率/复测通过率变化）
   - 证据入口：评测输出、`/memory-insights` 趋势窗口
5. `问答拆解卡`（P1）
   - 形态：把高频问题拆成“30 秒版/3 分钟版/技术细节版”三档回答
   - 证据入口：答辩问答包、主题演进链路卡
6. `版本迁移提醒卡`（P1）
   - 形态：标注版本升级后受影响的内容条目、失效证据与替代引用
   - 证据入口：`version_window`、回归结果、内容索引差异
7. `反馈回流工单卡`（P1）
   - 形态：发布后反馈 -> backlog 任务 -> 复测结论的一体化追踪
   - 证据入口：`/tasks`、开发任务记录、复测回执
8. `月度节目收官卡`（P1）
   - 形态：汇总本月节目产量、有效反馈、改进收益与下月排期
   - 证据入口：周主题节目单卡、反馈回流工单卡、指标快讯卡

发布编排机制（建议）：

1. 固定周节奏：每周至少 `1 条演示脚本 + 1 条失败复盘 + 1 条指标快讯`。
2. 固定双周复测：每两周抽取 2 条已发布内容做“证据回放复核”，验证结论仍有效。
3. 固定月度收官：每月最后一周产出 `月度节目收官卡`，并明确下月 2 个主栏目。
4. 固定回流约束：每周至少 1 条发布反馈进入 backlog，且必须带 `retest_due`。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `show_id`
2. `episode_id`
3. `content_track`（demo/failure/metric/qa/migration）
4. `publish_window`
5. `audience_type`（dev/defense/paper/public）
6. `feedback_ticket_refs`
7. `replay_check_status`（pending/pass/fail）
8. `next_episode_hook`

验收门槛（建议）：

1. 连续 6 周满足“每周 >= 3 条节目化内容（含 >= 1 条失败复盘）”。
2. 已发布内容中 >= 90% 具备 `show_id + episode_id + evidence_refs + replay_check_status`。
3. 每月至少 4 条发布反馈完成“进入 backlog -> 复测 -> 结论更新”闭环。
4. 答辩演示素材中 >= 80% 可映射到同一栏目链路（`show_id`）并可按 `episode_id` 连续回放。

#### 5.10.33 Step 6/6 内容扩展蓝图（第二十三层：证据工厂化与选题评分引擎）

目标：在“节目化发布”基础上，继续升级为“选题可评分、证据可复用、结论可复测”的内容工厂机制，让围绕 Memory-System 的内容产出从“靠经验”转为“靠规则”。

调研结论（2026-04-01）：

1. 当前内容体系已具备“持续发布”能力，但仍缺“选题优先级算法”，容易把产能消耗在低价值主题。
2. 现有证据链可回放，但“证据复用”没有统一打包规范，同类结论在不同内容中重复整理成本较高。
3. Step 6/6 的下一步应聚焦“选题评分 + 证据包复用 + 对照实验约束”三件事，避免继续横向扩张栏目而削弱质量。

建议新增 8 类工厂化内容资产（围绕 Memory-System 主线）：

1. `选题评分卡`（P0）
   - 形态：对候选主题按 `impact/replayability/novelty/risk` 四维打分并排序
   - 证据入口：`/memory-insights`、`/memory-debug [N]`、回归测试结果
2. `证据复用包`（P0）
   - 形态：将可复用证据按主题打包（命令序列、trace 片段、测试引用、指标快照）
   - 证据入口：`memory_evidence_traces.jsonl`、`src/test/**`、`开发实现process.md`
3. `对照实验卡`（P0）
   - 形态：固定“基线版本 -> 当前版本 -> 差异结论 -> 风险说明”
   - 证据入口：版本对照回归、`/memory-insights` 趋势窗口
4. `低效选题淘汰卡`（P1）
   - 形态：记录低得分选题及淘汰理由，避免重复投入
   - 证据入口：选题评分卡、发布反馈与复测失败记录
5. `高价值复写卡`（P1）
   - 形态：同一证据链生成 `答辩版/论文版/开发版` 三稿，统一口径分发
   - 证据入口：证据复用包、主题演进链路卡
6. `回归告警快反卡`（P1）
   - 形态：当关键指标退化时，48 小时内产出“现象/影响/修复计划”
   - 证据入口：回归结果、`/memory-insights` 退化诊断、任务状态
7. `周选题漏斗卡`（P1）
   - 形态：展示“候选 -> 入选 -> 发布 -> 回流 -> 验证”漏斗转化
   - 证据入口：选题评分卡、节目化索引、backlog 记录
8. `月度策略复盘卡`（P1）
   - 形态：总结本月选题命中率、复用率、回流收益与下月策略
   - 证据入口：周选题漏斗卡、反馈回流工单卡、测试趋势

执行机制（建议）：

1. 固定周节奏：每周至少评估 10 个候选选题，入选 3 个进入发布队列。
2. 固定证据门槛：未绑定 `baseline_ref + evidence_refs + command_refs` 的内容不得进入发布池。
3. 固定对照约束：每周至少 1 条内容必须包含“基线 vs 当前”对照，不得只给单点结论。
4. 固定回流闭环：每周至少 1 条内容反馈进入 backlog 并在两周内给出验证结果。

建议新增索引字段（`.memory/content-assets/index.md`）：

1. `topic_score`
2. `score_breakdown`
3. `evidence_pack_id`
4. `baseline_ref`
5. `compare_ref`
6. `funnel_stage`
7. `reuse_ratio`
8. `decision_status`（selected/rejected/published/verified/retired）

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 10 个候选选题 + >= 3 条发布内容 + >= 1 条对照实验内容”。
2. 发布内容中 100% 具备 `topic_score + evidence_pack_id + evidence_refs + decision_status`。
3. 每月至少 6 条内容完成“发布 -> 回流 -> 验证”闭环，其中失败样本占比 >= 30%。
4. 每月至少 2 条结论通过“复写三稿（答辩/论文/开发）”验证且口径一致。

---

#### 5.10.34 Step 6/6 内容扩展蓝图（收敛执行版：主题库 + 模板库 + 发布日历）

目标：在既有“内容工厂化”基础上，进一步把“更多内容”收敛为可执行的三件事：`主题库`、`模板库`、`发布日历`，避免继续增加抽象层级而降低执行效率。

调研结论（2026-04-01）：

1. 当前文档已经覆盖大量“内容方向”，短板不在“缺想法”，而在“选题进入发布前的标准化动作不够短”。
2. 只要把“选题卡 -> 模板卡 -> 发布卡”三步固定下来，内容产能会明显提升，且不会牺牲证据质量。
3. Step 6/6 下一步应优先建设“最小主题库（20 条）+ 固定模板（6 类）+ 4 周发布日历”，先跑出稳定节奏，再扩展更多栏目。

建议新增三类执行资产（围绕记忆系统主线）：

1. `主题候选库`（P0）
   - 形态：统一维护 `topic_id + topic_score + target_audience + evidence_pack_id`
   - 目标：候选选题不再散落在周会记录或临时笔记中
2. `模板库`（P0）
   - 形态：固定 6 类模板（失败复盘、对照实验、指标快讯、场景脚本、任务闭环、论文图表）
   - 目标：同类内容采用同结构，降低写作与复核成本
3. `发布日历`（P0）
   - 形态：按周维护 `publish_date + channel + owner + expected_feedback`
   - 目标：保证每周稳定产出，避免“只规划不发布”

推荐执行节奏（4 周）：

1. 第 1 周：完成主题库首批 20 条候选，并产出 Top 5 发布清单。
2. 第 2 周：完成 6 类模板落盘，并用 3 条真实内容试跑。
3. 第 3 周：接入发布日历，完成首轮“发布 -> 反馈 -> 回流”闭环。
4. 第 4 周：做周/月复盘，冻结有效模板并淘汰低效模板。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 1 条失败复盘 + >= 1 条反馈回流任务”。
2. 发布内容中 100% 具备 `topic_id + template_type + evidence_refs + owner + retest_due`。
3. 每月至少 4 条反馈完成“回流 -> 验证 -> 结论更新”闭环。
4. 月末复盘中明确“保留模板/淘汰模板/新增模板”决策各至少 1 条。

#### 5.10.35 Step 6/6 内容扩展蓝图（执行增强版：系列 IP + 复用链路 + 月度主题战役）

目标：在“主题库 + 模板库 + 发布日历”可执行的基础上，进一步把内容生产升级为“可连续追更、可跨渠道复用、可按月集中突破”的稳定机制。

调研结论（2026-04-01）：

1. 当前执行短板不在“能不能产出单条内容”，而在“同主题连续追更”和“跨受众复用”执行不稳定。
2. 把发布对象从“单条内容”升级为“系列 IP（3~5 期）”，能显著提升复盘深度与证据复用率。
3. Step 6/6 下一步应优先固化“三层执行资产”：`系列台账`、`复用链路图`、`月度主题战役板`，先提升连续发布稳定性，再扩展新主题池。

建议新增三类执行资产（围绕记忆系统主线）：

1. `系列台账`（P0）
   - 形态：统一维护 `series_id + episode_id + narrative_goal + release_window + evidence_pack_id`
   - 目标：同一主题从“单点更新”升级为“连续 3~5 期可追更”
2. `复用链路图`（P0）
   - 形态：记录 `source_content_id + reused_in + rewrite_target + reuse_ratio + quality_check`
   - 目标：把“答辩版/论文版/开发版”改写复用纳入同一索引链路
3. `月度主题战役板`（P0）
   - 形态：按月维护 `campaign_id + focus_theme + weekly_milestone + risk_flag + rollback_plan`
   - 目标：将零散发布收敛为“月主题突破”，提升反馈采样密度与迭代效率

推荐执行节奏（4 周）：

1. 第 1 周：确定本月 2 条核心系列 IP，并完成每条 3 期大纲与证据包映射。
2. 第 2 周：完成首批 4 条内容发布（至少 2 条来自系列 IP），并登记复用链路。
3. 第 3 周：完成跨受众改写（答辩/论文/开发）与回流任务绑定。
4. 第 4 周：完成月度主题战役复盘，输出保留系列、淘汰系列与下月接力系列清单。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条系列内容 + >= 1 条跨受众复写内容”。
2. 发布索引中 100% 条目具备 `series_id + episode_id + narrative_goal + evidence_refs + reuse_ratio`。
3. 每月至少 6 条内容完成“发布 -> 回流 -> 复测 -> 改写复用”闭环。
4. 月度复盘中明确“继续追更系列 >= 2 条、暂停/淘汰系列 >= 1 条、下月新开系列 >= 1 条”。

#### 5.10.36 Step 6/6 内容扩展蓝图（闭环实战版：场景资产包 + 回放脚本库 + 双周复测公报）

目标：在“系列 IP + 复用链路 + 月度主题战役”基础上，把内容体系进一步落到“可回放、可对照、可复测”的实战闭环，确保每条内容都能稳定反哺开发与答辩。

调研结论（2026-04-01）：

1. 当前内容链路已经具备连续产能，核心缺口转为“发布后复测与结论续更”执行不稳定，导致高价值内容寿命偏短。
2. 只靠发布数量难以衡量内容有效性，必须补齐“脚本化回放 + 双周复测 + 结论状态更新”三件事。
3. Step 6/6 下一步应优先建设三类执行资产：`场景资产包`、`回放脚本库`、`双周复测公报`，把内容从“可产出”升级为“可持续验证”。

建议新增三类执行资产（围绕记忆系统主线）：

1. `场景资产包`（P0）
   - 形态：统一维护 `scene_pack_id + scenario_goal + baseline_case + expected_delta + evidence_refs`
   - 目标：将零散内容合并为可复用场景包，支持答辩演示与开发回归共用
2. `回放脚本库`（P0）
   - 形态：统一维护 `replay_script_id + trigger_command + data_window + pass_criteria + owner`
   - 目标：每条核心内容至少绑定 1 个可执行回放脚本，避免“结论不可复现”
3. `双周复测公报`（P0）
   - 形态：按双周维护 `retest_batch_id + sampled_content_ids + result_status + drift_flag + action_items`
   - 目标：固定节奏复测历史内容，持续识别漂移、回归与低效结论

推荐执行节奏（4 周）：

1. 第 1 周：沉淀首批 10 个 `scene_pack_id`，并为 Top 5 内容绑定回放脚本。
2. 第 2 周：完成首轮双周复测，输出“稳定/漂移/失效”三态结果与处置清单。
3. 第 3 周：针对漂移内容完成“回流任务 -> 修复验证 -> 结论更新”闭环，并补齐改写复用版本。
4. 第 4 周：发布月度收敛复盘，冻结高价值资产包并淘汰失效脚本，更新下月复测计划。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条绑定回放脚本内容 + 双周 >= 1 次复测公报”。
2. 内容索引中 100% 核心条目具备 `scene_pack_id + replay_script_id + pass_criteria + result_status + evidence_refs`。
3. 每月至少 8 条内容完成“发布 -> 回放 -> 复测 -> 回流 -> 结论更新”闭环并可追溯。
4. 双周复测公报中明确“稳定条目 >= 3、漂移条目处置率 >= 80%、失效条目 100% 给出处置动作”。

#### 5.10.37 Step 6/6 内容扩展蓝图（运营稳态版：证据订阅流 + 选题待办池 + 回流收益账本）

目标：在“场景资产包 + 回放脚本 + 双周复测”基础上，把内容生产进一步升级为“持续供给 + 按优先级执行 + 收益可量化”的稳态运营机制，避免高质量内容只靠人工临时推进。

调研结论（2026-04-01）：

1. 当前链路已具备发布与复测能力，主要短板转为“证据补货不稳定”，导致选题经常断档或依赖临时补数。
2. 只看发布数量无法判断内容价值，必须新增“反馈收益归因”视角，明确哪些内容真正推动了修复、指标改善与答辩素材升级。
3. Step 6/6 下一步应优先建设三类执行资产：`证据订阅流`、`选题待办池`、`回流收益账本`，把内容体系从“可回放”升级为“可持续运营”。

建议新增三类执行资产（围绕记忆系统主线）：

1. `证据订阅流`（P0）
   - 形态：统一维护 `evidence_feed_id + source_command + data_window + freshness_sla + trigger_rule + stale_risk`
   - 目标：固定从 `/memory-insights`、`/memory-scenes`、`/memory-debug [N]`、`/tasks` 拉取新证据，避免周末集中补数
2. `选题待办池`（P0）
   - 形态：统一维护 `content_ticket_id + topic_cluster + priority_score + due_date + owner + blocking_risk + status`
   - 目标：把“想做但未做”的选题纳入同一任务池，按优先级稳定推进
3. `回流收益账本`（P0）
   - 形态：统一维护 `feedback_case_id + linked_content_id + linked_backlog + fix_cost + benefit_metric + verification_date + roi_status`
   - 目标：将“内容反馈 -> 开发动作 -> 验证收益”链路沉淀为可审计账本，支撑论文与答辩量化说明

推荐执行节奏（4 周）：

1. 第 1 周：建立 4 条核心 `evidence_feed_id`（insights/scenes/debug/tasks），完成首次自动采样。
2. 第 2 周：建立选题待办池，完成 Top 10 `content_ticket_id` 分级与排期。
3. 第 3 周：建立回流收益账本，至少登记 4 条“内容 -> backlog -> 验证结果”链路。
4. 第 4 周：完成月度运营复盘，输出“高 ROI 内容类型、低效内容类型、下月订阅与排期调整”决策。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条来自订阅流触发 + >= 1 条反馈收益登记”。
2. 发布索引中 100% 核心条目具备 `evidence_feed_id + content_ticket_id + linked_backlog + verification_date + roi_status`。
3. 每月至少 6 条内容完成“证据订阅 -> 发布 -> 回流任务 -> 收益验证”闭环并可追溯。
4. 月度复盘中明确“高 ROI 模板 >= 2 类、低 ROI 模板 >= 1 类、下一月下线主题 >= 1 条”与对应证据依据。

#### 5.10.38 Step 6/6 内容扩展蓝图（增长放大版：问题触发词库 + 一证三稿流水线 + 复用库存周转板）

目标：在“证据订阅 + 选题待办 + ROI 归因”稳态基础上，进一步补齐“同证据多版本改写 + 低成本复用 + 持续放大分发”的增长机制，让围绕记忆系统的内容持续扩张但不失真。

调研结论（2026-04-01）：

1. 当前主要瓶颈不在证据采集，而在“同一证据无法快速改写成多受众版本”，导致产能受限。
2. 高价值内容复用率仍偏低，缺少“可直接再利用”的库存视角，很多条目只发布一次即沉底。
3. Step 6/6 下一步应优先建设三类执行资产：`问题触发词库`、`一证三稿流水线`、`复用库存周转板`，把内容体系从“可持续运营”升级为“可持续放大”。

建议新增三类执行资产（围绕记忆系统主线）：

1. `问题触发词库`（P0）
   - 形态：统一维护 `question_trigger_id + trigger_question + mapped_evidence_types + target_audience + rewrite_template + priority_level`
   - 目标：把高频问题直接映射到可复用证据与模板，降低选题启动成本
2. `一证三稿流水线`（P0）
   - 形态：统一维护 `content_bundle_id + source_evidence_refs + dev_version_id + defense_version_id + public_version_id + consistency_status`
   - 目标：同一证据一次生产，输出开发版/答辩版/对外版三稿，减少重复写作
3. `复用库存周转板`（P0）
   - 形态：统一维护 `asset_stock_id + last_publish_date + reuse_count + turnover_days + decay_risk + refresh_action`
   - 目标：把“高价值旧资产”纳入周转管理，固定节奏复用、续更与淘汰

推荐执行节奏（4 周）：

1. 第 1 周：建立首批 20 条 `question_trigger_id`，覆盖记忆反思、证据追踪、场景回放、收益归因四类高频问题。
2. 第 2 周：建立一证三稿流水线，完成 Top 6 内容的三稿改写与一致性校验。
3. 第 3 周：建立复用库存周转板，筛出 10 条高价值历史条目并执行续更发布。
4. 第 4 周：输出月度增长复盘，给出“可放大主题、过载主题、需淘汰主题”清单与下月排期。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条来自一证三稿流水线 + >= 1 条历史库存复用内容”。
2. 发布索引中 100% 核心条目具备 `question_trigger_id + content_bundle_id + target_audience + consistency_status + reuse_count`。
3. 每月至少 8 条内容完成“问题触发 -> 证据映射 -> 三稿改写 -> 发布 -> 回流验证”闭环并可追溯。
4. 月度复盘中明确“高周转资产 >= 3 条、低周转资产淘汰 >= 1 条、三稿一致性通过率 >= 90%”。

#### 5.10.39 Step 6/6 内容扩展蓝图（稳态提效版：选题热力图 + 失败优先池 + 双周回流公报）

目标：在“问题触发 + 一证三稿 + 复用周转”基础上，进一步把内容体系升级为“可持续提效”的稳态机制，确保围绕记忆系统的内容不仅能持续产出，还能持续把失败样本转成可验证改进。

调研结论（2026-04-01）：

1. 当前体系已有内容供给能力，但“失败样本进入发布与回流链路”的比例仍不稳定，容易再次回到成功样本偏置。
2. 选题排期已具备基础字段，但缺少“主题热度 + 失败价值 + 回流收益”三维统一排序，导致高价值题目不一定优先发布。
3. Step 6/6 下一步应优先建设三类执行资产：`选题热力图`、`失败优先池`、`双周回流公报`，把内容体系从“可持续放大”升级为“可持续提效”。

建议新增三类执行资产（围绕记忆系统主线）：

1. `选题热力图`（P0）
   - 形态：统一维护 `topic_heat_id + topic_cluster + evidence_density + replay_cost + roi_trend + queue_rank`
   - 目标：将“证据密度 + 回放成本 + 收益趋势”统一量化，形成周排期 Top 队列
2. `失败优先池`（P0）
   - 形态：统一维护 `failure_case_id + failure_type + impact_level + fix_window + owner + publish_priority`
   - 目标：固定纳入失败/回归内容，避免内容池被成功案例长期挤占
3. `双周回流公报`（P0）
   - 形态：统一维护 `biweekly_report_id + linked_content_ids + linked_backlog_ids + verified_fixes + unresolved_risks + next_actions`
   - 目标：把“发布 -> 回流 -> 修复 -> 复测”进展双周公示，形成可审计改进节奏

推荐执行节奏（4 周）：

1. 第 1 周：建立首版 `选题热力图`，完成 Top 15 主题的三维排序与周排期。
2. 第 2 周：建立 `失败优先池`，至少纳入 8 条 failure/regression 样本并绑定负责人。
3. 第 3 周：输出首份 `双周回流公报`，覆盖最近两周发布内容的回流状态与修复进展。
4. 第 4 周：完成月度提效复盘，给出“高热度保留主题、低 ROI 下线主题、下月失败优先配额”决策。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + 失败/回归内容占比 >= 40% + >= 1 条回流验证更新”。
2. 发布索引中 100% 核心条目具备 `topic_heat_id + failure_case_id(可空) + queue_rank + linked_backlog + verification_status`。
3. 每双周至少 1 份回流公报，且覆盖最近发布内容的 Top 60% 高价值条目。
4. 月度复盘中明确“高热度低收益下线主题 >= 1 条、失败样本驱动修复闭环 >= 3 条”并附证据链。

#### 5.10.40 Step 6/6 内容扩展蓝图（落地复用版：证据母版库 + 问题剧本树 + 渠道发布包）

目标：在“热力排期 + 失败优先 + 双周回流”基础上，进一步把内容能力升级为“同一证据多问题复用 + 同一问题多渠道发布”的执行体系，确保围绕记忆系统的内容可以稳定复用而不是每次重写。

调研结论（2026-04-01）：

1. 当前内容链路已能持续发布，但“证据复用粒度”仍偏粗，多数内容在重写时无法直接复用到下一轮问题。
2. 失败样本已被纳入优先池，但缺少“问题路径模板”，导致同类问题重复排障、重复解释。
3. Step 6/6 下一步应优先建设三类执行资产：`证据母版库`、`问题剧本树`、`渠道发布包`，把内容体系从“可持续提效”升级为“可持续复用”。

建议新增三类执行资产（围绕记忆系统主线）：

1. `证据母版库`（P0）
   - 形态：统一维护 `master_evidence_id + claim_statement + source_trace_refs + confidence_level + expiry_date + reuse_scope`
   - 目标：将 `/memory-insights`、`/memory-debug [N]`、`/memory-review`、`/memory-report` 里的结论抽象为可复用母版，减少证据重复整理
2. `问题剧本树`（P0）
   - 形态：统一维护 `question_node_id + parent_question_id + scenario_tag + answer_route + required_evidence_ids + fallback_route`
   - 目标：把高频问题组织成可追踪路径，复用“主回答路线 + 兜底路线”，降低同类问题处理抖动
3. `渠道发布包`（P0）
   - 形态：统一维护 `release_pack_id + channel + format + generated_from + review_owner + publish_status + backflow_hook`
   - 目标：同一问题剧本一次编排，输出开发复盘版/答辩展示版/对外科普版，并绑定回流入口

推荐执行节奏（4 周）：

1. 第 1 周：建立首批 30 条 `master_evidence_id`，覆盖反思、证据追踪、场景回放、回流修复四类主题。
2. 第 2 周：建立 `问题剧本树`，完成 Top 20 高频问题的主路径与兜底路径编排。
3. 第 3 周：建立 `渠道发布包`，完成 8 组“一问题三渠道”发布与状态追踪。
4. 第 4 周：输出月度复用复盘，给出“高复用证据、低复用问题、需下线剧本”清单与处置策略。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条来自证据母版复用 + >= 1 组跨渠道发布包”。
2. 发布索引中 100% 核心条目具备 `master_evidence_id + question_node_id + release_pack_id + channel + publish_status`。
3. 每月至少 8 条内容完成“证据母版 -> 问题剧本 -> 渠道发布 -> 回流验证”闭环并可追溯。
4. 连续两周 `reuse_scope=single` 且 `reuse_count=0` 的条目 100% 完成“扩展复用/下线”处置并留痕。

#### 5.10.41 Step 6/6 内容扩展蓝图（决策归因版：反例账本 + 变更公告流 + 修复收益时间轴）

目标：在“证据母版复用 + 问题剧本 + 多渠道发布”基础上，进一步补齐“内容结论为何改变、何时改变、改变是否带来收益”的可追溯机制，把围绕记忆系统的内容从“能发布”升级为“能归因”。

调研结论（2026-04-01）：

1. 当前内容体系已覆盖“选题、生产、发布、回流”，但对“结论变化”的记录仍分散在日志与 commit 中，难以统一复盘。
2. 失败样本虽被纳入优先池，但“从失败到修复再到收益验证”的时间链路尚未结构化，导致决策复盘成本高。
3. Step 6/6 下一步应优先补齐三类资产：`反例账本`、`变更公告流`、`修复收益时间轴`，将内容体系升级为“结论可更新、更新可解释、收益可归因”。

建议新增三类执行资产（围绕记忆系统主线）：

1. `反例账本`（P0）
   - 形态：统一维护 `counterexample_id + scenario_tag + violated_claim + failure_trace_refs + risk_level + first_seen_at + owner`
   - 目标：把 `/memory-debug [N]` 与 `/memory-insights` 中高风险失败样本沉淀为可检索账本，避免反复踩同类坑
2. `变更公告流`（P0）
   - 形态：统一维护 `change_note_id + changed_claim + before_statement + after_statement + change_reason + linked_fix_refs + effective_from`
   - 目标：把结论变更从“口头同步”升级为“有证据的公告流”，保证开发/答辩/论文口径同步
3. `修复收益时间轴`（P0）
   - 形态：统一维护 `benefit_timeline_id + linked_backlog + baseline_metric + current_metric + delta_summary + verification_window + roi_status`
   - 目标：给每条关键修复提供“前后指标 + 验证窗口 + 收益判断”，沉淀可复用的优化证据

推荐执行节奏（4 周）：

1. 第 1 周：建立首批 20 条 `counterexample_id`，覆盖反思误判、证据未使用、场景连续性断裂、任务回流失效四类失败。
2. 第 2 周：建立 `变更公告流`，对 Top 10 高频结论形成 `before -> after -> reason` 变更记录。
3. 第 3 周：建立 `修复收益时间轴`，对本月关键修复完成至少 8 条“基线 vs 当前”对照验证。
4. 第 4 周：输出月度归因公报，明确“高收益修复、低收益修复、待观察修复”清单并给出处置建议。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 1 条反例账本更新 + >= 1 条收益时间轴更新”。
2. 发布索引中 100% 核心条目具备 `counterexample_id(可空) + change_note_id(可空) + benefit_timeline_id(可空) + linked_backlog`。
3. 每月至少 6 条内容完成“失败样本 -> 修复动作 -> 结论更新 -> 收益验证”闭环并可追溯。
4. 连续两周 `roi_status=negative` 且 `risk_level=high` 的主题 100% 完成“降级/撤回/重做”处置并留痕。

#### 5.10.42 Step 6/6 调研收敛版（14 天执行计划：围绕记忆系统打造更多内容）

目标：将“继续扩展内容方向”收敛为“可连续执行的 14 天内容冲刺”，固定产出节奏、命令入口和验收口径，避免只增概念不增落地结果。

调研结论（2026-04-01）：

1. 当前系统的内容证据入口已足够完整（`/memory-insights`、`/memory-debug [N]`、`/memory-scenes`、`/tasks`、`/memory-report`），瓶颈主要在执行编排而非数据来源。
2. 内容线条过多会导致维护成本上升；Step 6 下一阶段应收敛到“固定 4 线 + 固定模板 + 固定回流”。
3. 每条内容若不绑定“可回放命令 + 失败边界 + 回流任务”，很快会退化为不可验证叙述，不利于开发迭代与答辩复现。

收敛后的四条固定内容线（建议）：

1. `能力演进线`（P0）
   - 关注：本周能力提升点与证据
   - 固定入口：`/memory-insights`、`/memory-report`
2. `失败修复线`（P0）
   - 关注：失败样本、修复动作、复测结果
   - 固定入口：`/memory-debug [N]`、回归测试、任务台账
3. `连续性验证线`（P0）
   - 关注：跨会话/跨作用域/跨渠道是否连续
   - 固定入口：`/memory-scenes`、`/scope`、`/identity`
4. `产品化表达线`（P1）
   - 关注：同证据多版本改写（开发/答辩/论文）
   - 固定入口：证据母版、问题剧本、发布包索引

14 天执行节奏（建议）：

1. Day 1-2：建立本轮主题与 12 条候选选题池，按四条内容线分组。
2. Day 3-4：每条内容线产出 1 条 `P0` 样本，完成首轮证据回放校验。
3. Day 5-7：补齐失败修复线，至少沉淀 3 条“失败 -> 修复 -> 复测”闭环。
4. Day 8-10：补齐连续性验证线，完成 2 组跨作用域或跨渠道对照回放。
5. Day 11-12：完成 3 条“一证三稿”改写（开发/答辩/论文），校验口径一致。
6. Day 13-14：输出冲刺复盘，登记“保留主题/降级主题/下轮优先主题”。

统一模板字段（建议写入 `.memory/content-assets/index.md`）：

1. `content_id`
2. `content_line`（evolution/fix/continuity/product）
3. `evidence_refs`
4. `replay_command_refs`
5. `failure_boundary`
6. `backflow_task_ref`
7. `verification_status`（pending/pass/fail）
8. `next_iteration_action`

验收门槛（14 天）：

1. 新增内容 >= 12 条，且四条内容线均有覆盖（每条线 >= 2 条）。
2. 内容中 100% 具备 `evidence_refs + replay_command_refs + verification_status`。
3. 失败修复线至少完成 4 条可复测闭环（含 1 条回归失败后再修复）。
4. 至少 3 条内容反馈进入 backlog，并在冲刺结束时标注处理状态。

#### 5.10.43 Step 6/6 调研执行版（28 天内容货架：快照卡 + 案例卡 + 专题包）

目标：围绕记忆系统继续扩展内容，但从“单条产出”升级为“可复用内容货架”，把日常调试证据稳定沉淀成可持续发布资产。

调研结论（2026-04-01）：

1. 当前内容体系已能持续生产，但“资产复用层级”不清晰，导致高价值证据重复整理。
2. 开发、答辩、论文三类需求都在要内容，缺少统一分层时会出现排期冲突与重复劳动。
3. Step 6/6 下一步建议固定“三层内容货架”：`快照卡（日报）`、`案例卡（周复盘）`、`专题包（月度）`，用同一套证据源服务多种输出。

建议新增三类执行资产（围绕记忆系统主线）：

1. `快照卡货架`（P0）
   - 形态：统一维护 `snapshot_id + date_window + key_signal + evidence_refs + risk_flag + next_action`
   - 目标：每天沉淀最小事实快照，防止周末集中补录导致证据断层
2. `案例卡货架`（P0）
   - 形态：统一维护 `case_id + scenario_tag + trigger_command + before_after + replay_result + linked_backlog`
   - 目标：每周沉淀可复放案例，形成“失败-修复-验证”可讲述资产
3. `专题包货架`（P1）
   - 形态：统一维护 `topic_pack_id + thesis_claim + supporting_cases + counterexamples + audience_versions + publish_state`
   - 目标：月度聚合为“可答辩、可写作、可复测”的专题内容包

推荐执行节奏（28 天）：

1. Week 1：建立快照卡货架，每天至少 1 条 `snapshot_id`，覆盖 insights/debug/scenes/tasks 四入口。
2. Week 2：建立案例卡货架，至少完成 6 条 `case_id` 的回放校验并补齐 `before_after`。
3. Week 3：建立专题包货架，输出 2 个 `topic_pack_id`（各含 >= 3 条 supporting_cases）。
4. Week 4：执行月度复盘，给出“续做专题/降级专题/下线专题”清单与下月排期。

验收门槛（建议）：

1. 连续 4 周满足“每周 >= 5 条快照卡 + >= 2 条案例卡 + 每月 >= 2 个专题包”。
2. 发布索引中 100% 核心条目具备 `evidence_refs + trigger_command + replay_result + linked_backlog(可空)`。
3. 每月至少 8 条内容完成“快照 -> 案例 -> 专题 -> 回流验证”闭环并可追溯。
4. 月度专题中失败/反例素材占比 >= 30%，避免只保留成功样本。

## 6. 下一阶段开发清单（详细需求）

### 6.1 需求一：补齐 Memory Reflection 调用链

需要完成：

1. 新增结构化决策对象和 schema。
2. 在 `ConversationCli` 中插入反思阶段。
3. 根据反思结果决定是否增强记忆上下文。
4. 将反思结果传入 `SystemPromptBuilder`。
5. 为失败情况提供稳定回退逻辑。

完成标准：

1. 系统能区分“需要记忆”和“不需要记忆”的问题。
2. 调用记忆时能输出自然语言原因。
3. 主链路在任何异常下仍可回答。
4. 当 `needs_memory=true` 且 `evidence_types/evidence_purposes` 缺失或非法时，默认值必须按 `memory_purpose` 派生（例如 `ACTION_FOLLOWUP -> TASK + followup`），不得统一回退为 continuity。
5. 当 `needs_memory=true` 且 `evidence_types/evidence_purposes` 采用别名写法（如 `recent-history/recentHistory`、`follow-up/followUp`）时，主链路与提示词层需归一化为标准枚举值，避免误判为非法而触发错误回退。

### 6.2 需求二：补齐记忆证据追踪

需要完成：

1. 记录本轮命中的记忆、案例、任务、skill。
2. 区分“被检索到”和“真正用于回答”的证据。
3. 支持最近一轮 trace 查询。
4. 支持后续扩展为历史 trace 存储。

完成标准：

1. 每轮回答结束后都能生成一份 trace。
2. CLI 可查看最近一轮的 trace。
3. 最近一轮 trace 查询应支持“内存态优先 + `memory_evidence_traces.jsonl` 回退读取”，避免重启后丢失调试入口。
4. 支持按窗口查看最近 N 轮 trace（建议入口：`/memory-debug [N]`）。
5. `/memory-debug` 展示应包含四类证据的覆盖率（`used/retrieved` 百分比），便于快速定位“检索有效但未使用”的问题。
6. 历史 trace 回读时若 `reflection.needs_memory` 缺失或非法，展示必须保持 `unknown`，不得默认映射为“否”。
7. `/memory-debug` 单轮视图与 `/memory-debug [N]` 历史视图应复用同一 trace 解析规范（字段清洗与三态语义一致），避免出现历史视图与单轮视图展示偏差。
8. `/memory-debug [N]` 历史窗口查询应遵循“内存态优先 + 持久化补齐”：当最新一轮尚未异步落盘时，仍需展示该内存态 trace，避免历史视图落后一轮。
9. 历史 trace 回读需兼容 legacy 扁平字段：当 `needs_memory/reason/...` 位于顶层（无 `reflection` 对象）时，应保持可解析并沿用三态语义。
10. 历史 trace 回读需兼容“字符串化 JSON”格式：当 `reflection` 或 `retrieved/used` 列表被存为 JSON 字符串（如 `"[]"`、`"{...}"`）时，`/memory-debug` 仍应正确解析并展示，不得退化为原始字符串。
11. 反思服务异常返回 `null` 时，trace 侧仍需写入稳定反思结果：`use_saved_memories=true` 回退为 `ReflectionResult.fallback()`，`use_saved_memories=false` 固定为 `ReflectionResult.memoryDisabled()`，禁止出现空指针或语义漂移。
12. `/memory-debug` 与 `/memory-debug [N]` 需展示反思关键字段（`memory_purpose/confidence/retrieval_hint`），并在覆盖率较低时给出诊断提示（如“已检索但未使用”“使用偏低”），便于快速定位证据浪费。
13. 历史 trace 回读时 `reflection.confidence` 需兼容两类异常量纲：`1.0~2.0` 视为轻微越界并钳制为 `1.0`，`2.0~100` 视为百分制并归一化到 `0~1`，避免 `/memory-debug` 展示异常低置信度。
14. 历史 trace 回读需兼容“二次字符串化 JSON”格式：当字段值形如 `\"{...}\"`、`\"[...]\"` 时，`/memory-debug` 与 `/memory-debug [N]` 仍应正确解包并解析，不得退化为原始转义字符串。
15. 历史 trace 回读时 `memory_purpose/evidence_types/evidence_purposes` 需执行统一归一化（白名单过滤、大小写规范、去重）；当 `needs_memory=false` 时必须强制 `memory_purpose=NOT_NEEDED` 且清空证据类型/用途，避免展示语义冲突。
16. 历史 trace 回读需兼容多种时间戳格式：除 `ISO_LOCAL_DATE_TIME` 外，还应支持 `ISO_OFFSET_DATE_TIME`、`yyyy-MM-dd HH:mm:ss` 与 epoch 秒/毫秒（数字或数字字符串）；解析失败时才允许展示为 `(unknown)`，避免 `/memory-debug [N]` 在跨版本数据上丢失时间线。
17. 当历史 trace 回读中 `needs_memory=true` 且 `evidence_types/evidence_purposes` 缺失或全部非法时，`/memory-debug` 应提供稳定默认值（`USER_INSIGHT, RECENT_HISTORY` / `continuity`），避免提示词与调试视图语义漂移。
18. `/memory-debug [N]` 历史视图需显式展示 `evidence_types/evidence_purposes`（去空、去重后输出），保证与单轮视图的反思证据语义对齐，降低排障时的信息缺口。
19. 历史 trace 回读需兼容字段命名差异：除 `snake_case` 外，还应支持 `camelCase`（如 `needsMemory/memoryPurpose/retrievalHint/retrievedInsights/usedEvidenceSummary`），避免跨版本导出数据在 `/memory-debug` 中丢字段。
20. `/memory-debug` 与 `/memory-debug [N]` 在覆盖率异常时应补充“未使用证据样例（retrieved - used）”预览，至少覆盖 Insights/Examples/Skills/Tasks 四类，便于快速定位证据浪费来源。
21. 当反思结果中 `needs_memory=true` 且 `retrieval_hint` 为 null-like 值（如 `null/undefined/n/a/none`）时，trace 与提示词必须统一回退为默认检索提示“优先检索与用户当前问题最相关的历史证据。”，避免 `/memory-debug` 与主链路展示空提示或无效提示。
22. 历史 trace 回读需兼容 legacy 布尔字面量：`needs_memory/memory_loaded` 除 `true/false/on/off/1/0` 外，还应支持 `yes/no/y/n/是/否`，避免 `/memory-debug` 在跨系统导出数据上误判为 `unknown` 或 `false`。
23. 历史 trace 回读需兼容百分号字符串置信度：当 `reflection.confidence` 为 `"85%"`、`"85 %"` 或全角百分号形式时，应按百分制归一化为 `0.85`，避免回退默认值导致 `/memory-debug` 置信度失真。
24. 历史 trace 回读需兼容字段别名：反思对象除 `reflection` 外还应支持 `reflection_result/reflectionResult`；Skills 检索字段除 `loaded_skills/loadedSkills` 外还应支持 `retrieved_skills/retrievedSkills`，避免跨系统导出下 Skills 覆盖率被误判为 `0/N`。
25. 反思主链路与历史 trace 回读都需兼容 `memory_purpose` 输入别名（`ACTION_FOLLOWUP` / `action-followup` / `action_followup` / `actionFollowup` 等）；当 `needs_memory=true` 且输入语义有效时必须归一化为统一枚举值，禁止因分隔符差异回退到 `CONTINUITY`。
26. `/memory-insights` 的统计链路需与 `/memory-debug` 保持同等级兼容解析：支持 `snake_case/camelCase` 字段、`reflection/reflection_result/reflectionResult`、`loaded_skills/retrieved_skills` 别名、字符串化/二次字符串化 JSON 列表，以及 `yes/no/y/n/是/否` 布尔字面量，避免洞察报表与调试视图指标漂移。
27. 历史 trace 回读需兼容高精度 epoch 时间戳：当 `timestamp` 以微秒/纳秒（16-19 位数字或数字字符串）落盘时，`/memory-debug [N]` 需自动换算到毫秒并正确展示本地时间，避免跨系统导出数据出现时间异常。
28. 历史 trace 回读需兼容“对象数组列表”字段：当 `retrieved/used` 证据列表项为对象（如 `{"name":"debugging"}`、`{"title":"提交周报"}`）时，`/memory-debug` 与 `/memory-insights` 需提取可读主字段（如 `name/title/text/content/id/slot_name/value`）并参与统计，避免展示为原始对象字符串导致覆盖率失真。
29. 历史 trace 回读需兼容 null-like 噪声证据项：当 `retrieved/used` 列表出现 `null/undefined/n/a/none`（含大小写变体）时，`/memory-debug` 与 `/memory-insights` 必须在解析阶段过滤，禁止将其计入证据列表与覆盖率统计，避免诊断结论被噪声污染。
30. `/memory-debug` 与单轮 `/memory-debug` 的“未使用证据样例（retrieved - used）”比对需兼容格式差异：当证据项仅存在大小写、连续空白差异（如 `Debug  Skill` vs `debug skill`）时，不应误判为未使用，避免诊断噪声。
31. 历史 trace 回读需兼容嵌套证据结构：当证据以 `evidence.retrieved.{insights/examples/skills/tasks}` 与 `evidence.used.{...}` 落盘（含 `snake_case/camelCase` 与 `loaded` 组别别名）时，`/memory-debug` 与 `/memory-insights` 必须正确解析并参与覆盖率统计，避免跨系统导出数据出现“检索/使用均为 0”的误判。
32. 历史 trace 回读需兼容“分隔字符串证据列表”：当 `retrieved/used` 证据以逗号/分号/竖线/多行文本（含 `- item`、`1. item` 等项目符号）落盘时，`/memory-debug` 与 `/memory-insights` 必须拆分并标准化为多条证据，避免把整段文本误计为单条导致覆盖率失真。
33. 历史 trace 回读需兼容“多层字符串化 JSON”：当 `reflection` 或 `retrieved/used` 列表被三层及以上 JSON 字符串包裹（如 `\"\\\"{...}\\\"\"`、`\"\\\"[...]\\\"\"`）时，`/memory-debug` 与 `/memory-insights` 仍应逐层解包并正确解析，不得退化为原始转义文本。
34. 历史 trace 回读需兼容“点路径扁平字段”：当字段以 `reflection.needs_memory`、`evidence.retrieved.insights`、`retrieved.examples`、`loaded.skills` 等形式落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统扁平化导出数据出现覆盖率误判。
35. 历史 trace 回读需兼容“键值映射证据列表”：当 `retrieved/used/loaded` 证据以 map 形式落盘（如 `{"insight-a":true}`、`{"case-1":{"title":"example-a"}}`、`{"task-a":"on"}`）时，`/memory-debug` 与 `/memory-insights` 必须正确提取有效证据项并参与覆盖率统计；`false/no/off/0` 等否定值不得计入命中，避免跨系统导出数据出现检索量/使用量低估。
36. 历史 trace 回读需兼容“中括号路径扁平字段”：当字段以 `reflection[needs_memory]`、`evidence[retrieved][insights][0]`、`retrieved[examples][0]`、`loaded[skills][1]` 等形式落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建嵌套结构并正确统计，避免跨系统数组索引导出数据出现覆盖率误判。
37. 历史 trace 回读需兼容“斜杠路径扁平字段”：当字段以 `reflection/needs_memory`、`evidence/retrieved/insights/0`、`retrieved/examples/0`、`loaded/skills/1` 等形式落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建嵌套结构并正确统计，避免跨系统 JSON Pointer 风格导出数据出现覆盖率误判。
38. 历史 trace 回读需兼容“字段命名风格漂移”：当字段名出现大小写差异或分隔符差异（如 `MEMORY-LOADED`、`REFLECTION-RESULT`、`NEEDS-MEMORY`、`RETRIEVED-INSIGHTS`）时，`/memory-debug` 与 `/memory-insights` 必须按归一化键匹配并正确解析，避免跨系统导出数据因命名风格差异丢字段。
39. 历史 trace 回读需兼容“JSON Pointer 前导斜杠路径”：当字段以 `/reflection/needs_memory`、`/evidence/retrieved/insights/0`、`/retrieved/examples/0`、`/loaded/skills/1` 等形式落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建嵌套结构并正确统计；同时需兼容 `~1/~0` 转义片段，避免跨系统 JSON Pointer 导出数据出现覆盖率误判。
40. 历史 trace 回读需兼容“JSON Pointer URI Fragment 路径”：当字段以 `#/reflection/needs_memory`、`#/evidence/retrieved/insights/0`、`#/retrieved/examples/0`、`#/loaded/skills/1` 等形式落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建嵌套结构并正确统计，避免跨系统 URI Fragment 导出数据出现覆盖率误判。
41. 历史 trace 回读需兼容“扁平路径根键命名漂移”：当路径根键出现大小写或分隔符差异（如 `#/REFLECTION-RESULT/NEEDS-MEMORY`、`#/RETRIEVED-INSIGHTS/0`、`#/USED-SKILLS/0`）时，`/memory-debug` 与 `/memory-insights` 必须按归一化键匹配并正确重建结构，避免跨系统导出数据因根键风格差异丢字段。
42. 历史 trace 回读需兼容“扁平路径键前后空白”：当字段键存在前后空白（如 `  #/REFLECTION-RESULT/NEEDS-MEMORY  `、`  #/RETRIEVED-INSIGHTS/0  `）时，`/memory-debug` 与 `/memory-insights` 仍需在去空白后正确匹配并重建结构，避免跨系统导出数据因键名空白导致证据统计缺失。
43. 历史 trace 回读需兼容“JSONPath 扁平路径字段”：当字段以 `$.reflection.needs_memory`、`$['reflection']['evidence_purposes'][0]`、`$.retrieved.examples[0]`、`$[loaded][skills][1]` 等 JSONPath 风格落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统 JSONPath 导出数据出现覆盖率误判。
44. 历史 trace 回读需兼容“冒号路径扁平字段”：当字段以 `reflection:needs_memory`、`evidence:retrieved:insights:0`、`retrieved:examples:0`、`loaded:skills:1` 等 colon-path 风格落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统自定义分隔符导出数据出现覆盖率误判。
45. 历史 trace 回读需兼容“冒号路径分隔符两侧空白”：当字段以 `reflection : needs_memory`、`evidence : retrieved : insights : 0`、`loaded : skills : 1` 等含分隔符空白的 colon-path 风格落盘时，`/memory-debug` 与 `/memory-insights` 必须对路径 token 去空白后再重建结构并统计，避免跨系统导出数据因分隔符空白导致字段命中失败。
46. 历史 trace 回读需兼容“反斜杠路径扁平字段”：当字段以 `reflection\\needs_memory`、`evidence\\retrieved\\insights\\0`、`retrieved\\examples\\0`、`loaded\\skills\\1` 等 backslash-path 风格落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统（含 Windows 导出）路径分隔符差异导致覆盖率误判。
47. 历史 trace 回读需兼容“JSONPath URI Fragment 扁平字段”：当字段以 `#$.reflection.needs_memory`、`#$['evidence']['retrieved']['insights'][0]`、`#$.retrieved.examples[0]`、`#$[loaded][skills][1]` 等 `# + JSONPath` 组合风格落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统 URI Fragment 导出 JSONPath 路径时出现覆盖率误判。
48. 历史 trace 回读需兼容“双下划线路径扁平字段”：当字段以 `reflection__needs_memory`、`evidence__retrieved__insights__0`、`retrieved__examples__0`、`loaded__skills__1` 等 double-underscore 风格落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统自定义扁平化导出数据出现覆盖率误判。
49. 历史 trace 回读需兼容“竖线路径扁平字段”：当字段以 `reflection|needs_memory`、`evidence|retrieved|insights|0`、`retrieved|examples|0`、`loaded|skills|1`（含 `reflection | needs_memory` 等分隔符两侧空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统自定义 `|` 分隔导出数据出现覆盖率误判。
50. 历史 trace 回读需兼容“箭头路径扁平字段”：当字段以 `reflection->needs_memory`、`evidence->retrieved->insights->0`、`retrieved->examples->0`、`loaded->skills->1`（含 `# -> reflection -> ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统 ETL 日志/规则引擎常见 `->` 路径导出格式导致覆盖率误判。
51. 历史 trace 回读需兼容“胖箭头路径扁平字段”：当字段以 `reflection=>needs_memory`、`evidence=>retrieved=>insights=>0`、`retrieved=>examples=>0`、`loaded=>skills=>1`（含 `# => reflection => ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统规则链路/日志导出中的 `=>` 路径格式导致覆盖率误判。
52. 历史 trace 回读需兼容“双尖括号路径扁平字段”：当字段以 `reflection>>needs_memory`、`evidence>>retrieved>>insights>>0`、`retrieved>>examples>>0`、`loaded>>skills>>1`（含 `# >> reflection >> ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统规则/日志导出中的 `>>` 路径格式导致覆盖率误判。
53. 历史 trace 回读需兼容“分号路径扁平字段”：当字段以 `reflection;needs_memory`、`evidence;retrieved;insights;0`、`retrieved;examples;0`、`loaded;skills;1`（含 `# ; reflection ; ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统自定义 `;` 分隔导出数据出现覆盖率误判。
54. 历史 trace 回读需兼容“逗号路径扁平字段”：当字段以 `reflection,needs_memory`、`evidence,retrieved,insights,0`、`retrieved,examples,0`、`loaded,skills,1`（含 `# , reflection , ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统自定义 `,` 分隔导出数据出现覆盖率误判。
55. 历史 trace 回读需兼容“全角分隔符路径扁平字段”：当字段以 `reflection，needs_memory`、`evidence：retrieved：insights：0`、`retrieved；examples；0`、`loaded｜skills｜1`（含 `# ， reflection ， ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统本地化导出（全角 `，；：｜`）导致覆盖率误判。
56. 历史 trace 回读需兼容“Unicode 箭头路径扁平字段”：当字段以 `reflection→needs_memory`、`evidence→retrieved→insights→0`、`retrieved→examples→0`、`loaded→skills→1`（含 `# → reflection → ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统日志/ETL 导出中的 `→` 路径格式导致覆盖率误判。
57. 历史 trace 回读需兼容“Unicode 粗箭头路径扁平字段”：当字段以 `reflection⇒needs_memory`、`evidence⇒retrieved⇒insights⇒0`、`retrieved⇒examples⇒0`、`loaded⇒skills⇒1`（含 `# ⇒ reflection ⇒ ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统规则/日志导出中的 `⇒` 路径格式导致覆盖率误判。
58. 历史 trace 回读需兼容“Unicode 映射箭头路径扁平字段”：当字段以 `reflection↦needs_memory`、`evidence↦retrieved↦insights↦0`、`retrieved↦examples↦0`、`loaded↦skills↦1`（含 `# ↦ reflection ↦ ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统规则映射/日志导出中的 `↦` 路径格式导致覆盖率误判。
59. 历史 trace 回读需兼容“Unicode 长箭头路径扁平字段”：当字段以 `reflection⟶needs_memory`、`evidence⟶retrieved⟶insights⟶0`、`retrieved⟶examples⟶0`、`loaded⟶skills⟶1`（含 `# ⟶ reflection ⟶ ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统日志/规则链路导出中的 `⟶` 路径格式导致覆盖率误判。
60. 历史 trace 回读需兼容“Unicode 双线长箭头路径扁平字段”：当字段以 `reflection⟹needs_memory`、`evidence⟹retrieved⟹insights⟹0`、`retrieved⟹examples⟹0`、`loaded⟹skills⟹1`（含 `# ⟹ reflection ⟹ ...` 等 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统规则链路/日志导出中的 `⟹` 路径格式导致覆盖率误判。
61. 历史 trace 回读需兼容“Unicode 黑箭头路径扁平字段”：当字段以 `reflection➡needs_memory`、`evidence➡retrieved➡insights➡0`、`retrieved➡examples➡0`、`loaded➡skills➡1`（含 `# ➡ reflection ➡ ...` 以及 emoji 变体 `➡️` 的 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统协作文档/聊天记录导出中的 `➡/➡️` 路径格式导致覆盖率误判。
62. 历史 trace 回读需兼容“Unicode 粗黑箭头路径扁平字段”：当字段以 `reflection➜needs_memory`、`evidence➜retrieved➜insights➜0`、`retrieved➜examples➜0`、`loaded➜skills➜1`（含 `# ➜ reflection ➜ ...` 以及 emoji 变体 `➜️` 的 fragment 前缀与分隔符空白形式）落盘时，`/memory-debug` 与 `/memory-insights` 必须自动重建结构并正确统计，避免跨系统协作文档/聊天记录导出中的 `➜/➜️` 路径格式导致覆盖率误判。

### 6.3 需求三：补齐场景化展示命令

需要完成：

1. `/memory-debug`
2. `/memory-timeline`
3. `/memory-review`
4. `/memory-report`
5. `/memory-scenes`
6. `/tasks`
7. `/memory-insights [limit]`

完成标准：

1. 命令输出清晰，适合终端展示。
2. 结果中能直观看到记忆如何参与回答。
3. 能直接支撑答辩演示，不依赖临场解释太多细节。

### 6.4 需求四：补齐案例蒸馏基础设施

需要完成：

1. 定义 `Example`、`Pattern` 基础数据结构。
2. 设计持久化文件格式。
3. 实现人工触发或定时触发的蒸馏入口。
4. 初步支持从 `Pattern` 生成 `Skill` 草稿。

完成标准：

1. 至少可从多个案例生成一个 Pattern。
2. Pattern 能保存适用条件、核心步骤、失败边界。

### 6.5 需求五：补齐自然语言任务与主动服务链路

需要完成：

1. 强化自然语言任务提取能力，并统一接入主对话链路。
2. 支持从用户 NL 中生成结构化任务并落盘。
3. 支持查询待办任务、即将到期任务、已触发任务。
4. 支持到期主动提醒原会话。
5. 支持基于记忆、摘要和任务状态生成主动建议。
6. 在 `/memory-debug` 或 `/tasks` 中展示主动服务依据。

完成标准：

1. 用户能直接通过自然语言创建任务。
2. 到时间后系统能主动提醒。
3. 系统能基于记忆主动给出至少一种阶段性建议。
4. 主动输出可解释，能说明依赖了哪些记忆证据。

### 6.6 需求六：补齐答辩演示数据集

需要完成：

1. 准备五类固定场景的输入脚本。
2. 为每个场景准备预期展示点。
3. 让系统在演示前可一键回放或快速恢复数据状态。
4. 其中必须包含“自然语言建任务”和“主动提醒/主动建议”两个固定场景。

完成标准：

1. 演示过程稳定，不依赖临场即兴输入。
2. 每个场景都能展示 Memory-System 的独特价值。

### 6.7 优先级排序

1. `Memory Reflection`
2. `Memory Evidence View`
3. 场景化展示命令
4. 自然语言任务与主动服务
5. 会话摘要
6. 案例蒸馏
7. 评测脚本

### 6.8 不在下一阶段优先做的内容

1. 不优先做显式记忆路由器
2. 不优先做重量级 Web UI
3. 不优先做泛化多 Agent 编排
4. 不优先做与 Memory-System 主线无关的通用平台能力

### 6.9 需求七：补齐内容化产出工作流（Step 6/6）

需要完成：

1. 固化五类内容模板：`周复盘卡`、`场景案例卡`、`任务闭环卡`、`回归告警卡`、`论文图表卡`。
2. 为每类模板绑定固定命令入口（至少覆盖 `/memory-insights`、`/memory-scenes`、`/memory-debug [N]`、`/tasks`、`/memory-review`）。
3. 建立内容资产目录规范（`.memory/content-assets/**`），并约束命名规则：`YYYY-MM-DD_<type>_<topic>.md`。
4. 在 `开发实现process.md` 中按迭代记录每次内容产出的数据窗口、结论与后续动作。
5. 在答辩前准备至少 8 份可复现内容卡，其中场景案例卡不少于 5 份，并标注输入脚本和预期观测点。
6. 每周固定执行一次“内容盘点”，淘汰无证据结论，更新高价值案例索引。
7. 维护 `.memory/content-assets/index.md` 索引文件，记录内容资产与证据来源映射关系。
8. 建立“选题覆盖约束”：每月至少覆盖 5 个不同场景主题（个性化/连续性/任务闭环/主动服务/治理与评测中至少 5 类）。

完成标准：

1. 任意一次迭代后，能在 10 分钟内从命令输出生成一份结构化复盘内容。
2. 至少 5 条“系统改进结论”可被证据链回放验证。
3. 论文与答辩材料的关键图表口径与 CLI 输出一致，无口径漂移。
4. 周度内容产出稳定达到：每周 >= 1 份周复盘卡 + >= 1 份场景案例卡。
5. 出现质量回归时，48 小时内补齐一份回归告警卡和修复跟踪结论。
6. `.memory/content-assets/index.md` 中每条索引都能追溯到对应命令输出或原始数据文件。
7. 月度内容中不存在“单一场景占比 > 60%”的失衡情况。

### 6.10 需求八：补齐内容发布漏斗与反馈回流（Step 6/6）

需要完成：

1. 建立“内容分层发布”机制：L1（短）/L2（中）/L3（长）三级节奏固定化。
2. 在每份内容卡新增 `publish_status`、`target_audience`、`feedback_notes` 字段。
3. 固化 30 天发布计划，至少覆盖反思、连续性、治理、主动服务四大主题。
4. 建立反馈回流流程：每次发布后 48 小时内沉淀“有效反馈 -> backlog 条目 -> 验证计划”。
5. 在 `开发实现process.md` 记录每次发布对应的指标变化与是否形成代码改动。

完成标准：

1. 每周至少形成 1 条“内容反馈驱动的开发任务”。
2. 每月至少完成 1 次“内容结论复测”（验证结论是否仍成立）。
3. 内容发布与开发迭代可建立可追溯映射（内容 ID -> backlog -> commit/test/doc）。
4. 出现“高关注但低复现”的内容时，72 小时内补齐复现实验或撤回结论。

### 6.11 需求九：补齐内容增长实验与资产治理（Step 6/6）

需要完成：

1. 为每条可发布内容绑定 `content_id`，并在 `.memory/content-assets/index.md` 维护唯一索引。
2. 引入内容实验最小框架：每周至少 1 个 A/B 实验，覆盖标题/结构/证据密度/受众中的任一维度。
3. 为内容卡新增治理字段：`verification_status`、`stale_after`、`retraction_note`。
4. 建立“反馈回流 SLA”：高价值反馈 48 小时内转 backlog；不可复现结论 72 小时内修正。
5. 建立“新鲜度复测”机制：超过 30 天的核心结论必须复测后才能复用到答辩/PPT/论文。

完成标准：

1. 内容索引中 100% 条目具备 `content_id` 与证据引用，且可追溯到命令输出或原始文件。
2. 每月至少完成 4 个内容实验，并形成“保留/淘汰”决策记录。
3. 过期结论（`stale`）占比 <= 10%，且答辩材料中不出现 `stale/retracted` 结论。
4. 每月至少 2 条“内容实验结论”转化为开发优化项并完成验证。

### 6.12 需求十：补齐内容产品矩阵与 12 周执行机制（Step 6/6）

需要完成：

1. 在 `.memory/content-assets/index.md` 中新增 `content_line` 字段（`builder/user/defense/research`）。
2. 建立 12 周执行看板（可先用 Markdown 文件），按周记录计划产出、实际产出、回流 backlog、验证状态。
3. 每周执行一次“栏目覆盖检查”，确保四条内容线中至少两条有新增产出。
4. 为每条 L3 内容绑定至少 1 个可复现实验（脚本或固定命令序列）。
5. 对答辩线内容增加 `demo_ready` 标记，未通过回放校验不得进入答辩素材池。

完成标准：

1. 连续 4 周满足“每周 >= 1 条可发布内容 + >= 1 条反馈回流 backlog”。
2. `index.md` 中 100% 条目可映射到内容产品线，且可追溯到证据来源。
3. 12 周结束后，答辩素材池内所有内容均具备 `demo_ready=true` 与最近一次复测记录。
4. 研究线至少形成 6 份图表/结论卡，且与论文口径一致。

### 6.13 需求十一：补齐 Step 6/6 扩展内容模板与周度执行看板

需要完成：

1. 新增 8 类扩展模板：`记忆对照实验卡`、`误判剖析卡`、`证据利用率优化卡`、`治理决策卡`、`主动服务命中卡`、`里程碑时间线卡`、`答辩演示快照卡`、`论文威胁与局限卡`。
2. 在 `.memory/content-assets/index.md` 增加字段：`content_type_extended`、`command_refs`、`replay_ready`、`week_bucket`。
3. 建立“7天执行看板”（Markdown 文件即可），按 Day 1~Day 7 记录计划、产出、回流、复测状态。
4. 固定周度结构：每周至少 1 份“失败剖析类”内容，不得全部为成功案例。
5. 每周盘点时执行“内容去重 + 证据复测 + stale 标记”三步检查，并输出处理结果。

完成标准：

1. 连续 2 周满足“每周新增 >= 6 份内容卡，且 >= 4 个主题方向”。
2. 扩展模板内容中 100% 条目具备 `command_refs` 与 `replay_ready` 标记。
3. 每周至少 2 条内容反馈进入 backlog，且至少 1 条完成验证闭环。
4. 连续 2 周无“无证据结论”内容进入发布池。

### 6.14 需求十二：补齐 Step 6/6 系列栏目化执行机制

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`series_name`、`episode_no`、`baseline_ref`、`delta_summary`、`decision`。
2. 固化 6 条系列栏目（Benchmark / Failure / Replay / Governance / Proactive / Defense）的最小产出频率与证据入口。
3. 建立“基线对照”约束：系列内容默认必须包含上期或上版本对照，禁止只给单点结论。
4. 建立“系列回流”约束：每条系列内容发布后 48 小时内判断是否进入 backlog，并记录结论。
5. 在 `开发实现process.md` 每周追加“栏目执行小结”，记录产量、回流数、复测状态。

完成标准：

1. 连续 4 周满足“每周 >= 1 条系列内容 + >= 1 条回流判断记录”。
2. 系列内容中 >= 70% 包含 `baseline_ref + delta_summary`。
3. 每月至少 3 条系列结论形成开发任务，其中至少 2 条完成验证闭环。
4. 答辩线素材 100% 具备 `series_name/episode_no/demo_ready`，且可回放到原始证据。

### 6.15 需求十三：补齐 Step 6/6 资产包化执行机制（第三层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`asset_pack_id`、`source_content_ids`、`command_bundle_ref`、`expected_observations`、`pack_status`。
2. 新增资产包目录规范：`.memory/content-assets/packs/{demo|diagnosis|benchmark|paper|publish}/`。
3. 为资产包建立统一状态机：`draft -> verified -> stale -> retired`，并记录 `last_replay_at`。
4. 每个资产包至少绑定 1 份失败或回归样本，禁止“只含成功案例”的空洞模板。
5. 建立“7 天复测”机制：`verified` 状态超过 7 天未回放自动降级为 `stale`。
6. 在 `开发实现process.md` 每周追加“资产包执行小结”，记录新增数量、回放通过率、回流 backlog 数。

完成标准：

1. 连续 2 周满足“每周 >= 1 个资产包 + 回放通过率 >= 80%”。
2. 资产包索引中 100% 条目具备 `asset_pack_id + source_content_ids + command_bundle_ref`。
3. 答辩池与发布池中 100% 条目处于 `verified` 状态，且 7 天内有回放记录。
4. 每月至少 2 个资产包结论转化为开发任务，其中至少 1 个完成验证闭环。

### 6.16 需求十四：补齐 Step 6/6 问题库化与专题化执行机制（第四层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`topic_cluster`、`content_form`、`reuse_target`、`proof_level`、`next_refresh_at`。
2. 新增问题库目录规范：`.memory/content-assets/question-bank/{faq|failure-boundary|defense-qa}/`。
3. 新增专题目录规范：`.memory/content-assets/series-special/{deep-dive|benchmark-weekly|paper-chapter}/`。
4. 固化 `Memory-System 100 问` 最小结构：问题、短答、命令入口、证据引用、回放步骤、更新时间。
5. 固化“专题转开发”约束：每两周至少 1 条专题结论进入 backlog，并在 `开发实现process.md` 记录验证状态。
6. 建立“过期刷新”机制：`next_refresh_at` 超时的条目自动标记 `stale_candidate`，周盘点必须处理。

完成标准：

1. 连续 2 周满足“每周 >= 1 份问题库内容 + >= 1 份专题内容”。
2. 问题库索引中 100% 条目具备 `proof_level + command_refs + next_refresh_at`。
3. 每月至少 2 条问题库/专题结论形成开发任务，其中至少 1 条完成验证闭环。
4. 答辩问答素材中 >= 80% 可直接映射到 `question-bank` 或 `series-special` 条目。

### 6.17 需求十五：补齐 Step 6/6 渠道化分发与自动化生成机制（第五层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`distribution_channel`、`audience_stage`、`render_template`、`auto_generated`、`publish_url`、`feedback_score`、`feedback_volume`、`next_distribution_at`。
2. 新增渠道目录规范：`.memory/content-assets/distribution/{daily|weekly|fix-note|benchmark|im-digest}/`。
3. 固化“自动组装 + 人工复核”双阶段流程：自动填模板，人工只审证据、口径、复测三项。
4. 固化分发节奏：每日 1 条内部快照、每周 1 份周报简报、每周至少 1 次 IM 推送。
5. 建立“渠道反馈回流”约束：每条分发内容 48 小时内完成反馈登记，并判断是否进入 backlog。
6. 在 `开发实现process.md` 每周追加“渠道化执行小结”，记录分发数量、反馈量、回流任务与验证状态。

完成标准：

1. 连续 2 周满足“每周 >= 3 条可分发内容 + >= 1 条反馈回流 backlog”。
2. 渠道内容索引中 100% 条目具备 `distribution_channel + evidence_refs + verification_status`。
3. 自动组装内容占比 >= 50%，且人工复核后可发布率 >= 80%。
4. 每月至少 2 条渠道反馈形成开发任务，其中至少 1 条完成验证闭环。

### 6.18 需求十六：补齐 Step 6/6 专题季化与对照评测产品化机制（第六层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`season_id`、`season_track`、`baseline_version`、`compare_version`、`metric_delta`、`evidence_density`、`reusability_level`、`season_review_status`。
2. 新增专题季目录规范：`.memory/content-assets/seasons/{quarterly-report|benchmark-pack|failure-calendar|defense-script|paper-figures}/`。
3. 固化“跨版本对照”约束：专题季条目默认必须绑定 `baseline_version + compare_version`，禁止仅给单版本结论。
4. 固化“失败素材占比”约束：专题季新增内容中至少 40% 来自失败/回归样本。
5. 固化“季报收敛”机制：每季度至少形成 1 份季度状态报告，含核心指标变化、主要风险、下季度动作。
6. 在 `开发实现process.md` 每两周追加“专题季执行小结”，记录新增条目、回流任务、复测通过率、失效结论处理结果。

完成标准：

1. 连续 4 周满足“每周 >= 2 条专题季素材 + >= 1 条回流任务登记”。
2. 专题季索引中 100% 条目具备 `season_id + baseline_version + compare_version + evidence_refs`。
3. 每月至少 2 条专题季结论形成开发任务，其中至少 1 条完成验证闭环。
4. 每季度输出的状态报告中，100% 关键结论可回放到命令输出、trace 或测试记录。

### 6.19 需求十七：补齐 Step 6/6 场景赛题化与公开挑战集机制（第七层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`challenge_id`、`challenge_scene`、`expected_outcome`、`failure_signature`、`fix_commit_ref`、`replay_script_ref`、`external_retest_status`、`challenge_score`。
2. 新增挑战目录规范：`.memory/content-assets/challenges/{scenarios|replays|before-after|external-retests|season-summary}/`。
3. 固化“挑战卡最小结构”：输入脚本、预期行为、失败判定、证据引用、回放命令、更新时间。
4. 固化“失败样本占比”约束：每周新增挑战中至少 50% 来自失败/回归场景。
5. 固化“修复对照”约束：进入发布池的挑战结论必须包含 `before-after` 对照，不允许仅给修复后结果。
6. 在 `开发实现process.md` 每周追加“挑战集执行小结”，记录新增挑战数、通过率、退化率、回流任务和复测状态。

完成标准：

1. 连续 4 周满足“每周 >= 2 个挑战场景 + >= 1 条回流任务登记”。
2. 挑战索引中 100% 条目具备 `challenge_id + replay_script_ref + evidence_refs`。
3. 每月至少 4 条挑战结论形成开发任务，其中至少 2 条完成验证闭环。
4. 答辩或对外演示使用的关键案例中 >= 70% 可映射到挑战卡或修复前后对照卡。

### 6.20 需求十八：补齐 Step 6/6 生态协作化与共创回流机制（第八层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`collab_batch_id`、`contributor_type`、`retest_case_count`、`valid_issue_count`、`fix_adoption_count`、`external_proof_level`、`closure_status`、`impact_summary`。
2. 新增协作目录规范：`.memory/content-assets/collab/{retest-batches|issue-recycles|fix-impact|proof-cards|closure-tracking}/`。
3. 固化“复测任务单最小结构”：场景输入、执行步骤、预期行为、失败判定、提交模板、证据引用。
4. 固化“失败样本回收约束”：每周新增协作样本中至少 50% 为失败/回归样本，禁止只展示通过案例。
5. 固化“共创回流约束”：每条有效外部反馈 72 小时内必须完成 `backlog` 归档与状态标记（`open/in_progress/verified`）。
6. 在 `开发实现process.md` 每两周追加“生态协作执行小结”，记录复测批次数、有效问题数、修复采纳数和闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 1 批复测任务 + >= 1 条外部反馈回流任务”。
2. 协作索引中 100% 条目具备 `collab_batch_id + evidence_refs + closure_status`。
3. 每月至少 3 条外部反馈形成开发任务，其中至少 2 条完成验证闭环。
4. 答辩与论文中的“外部有效性”素材中 >= 60% 可追溯到协作复测证据。

### 6.21 需求十九：补齐 Step 6/6 基准化资产与公开评测包机制（第九层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`benchmark_id`、`dataset_version`、`protocol_version`、`runner_env`、`model_variant`、`score_summary`、`reproducibility_badge`、`benchmark_status`。
2. 新增基准目录规范：`.memory/content-assets/benchmark/{datasets|protocols|replay-packs|monthly-bulletins|regression-board}/`。
3. 固化“基准最小结构”：输入样本、标签说明、评分协议、回放命令、预期行为、更新时间。
4. 固化“对照评测约束”：发布的基准结论必须包含 `dataset_version + protocol_version + model_variant`，禁止只给单次跑分截图。
5. 固化“回流时限约束”：每份月度基准公报 72 小时内必须完成“进入/不进入 backlog”判断并留痕。
6. 在 `开发实现process.md` 每两周追加“基准执行小结”，记录样本增量、回放次数、退化样本、修复采纳与复测结果。

完成标准：

1. 连续 4 周满足“每周 >= 1 批样本入库 + >= 1 次基准回放登记”。
2. 基准索引中 100% 条目具备 `benchmark_id + dataset_version + protocol_version + evidence_refs`。
3. 每月至少 3 条基准结论形成开发任务，其中至少 2 条完成验证闭环。
4. 对外展示使用的关键指标中 >= 70% 可回放到基准评测包或月度基准公报。

### 6.22 需求二十：补齐 Step 6/6 记忆产品化内容与演示资产库机制（第十层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`product_card_id`、`audience_type`、`scenario_script_ref`、`qa_bundle_ref`、`demo_ready`、`proof_level`、`last_replay_at`、`publish_window`。
2. 新增产品化目录规范：`.memory/content-assets/product/{capability-cards|demo-cards|failure-stories|qa-bundles|paper-figure-notes|release-cards|public-invites}/`。
3. 固化“产品卡最小结构”：场景背景、输入脚本、观测点、证据引用、结论边界、复测命令、最近回放时间。
4. 固化“演示可用约束”：`demo_ready=true` 必须包含最近一次回放记录（建议 14 天内），否则不得进入答辩/对外演示池。
5. 固化“发布可信约束”：`proof_level=public` 条目必须包含至少一条外部复测或跨版本对照证据。
6. 在 `开发实现process.md` 每两周追加“产品化内容执行小结”，记录新增卡片数、回放通过率、反馈回流任务与闭环状态。

完成标准：

1. 连续 4 周满足“每周 >= 1 份产品化内容 + >= 1 次回放校验登记”。
2. 产品化索引中 100% 条目具备 `product_card_id + audience_type + evidence_refs + demo_ready`。
3. 每月至少 2 条产品化内容反馈形成开发任务，其中至少 1 条完成验证闭环。
4. 答辩或对外演示素材中 100% 条目可追溯到“复测命令/脚本 + 证据来源 + 最近回放时间”。

### 6.23 需求二十一：补齐 Step 6/6 选题智能编排与质量闸门机制（第十一层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`topic_score`、`evidence_completeness`、`replay_cost`、`freshness_risk`、`content_debt_level`、`qa_gate_status`、`automation_run_id`、`roi_tag`。
2. 新增运营目录规范：`.memory/content-assets/ops/{topic-ranking|content-debt|replay-schedule|qa-gate|post-publish-receipt}/`。
3. 固化“选题评分”规则：每周至少一次基于价值、证据完整度与复测成本计算 Top 5 选题。
4. 固化“质量闸门”规则：未通过 `qa_gate_status=passed` 的条目不得进入发布池与答辩池。
5. 固化“内容债处理”规则：`major/critical` 内容债需在 7 天内处理并留痕（修复/降级/撤回）。
6. 固化“回流收益归因”规则：每两周至少完成 1 条内容反馈的指标归因，并在 `开发实现process.md` 记录验证结论。

完成标准：

1. 连续 4 周满足“每周 Top 5 选题均具备 `topic_score + evidence_refs`”。
2. 发布池索引中 100% 条目具备 `qa_gate_status=passed` 与最近一次回放时间。
3. 每周至少关闭 2 条 `major/critical` 内容债，且处理结果可追溯。
4. 每月至少 2 条“内容 -> backlog -> 指标改善”链路完成归因并通过复测验证。

### 6.24 需求二十二：补齐 Step 6/6 课程化训练与认证化输出机制（第十二层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`training_asset_id`、`skill_track`、`difficulty_level`、`rubric_ref`、`pass_threshold`、`cert_status`、`cert_valid_until`、`mentor_feedback_ref`。
2. 新增课程化目录规范：`.memory/content-assets/training/{quickstart-labs|scenario-drills|misjudge-fixes|mentor-scripts|rubrics|camp-weekly|cert-receipts|onboarding-paths}/`。
3. 固化“训练任务最小结构”：目标场景、输入脚本、观测点、失败样例、评分标准、复测命令、结论边界。
4. 固化“认证有效性约束”：`cert_status=certified` 条目必须绑定最近一次回放评分记录（建议 30 天内），超期自动降级为 `expired`。
5. 固化“训练反馈回流约束”：每两周至少 1 条训练反馈进入 backlog，并在 `开发实现process.md` 记录采纳与验证结果。
6. 固化“对外能力声明约束”：任何对外宣称“可复现能力”的内容必须包含 `rubric_ref + pass_threshold + evidence_refs`，禁止只给演示截图。

完成标准：

1. 连续 4 周满足“每周 >= 1 份训练资产 + >= 1 次回放评分登记”。
2. 训练索引中 100% 条目具备 `training_asset_id + skill_track + cert_status + evidence_refs`。
3. 每月至少 2 条训练反馈进入 backlog，其中至少 1 条完成验证闭环。
4. 答辩或对外使用的能力证明条目中 100% 可追溯到“训练题单 + 评分量表 + 最近回放时间”。

### 6.25 需求二十三：补齐 Step 6/6 周主题策展与双轨内容编排机制（第十三层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`theme_week`、`theme_name`、`track_type`、`content_tier`、`anchor_conclusion_id`、`counterexample_ref`、`reuse_from_ids`、`weekly_close_status`。
2. 新增主题策展目录规范：`.memory/content-assets/themes/{weekly-curation|anchor-conclusions|quick-briefs|deep-dives|counterexamples|weekly-close}/`。
3. 固化“双轨编排”规则：生产轨固定产出主线/短内容/长内容，复核轨固定补齐反例或失败边界。
4. 固化“跨周复用”规则：每周至少复用 2 份历史资产，并记录 `reuse_from_ids + 决策结论`。
5. 固化“周收官”规则：每周末必须产出主题收官卡，汇总产量、质量、回流任务和下周主题建议。
6. 在 `开发实现process.md` 每周追加“主题策展执行小结”，记录主题命中率、复用率、回流率与复测状态。

完成标准：

1. 连续 4 周满足“每周 >= 4 条主题内容（含 >= 1 条 L2/L3）+ >= 1 条反馈回流任务”。
2. 主题索引中 100% 条目具备 `theme_week + theme_name + track_type + evidence_refs`。
3. 主线结论条目中 >= 80% 具备 `counterexample_ref` 或失败边界说明。
4. 每周至少复用 2 份历史资产，且复用条目 100% 有“保留/淘汰/重写”决策留痕。

### 6.26 需求二十四：补齐 Step 6/6 证据图谱叙事与多受众改写机制（第十四层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`evidence_graph_id`、`source_trace_ids`、`audience_variant`、`claim_id`、`metric_window`、`consistency_status`、`narrative_template`、`drift_ticket_ref`。
2. 新增图谱叙事目录规范：`.memory/content-assets/narratives/{evidence-graphs|claim-variants|metric-stories|counterexample-debates|term-glossary|chart-voiceover|audience-receipts|drift-alerts}/`。
3. 固化“一证多稿”规则：同一 `claim_id` 至少产出开发版与答辩版，且 `source_trace_ids` 完全一致。
4. 固化“口径一致性”规则：发布前必须通过 `consistency_status=aligned`，检查结论定义、指标口径与时间窗口一致性。
5. 固化“图谱回放审查”规则：每周至少抽查 3 条内容，必须可回放“证据 -> 结论”链路。
6. 固化“口径漂移处置”规则：发现冲突后 48 小时内产出 `drift_alert` 并完成修正留痕。

完成标准：

1. 连续 4 周满足“每周 >= 3 条一证多稿内容 + >= 1 条证据图谱卡 + >= 1 次图谱回放审查”。
2. 多受众索引中 100% 条目具备 `claim_id + source_trace_ids + audience_variant + consistency_status`。
3. 抽检样本中 >= 90% 可从 trace 回放到结论，且 `metric_window` 与正文一致。
4. `consistency_status=diverged` 条目 100% 在 48 小时内关联 `drift_ticket_ref` 并完成修正或降级。

### 6.27 需求二十五：补齐 Step 6/6 场景剧本化与复盘栏目闭环机制（第十五层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`column_id`、`scenario_script_id`、`baseline_run_ref`、`current_run_ref`、`delta_signal`、`failure_case_ref`、`feedback_to_backlog_ref`、`next_validation_due`。
2. 新增栏目目录规范：`.memory/content-assets/columns/{scenario-master|weekly-replay|repair-ledger|failure-digest|version-bulletin|qa-updates|evidence-gap|monthly-close}/`。
3. 固化“栏目周更”规则：每周至少更新 2 条栏目，且每条必须包含“基线对照 + 变化结论 + 下次验证点”。
4. 固化“失败占比”规则：每周至少 1 条失败样本进入公开复盘，不允许只输出成功故事。
5. 固化“回流约束”规则：每周至少 1 条栏目反馈进入 backlog，并在下次栏目更新中标注处理状态（open/doing/verified/archived）。
6. 在 `开发实现process.md` 每周追加“栏目化执行小结”，记录栏目更新数、失败样本数、回流任务数、闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 2 条栏目更新 + >= 1 条失败样本复盘 + >= 1 条反馈回流任务”。
2. 栏目索引中 100% 条目具备 `column_id + scenario_script_id + baseline_run_ref + current_run_ref + delta_signal`。
3. 栏目内容中 >= 80% 具备“下一次验证点（next_validation_due）”且按时回放。
4. 回流任务中 >= 70% 在两周内完成“verified 或 archived”状态闭环。

### 6.28 需求二十六：补齐 Step 6/6 选题评分引擎与发布回流实验机制（第十六层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`topic_id`、`topic_score`、`score_breakdown`、`experiment_variant`、`publish_channel`、`feedback_signal`、`feedback_to_task_ref`、`template_reuse_ref`。
2. 新增目录规范：`.memory/content-assets/engine/{topic-pool|topic-score|weekly-schedule|ab-experiments|publish-receipts|feedback-tasks|reuse-templates|monthly-growth}/`。
3. 固化“选题评分”规则：每周候选选题至少 10 条，统一按 5 维度加权评分并排序，Top 3 进入排期。
4. 固化“实验对照”规则：每周至少 2 条内容执行 A/B 对照，明确对照变量（标题、结构或证据排布）且禁止混杂变量。
5. 固化“反馈回流”规则：每条发布内容必须关联至少 1 条反馈回流任务，并在下轮内容中标注处理状态（open/doing/verified/archived）。
6. 在 `开发实现process.md` 每周追加“选题引擎执行小结”，记录候选数、入排期数、实验数、回流任务闭环率、模板复用数。

完成标准：

1. 连续 4 周满足“每周 >= 10 条候选选题 + >= 3 条排期内容 + >= 2 条 A/B 对照内容”。
2. 发布回执索引中 100% 条目具备 `topic_id + topic_score + publish_channel + feedback_signal + feedback_to_task_ref`。
3. 回流任务中 >= 70% 在两周内完成“verified 或 archived”状态闭环。
4. 月度复盘中每月至少沉淀 3 条“模板复用卡”，且次月至少复用 2 条。

### 6.29 需求二十七：补齐 Step 6/6 长期记忆演化年表与纵向研究机制（第十七层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`evolution_track_id`、`time_window`、`baseline_ref`、`current_ref`、`delta_summary`、`drift_signal`、`backlog_link`、`next_checkpoint`。
2. 新增目录规范：`.memory/content-assets/evolution/{weekly-snapshots|monthly-trends|quarterly-reports|drift-observations|personalization-deltas|task-trends|proactive-hits|paper-figures}/`。
3. 固化“时间窗口”规则：所有纵向结论必须明确 `7d/30d/90d` 窗口，禁止无窗口结论进入发布池。
4. 固化“基线对照”规则：每条纵向内容必须包含 `baseline_ref + current_ref + delta_summary`，禁止仅给单点结论。
5. 固化“回流约束”规则：每月至少 1 条纵向观察结论进入 backlog，并在 `开发实现process.md` 记录验证计划与状态。
6. 在 `开发实现process.md` 每月追加“纵向演化执行小结”，记录快照产量、漂移告警、回流任务与闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 1 份纵向快照 + >= 1 条结论留痕”。
2. 纵向索引中 100% 条目具备 `evolution_track_id + time_window + baseline_ref + current_ref + evidence_refs`。
3. 每月至少 2 条纵向结论形成开发任务，其中至少 1 条完成验证闭环。
4. 季度演化报告中 100% 关键结论可回放到命令输出、trace 或评测记录。

### 6.30 需求二十八：补齐 Step 6/6 证据驱动内容实验室与发布流水线（第十八层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`experiment_id`、`hypothesis`、`baseline_ref`、`variant_ref`、`metric_guardrail`、`audience_variant`、`distribution_channel`、`feedback_to_backlog_ref`。
2. 新增目录规范：`.memory/content-assets/lab/{experiment-design|baseline-runs|failure-anatomy|audience-rewrite|publish-receipts|stability-weekly|replay-scripts|quarterly-bulletin}/`。
3. 固化“对照实验”规则：每周至少 1 组 baseline vs variant 对照，必须显式声明变量与评测窗口（7d 或 30d）。
4. 固化“证据绑定”规则：每条可发布内容必须绑定 `baseline_ref + experiment_id + evidence_refs`，缺一不可。
5. 固化“失败配额”规则：每周新增内容中失败样本占比 >= 40%，并要求记录修复动作与复测结果。
6. 固化“发布回流”规则：每周至少 1 条发布反馈进入 backlog，并在下一周输出复测结论卡。
7. 在 `开发实现process.md` 每周追加“内容实验室执行小结”，记录实验数、失败样本数、回流任务数、复测闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 1 组对照实验 + >= 3 条可分发内容 + >= 1 条反馈回流任务”。
2. 内容索引中 100% 发布条目具备 `experiment_id + baseline_ref + evidence_refs + distribution_channel`。
3. 每月至少 4 条失败样本完成“问题 -> 修复 -> 复测”闭环并有结果留痕。
4. 季度内容公报中 >= 80% 关键结论可回放到实验记录、trace 或评测输出。

### 6.31 需求二十九：补齐 Step 6/6 记忆场景基准集与内容复用引擎（第十九层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`benchmark_id`、`benchmark_topic`、`input_script_ref`、`command_seq_ref`、`expected_signal`、`benchmark_ready`、`stability_state`、`repair_task_ref`。
2. 新增目录规范：`.memory/content-assets/benchmarks/{scenario-bank|replay-sequences|weekly-compare|failure-repair|defense-demo|paper-mapping|reuse-templates|quarterly-stability}/`。
3. 固化“入池门槛”规则：仅当题单同时具备输入脚本、命令序列、预期信号与证据引用时，才允许标记 `benchmark_ready=true`。
4. 固化“周复测”规则：每周至少复测 3 条历史基准题单，并更新 `stability_state`（stable/improving/regressing）。
5. 固化“失败处置”规则：失败题单 72 小时内必须完成“修复任务登记或降级说明”二选一，并在 `开发实现process.md` 留痕。
6. 固化“复用约束”规则：每周至少 1 条对外内容必须来自基准池，禁止全部使用一次性素材。
7. 在 `开发实现process.md` 每周追加“基准集执行小结”，记录入池数量、复测数量、失败处置与复用产出。

完成标准：

1. 连续 4 周满足“每周 >= 2 条基准入池 + >= 3 条历史题单复测 + >= 1 条失败处置记录”。
2. 基准索引中 100% 条目具备 `benchmark_id + input_script_ref + command_seq_ref + evidence_refs + benchmark_ready`。
3. 每月至少 6 条题单完成“问题 -> 修复/降级 -> 复测”闭环并可回放。
4. 答辩素材池中 >= 80% 条目可映射到基准题单与固定复现命令序列。

### 6.32 需求三十：补齐 Step 6/6 跨作用域连续性叙事与身份链路内容化（第二十层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`continuity_case_id`、`scope_pair`、`unified_id_ref`、`channel_set`、`consistency_state`、`governance_decision_ref`、`continuity_risk_level`、`retest_due`。
2. 新增目录规范：`.memory/content-assets/continuity/{scope-compare|identity-chain|conflict-arbitration|task-fulfillment|proactive-consistency|continuity-alerts|defense-cross-channel|paper-consistency}/`。
3. 固化“作用域对照”规则：每周至少 1 组 `personal vs team:*` 同输入脚本对照，必须记录证据差异与结论差异。
4. 固化“身份抽查”规则：每周至少抽查 2 条跨平台 `unified_id` 链路，并标记 `consistent/partial/broken`。
5. 固化“异常回流”规则：`consistency_state=broken` 的条目 7 天内必须完成“修复任务登记或降级说明”二选一。
6. 固化“演示复用”规则：每两周至少更新 1 份跨渠道演示剧本，并绑定固定命令序列与预期信号。
7. 在 `开发实现process.md` 每周追加“连续性内容执行小结”，记录对照组数量、身份抽查数、异常处置与复测通过率。

完成标准：

1. 连续 4 周满足“每周 >= 1 组作用域对照 + >= 2 条身份连续性抽查 + >= 1 条异常回流任务”。
2. 连续性索引中 100% 条目具备 `continuity_case_id + scope_pair + unified_id_ref + evidence_refs + consistency_state`。
3. 每月至少 4 条跨作用域/跨平台异常完成“发现 -> 修复/降级 -> 复测”闭环并可回放。
4. 答辩跨渠道素材中 >= 80% 条目可回放到同一 `unified_id` 的连续证据链。

### 6.33 需求三十一：补齐 Step 6/6 记忆场景节目化与发布编排台（第二十二层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`show_id`、`episode_id`、`content_track`、`publish_window`、`audience_type`、`feedback_ticket_refs`、`replay_check_status`、`next_episode_hook`。
2. 新增目录规范：`.memory/content-assets/showcase/{weekly-rundown|demo-script|failure-column|metric-brief|qa-breakdown|migration-alert|feedback-pipeline|monthly-finale}/`。
3. 固化“周发布节奏”规则：每周至少产出 `1 条 demo + 1 条 failure + 1 条 metric` 三类节目化内容。
4. 固化“双周回放复核”规则：每两周至少抽查 2 条已发布内容，执行证据回放并更新 `replay_check_status`。
5. 固化“反馈回流”规则：每周至少 1 条发布反馈进入 backlog，且必须绑定 `feedback_ticket_refs + retest_due`。
6. 固化“栏目连续性”规则：同一 `show_id` 每月至少更新 2 个 `episode_id`，避免单次发布后停更。
7. 在 `开发实现process.md` 每周追加“节目化执行小结”，记录发布数量、回放通过率、反馈回流与下一期钩子。

完成标准：

1. 连续 6 周满足“每周 >= 3 条节目化内容（含 >= 1 条失败复盘）”。
2. 节目化索引中 100% 条目具备 `show_id + episode_id + content_track + evidence_refs + replay_check_status`。
3. 每月至少 4 条反馈完成“发布 -> 回流任务 -> 复测结论更新”闭环并可回放。
4. 答辩演示素材中 >= 80% 条目可按 `show_id -> episode_id` 连续回放且结论口径一致。

### 6.34 需求三十二：补齐 Step 6/6 证据工厂化与选题评分执行机制（第二十三层）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`topic_score`、`score_breakdown`、`evidence_pack_id`、`baseline_ref`、`compare_ref`、`funnel_stage`、`reuse_ratio`、`decision_status`。
2. 新增目录规范：`.memory/content-assets/factory/{topic-scoring|evidence-pack|ab-contrast|rejection-log|multi-audience-rewrite|regression-fast-response|weekly-funnel|monthly-strategy}/`。
3. 固化“选题评分”规则：每周至少评估 10 个候选选题，并按 `impact/replayability/novelty/risk` 评分后选择 Top 3 进入发布队列。
4. 固化“证据准入”规则：任何可发布内容必须绑定 `baseline_ref + evidence_refs + command_refs`，缺一不可。
5. 固化“对照实验”规则：每周至少 1 条内容必须包含“基线 vs 当前”对照，明确差异与风险。
6. 固化“失败配额”规则：每月发布内容中失败/回归样本占比不得低于 30%，避免只保留成功案例。
7. 固化“回流验证”规则：每周至少 1 条内容反馈进入 backlog，并在两周内完成验证登记（修复/降级/撤回其一）。
8. 在 `开发实现process.md` 每周追加“内容工厂执行小结”，记录候选数、入选数、复用率、回流任务数与闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 10 个候选选题 + >= 3 条发布内容 + >= 1 条对照实验内容”。
2. 内容工厂索引中 100% 条目具备 `topic_score + evidence_pack_id + evidence_refs + decision_status`。
3. 每月至少 6 条内容完成“发布 -> 回流 -> 验证”闭环并可回放。
4. 每月至少 2 条结论完成“答辩版/论文版/开发版”三稿改写且证据口径一致。

### 6.35 需求三十三：补齐 Step 6/6 主题库与模板库落地机制（收敛执行版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`topic_id`、`template_type`、`target_audience`、`owner`、`publish_date`、`publish_status`、`expected_feedback`、`retest_due`。
2. 新增目录规范：`.memory/content-assets/mvp/{topic-pool|template-library|publish-calendar|weekly-retro|feedback-loop}/`。
3. 固化“主题入池”规则：每周至少新增 5 条候选主题，并以 `topic_score` 排序，Top 3 进入本周发布日历。
4. 固化“模板准入”规则：仅允许 6 类模板进入发布池（失败复盘、对照实验、指标快讯、场景脚本、任务闭环、论文图表），禁止临时自由格式直接发布。
5. 固化“发布节奏”规则：每周至少 3 条发布内容，其中至少 1 条必须为失败复盘或回归案例。
6. 固化“反馈回流”规则：每条已发布内容必须绑定 `expected_feedback + retest_due`，并在两周内完成状态更新（open/doing/verified/archived）。
7. 在 `开发实现process.md` 每周追加“Step 6/6 收敛执行小结”，记录主题入池数、模板使用率、发布数、反馈闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 5 条主题入池 + >= 3 条发布内容 + >= 1 条失败复盘内容”。
2. 发布索引中 100% 条目具备 `topic_id + template_type + owner + publish_status + evidence_refs`。
3. 每月至少 4 条反馈完成“发布 -> 回流任务 -> 复测结论”闭环并可追溯。
4. 月度复盘中明确 Top 3 高复用模板与 Top 3 低效模板，并给出保留/淘汰决策。

### 6.36 需求三十四：补齐 Step 6/6 系列化发布与复用链路机制（执行增强版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`series_id`、`episode_id`、`narrative_goal`、`campaign_id`、`rewrite_target`、`reuse_ratio`、`quality_check`、`campaign_status`。
2. 新增目录规范：`.memory/content-assets/series/{series-ledger|reuse-graph|campaign-board|cross-audience-rewrite|monthly-retro}/`。
3. 固化“系列准入”规则：每周至少 2 条发布内容来自在册系列，且每个系列需保持 3~5 期连续规划。
4. 固化“复用链路”规则：每条核心内容必须登记至少 1 条跨受众复写（答辩/论文/开发其一）并记录 `reuse_ratio`。
5. 固化“主题战役”规则：每月设定 1 个 `campaign_id`，并按周维护里程碑与风险状态（green/yellow/red）。
6. 固化“质量复核”规则：系列内容发布前必须完成 `quality_check`（evidence_complete/logic_consistent/replay_available 三项全通过）。
7. 在 `开发实现process.md` 每周追加“Step 6/6 系列化执行小结”，记录系列在册数、系列发布占比、复用率、战役状态与闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条系列内容 + >= 1 条跨受众复写内容”。
2. 发布索引中 100% 条目具备 `series_id + episode_id + campaign_id + evidence_refs + quality_check`。
3. 每月至少 6 条内容完成“发布 -> 回流 -> 复测 -> 改写复用”闭环并可追溯。
4. 月度战役复盘中明确“延续系列 >= 2 条、淘汰系列 >= 1 条、接力系列 >= 1 条”与对应证据依据。

### 6.37 需求三十五：补齐 Step 6/6 场景资产包与双周复测机制（闭环实战版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`scene_pack_id`、`scenario_goal`、`replay_script_id`、`trigger_command`、`pass_criteria`、`result_status`、`drift_flag`、`retest_batch_id`。
2. 新增目录规范：`.memory/content-assets/replay/{scene-packs|script-library|biweekly-retest|drift-board|closure-ledger}/`。
3. 固化“场景资产包”规则：每周至少新增 3 个候选场景包，并确保 Top 2 场景包进入当周发布与复测计划。
4. 固化“脚本绑定”规则：每条核心发布内容必须绑定 `replay_script_id + trigger_command + pass_criteria`，缺任一字段不得进入发布池。
5. 固化“双周复测”规则：每两周至少完成 1 次批量复测，覆盖最近 2 周发布内容中的 Top 50% 高价值条目。
6. 固化“漂移处置”规则：`drift_flag=true` 的条目需在 7 天内完成处置（修复/降级/撤回其一），并记录到 `closure-ledger`。
7. 固化“结论续更”规则：复测后若结论发生变化，必须在 48 小时内同步更新内容索引与对应改写版本（答辩/论文/开发至少一版）。
8. 在 `开发实现process.md` 每两周追加“Step 6/6 复测执行小结”，记录复测覆盖率、漂移率、处置完成率与结论更新时间。

完成标准：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条绑定回放脚本内容 + 双周 >= 1 次复测公报”。
2. 发布索引中 100% 核心条目具备 `scene_pack_id + replay_script_id + pass_criteria + result_status + evidence_refs`。
3. 每月至少 8 条内容完成“发布 -> 回放 -> 复测 -> 回流 -> 结论更新”闭环并可回放。
4. 漂移条目在 7 天内的处置完成率 >= 80%，且失效条目 100% 具备处置结论与责任人。

### 6.38 需求三十六：补齐 Step 6/6 证据订阅与收益归因机制（运营稳态版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`evidence_feed_id`、`content_ticket_id`、`topic_cluster`、`priority_score`、`linked_backlog`、`fix_cost`、`benefit_metric`、`roi_status`。
2. 新增目录规范：`.memory/content-assets/ops/{evidence-feeds|ticket-pool|roi-ledger|weekly-ops-board|monthly-ops-retro}/`。
3. 固化“证据订阅”规则：每周至少维护 4 条活跃订阅流（`/memory-insights`、`/memory-scenes`、`/memory-debug [N]`、`/tasks`），并标记 `freshness_sla` 与 `stale_risk`。
4. 固化“待办准入”规则：所有新增发布主题必须先进入 `ticket-pool`，按 `priority_score` 排序后进入发布日历，禁止绕过待办池直接发布。
5. 固化“收益归因”规则：每条高价值内容必须绑定 `linked_backlog + benefit_metric + verification_date`，并在两周内更新 `roi_status`（positive/neutral/negative）。
6. 固化“低效清退”规则：连续两次 `roi_status=negative` 的主题需进入下线评审，并在月度复盘给出保留/淘汰决策。
7. 在 `开发实现process.md` 每周追加“Step 6/6 运营稳态执行小结”，记录订阅触发数、待办消化率、收益验证数与低效主题处置率。

完成标准：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条订阅触发内容 + >= 1 条收益归因记录”。
2. 发布索引中 100% 核心条目具备 `evidence_feed_id + content_ticket_id + linked_backlog + benefit_metric + roi_status`。
3. 每月至少 6 条内容完成“证据订阅 -> 发布 -> 回流 -> 收益验证”闭环并可回放。
4. 连续两周 `roi_status=negative` 的主题 100% 完成下线评审并留痕决策。

### 6.39 需求三十七：补齐 Step 6/6 问题触发与一证三稿放大机制（增长放大版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`question_trigger_id`、`content_bundle_id`、`target_audience`、`dev_version_id`、`defense_version_id`、`public_version_id`、`consistency_status`、`reuse_count`、`turnover_days`、`decay_risk`。
2. 新增目录规范：`.memory/content-assets/growth/{question-triggers|bundle-pipeline|stock-board|weekly-growth-board|monthly-growth-retro}/`。
3. 固化“问题触发”规则：每周至少维护 20 条活跃触发项，覆盖记忆反思、证据追踪、场景回放、收益归因四类问题，并标记 `priority_level`。
4. 固化“一证三稿”规则：每条核心内容必须产出开发版/答辩版/对外版三稿并校验 `consistency_status=pass` 后方可进入发布池。
5. 固化“库存周转”规则：每周至少复用 1 条历史高价值资产；`turnover_days > 21` 且 `reuse_count=0` 的条目需进入续更或淘汰评审。
6. 固化“衰减处置”规则：连续两周 `decay_risk=high` 的条目必须在 7 天内完成“重写/下线”处置并记录到增长复盘。
7. 在 `开发实现process.md` 每周追加“Step 6/6 增长放大执行小结”，记录触发命中数、三稿产出数、库存周转率与衰减处置率。

完成标准：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条一证三稿内容 + >= 1 条库存复用内容”。
2. 发布索引中 100% 核心条目具备 `question_trigger_id + content_bundle_id + target_audience + consistency_status + reuse_count`。
3. 每月至少 8 条内容完成“问题触发 -> 三稿改写 -> 发布 -> 回流验证”闭环并可回放。
4. 连续两周 `decay_risk=high` 的条目 100% 完成处置并保留决策记录。

### 6.40 需求三十八：补齐 Step 6/6 选题热力图与失败优先回流机制（稳态提效版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`topic_heat_id`、`queue_rank`、`evidence_density`、`replay_cost`、`roi_trend`、`failure_case_id`、`failure_type`、`impact_level`、`biweekly_report_id`、`unresolved_risk_level`。
2. 新增目录规范：`.memory/content-assets/efficiency/{topic-heatmap|failure-priority-pool|biweekly-feedback-report|weekly-queue-board|monthly-efficiency-retro}/`。
3. 固化“热力排期”规则：每周至少维护 15 条活跃主题，按 `queue_rank` 进入发布队列，禁止跳过热力队列直接发布。
4. 固化“失败优先”规则：每周发布内容中失败/回归样本占比不得低于 40%，且每条失败样本必须绑定 `linked_backlog` 与 `fix_window`。
5. 固化“双周公报”规则：每两周至少产出 1 份回流公报，覆盖最近发布内容 Top 60% 高价值条目，并更新 `verified_fixes/unresolved_risks`。
6. 固化“低效下线”规则：连续两次 `roi_trend=down` 且 `failure_replay=fail` 的主题，7 天内完成“重写/下线”处置并登记结果。
7. 在 `开发实现process.md` 每周追加“Step 6/6 稳态提效执行小结”，记录热力入队数、失败样本占比、回流验证数与低效下线处置率。

完成标准：

1. 连续 4 周满足“每周 >= 3 条发布内容 + 失败/回归占比 >= 40% + >= 1 条回流验证更新”。
2. 发布索引中 100% 核心条目具备 `topic_heat_id + queue_rank + linked_backlog + verification_status`，失败样本额外具备 `failure_case_id + impact_level`。
3. 每双周至少 1 份回流公报且覆盖最近发布 Top 60% 高价值内容，并可追溯到修复任务状态。
4. 连续两次低效主题 100% 完成“重写/下线”处置并留痕。

### 6.41 需求三十九：补齐 Step 6/6 证据母版复用与多渠道发布机制（落地复用版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`master_evidence_id`、`claim_statement`、`source_trace_refs`、`question_node_id`、`parent_question_id`、`answer_route`、`release_pack_id`、`channel`、`format`、`publish_status`、`backflow_hook`。
2. 新增目录规范：`.memory/content-assets/reuse/{evidence-master|question-playbook-tree|release-packs|weekly-release-board|monthly-reuse-retro}/`。
3. 固化“证据母版”规则：每周至少维护 30 条活跃 `master_evidence_id`，并标记 `confidence_level + expiry_date + reuse_scope`，过期条目不得进入发布池。
4. 固化“问题剧本”规则：所有进入发布池的主题必须先绑定 `question_node_id + answer_route + required_evidence_ids`，禁止绕过剧本树直接发布。
5. 固化“多渠道发布”规则：每周至少 1 组 `release_pack_id` 输出开发版/答辩版/对外版三渠道内容，并记录统一 `generated_from`。
6. 固化“回流挂钩”规则：每个发布包必须绑定 `backflow_hook`（`/memory-insights` 或 `/memory-review` 或 `/tasks` 其一），发布后 7 天内更新回流状态。
7. 固化“低复用处置”规则：连续两周 `reuse_scope=single` 且 `reuse_count=0` 的条目，必须在 7 天内完成“扩展复用/下线”处置并登记原因。
8. 在 `开发实现process.md` 每周追加“Step 6/6 落地复用执行小结”，记录母版新增数、剧本覆盖率、跨渠道发布组数与回流更新率。

完成标准：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 2 条母版复用内容 + >= 1 组跨渠道发布包”。
2. 发布索引中 100% 核心条目具备 `master_evidence_id + question_node_id + release_pack_id + channel + publish_status`。
3. 每月至少 8 条内容完成“证据母版 -> 问题剧本 -> 多渠道发布 -> 回流验证”闭环并可回放。
4. 连续两周低复用条目 100% 完成处置并保留决策记录。

### 6.42 需求四十：补齐 Step 6/6 决策归因与结论续更机制（归因执行版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`counterexample_id`、`violated_claim`、`change_note_id`、`before_statement`、`after_statement`、`change_reason`、`benefit_timeline_id`、`baseline_metric`、`current_metric`、`delta_summary`、`verification_window`、`roi_status`。
2. 新增目录规范：`.memory/content-assets/attribution/{counterexample-ledger|change-note-stream|benefit-timeline|weekly-attribution-board|monthly-attribution-report}/`。
3. 固化“反例入账”规则：每周至少登记 10 条高价值失败样本，且 `risk_level=high` 条目必须绑定 `failure_trace_refs + owner + planned_fix_window`。
4. 固化“结论续更”规则：任何进入发布池的核心结论若发生变化，48 小时内必须补齐 `change_note_id + before_statement + after_statement + change_reason`，禁止只改正文不留变更记录。
5. 固化“收益归因”规则：每周至少 3 条修复进入 `benefit_timeline`，并给出 `baseline_metric vs current_metric` 对照及 `roi_status`（positive/neutral/negative）。
6. 固化“高风险负收益处置”规则：连续两周 `roi_status=negative` 且 `risk_level=high` 的主题，7 天内必须完成“降级/撤回/重做”其一并登记处置说明。
7. 在 `开发实现process.md` 每周追加“Step 6/6 归因执行小结”，记录反例入账数、结论续更数、收益验证数与高风险处置完成率。

完成标准：

1. 连续 4 周满足“每周 >= 3 条发布内容 + >= 1 条反例账本更新 + >= 1 条收益时间轴更新”。
2. 发布索引中 100% 核心条目具备 `linked_backlog + verification_window`，发生结论变化的条目额外具备 `change_note_id + before_statement + after_statement`。
3. 每月至少 6 条内容完成“失败样本 -> 修复动作 -> 结论更新 -> 收益验证”闭环并可回放。
4. 连续两周高风险负收益主题 100% 完成“降级/撤回/重做”处置并保留可追溯记录。

### 6.43 需求四十一：补齐 Step 6/6 内容货架化与分层发布机制（收口执行版）

需要完成：

1. 在 `.memory/content-assets/index.md` 新增字段：`content_tier`、`snapshot_id`、`case_id`、`topic_pack_id`、`date_window`、`before_after`、`replay_result`、`audience_versions`、`publish_state`。
2. 新增目录规范：`.memory/content-assets/shelf/{snapshots|cases|topic-packs|weekly-shelf-board|monthly-shelf-retro}/`。
3. 固化“快照卡准入”规则：每日至少沉淀 1 条快照卡，必须绑定 `evidence_refs + key_signal + next_action`。
4. 固化“案例卡准入”规则：每周至少 2 条案例卡，必须绑定 `trigger_command + before_after + replay_result`，未回放通过不得升级为专题素材。
5. 固化“专题包准入”规则：每月每个专题包至少包含 3 条支持案例 + 1 条反例，且必须提供 `audience_versions`（开发/答辩/论文至少两版）。
6. 固化“分层发布”规则：发布池至少 60% 来自案例卡或专题包，快照卡仅作信号输入，不直接替代深度内容。
7. 固化“回流约束”规则：每个专题包发布后 7 天内必须关联至少 1 条反馈任务，并更新状态（open/doing/verified/archived）。
8. 在 `开发实现process.md` 每周追加“Step 6/6 内容货架执行小结”，记录快照产出数、案例回放通过率、专题包发布数与回流闭环率。

完成标准：

1. 连续 4 周满足“每周 >= 5 条快照卡 + >= 2 条案例卡 + 每月 >= 2 个专题包”。
2. 发布索引中 100% 核心条目具备 `content_tier + evidence_refs + replay_result + publish_state`。
3. 每月至少 8 条内容完成“快照 -> 案例 -> 专题 -> 回流验证”闭环并可回放。
4. 专题包中失败/反例素材占比 >= 30%，且 100% 具备处置结论（保留/修复/撤回其一）。

### 4.3 文件加载与工具策略

固定工具：

1. `load_skill(name)`：按名称读取单个 skill 文件

默认文件访问方式：

1. 对于 `Agent.md`，会话启动自动加载
2. 对于 `user-insights.md`，在启用记忆时直接加载
3. 对于 `Skill`，优先使用固定工具 `load_skill`
4. 对于其他文件，优先使用只读 shell 命令按需探索和读取

只读 shell 白名单：

1. `rg`
2. `grep`
3. `head`
4. `tail`
5. `sed -n`
6. `ls`
7. `find`
8. `cat`

策略原则：

1. 工具优先，shell 兜底
2. 默认只读，不开放任意写命令
3. 渐进式暴露，不在启动时全量读取所有文件
4. 对高价值、高频资源使用固定工具；对低频杂项资源使用 shell

---

## 5. 模块与包结构

```text
src/main/java/com/memsys/
├── cli/          # 交互与编排
├── memory/       # 记忆核心（模型、存储、管理、写入、任务）
├── llm/          # 模型通信与结构化提取
├── prompt/       # system prompt 组装
└── rag/          # 向量索引与语义检索
```

### 5.1 `cli/`

- `CliRunner`：命令路由、交互展示
- `ConversationCli`：用户消息处理主链路

### 5.2 `memory/`

- `model/Memory`：统一记忆对象
- `storage/MemoryStorage`：文件 IO、初始化、迁移
- `MemoryManager`：Top of Mind 队列与清理
- `MemoryWriteService`：统一记忆写入入口
- `MemoryExtractor`：提取编排
- `NightlyMemoryExtractionJob`：定时隐式提取
- `MemoryAsyncService`：单线程异步池

### 5.3 `llm/`

- `LlmClient`：聊天、工具轮转与 JSON Schema 结构化调用
- `LlmExtractionService`：显式记忆 / 用户洞察提取
- `LlmDtos`：提取结果 DTO（合并单文件）
- `schema/Schemas`：结构化输出 Schema

### 5.4 `rag/`

- `RagService`：单文件 RAG 服务（索引、检索、持久化与对外业务接口）

### 5.5 `prompt/`

- `AgentGuideService`：启动时缓存 `Agent.md` 和可选 `.memory/Agent.local.md`
- `SystemPromptBuilder`：系统提示词区块组装

---

## 6. 存储文件规范

### 6.1 运行时文件（主流程使用）

1. `Agent.md`
2. `metadata.json`（含 `global_controls` 与 `ui_settings`）
3. `.memory/user-insights.md`
4. `memory_queues.json`
5. `recent_user_messages.jsonl`
6. `conversation_history.jsonl`
7. `vector_store.json`
8. `pending_explicit_memories.jsonl`
9. `memory_evidence_traces.jsonl`（每轮证据追踪历史）

### 6.2 预留文件

1. `model_set_context.json`（当前未接入主流程）
2. `user_insights.json`（历史格式，迁移源）

### 6.3 兼容迁移

启动时支持自动迁移：

- `implicit_memories.json -> user_insights.json`
- `user_insights.json -> .memory/user-insights.md`

---

## 7. 核心流程

### 7.1 对话主流程

1. 读取全局控制（是否启用记忆/历史）
2. 构建 system prompt（Agent 导航 + Metadata + 历史日志 + User Insight + 按需 Skill + RAG）
3. 读取最近完整对话作为 messages
4. 调用 LLM；如需要 skill 正文，通过 `load_skill(name)` 工具按需补充
5. 异步写入 recent/history
6. 异步执行显式提取与访问更新

### 7.2 显式记忆流程

1. 从当前用户消息提取候选记忆
2. 如槽位冲突，写入 `pending_explicit_memories.jsonl`
3. 如无冲突，更新 `user-insights.md` 的画像正文
4. 当前实现仍兼容更新 Top of Mind 和 RAG 索引；迁移后需重新评估

### 7.3 隐式提取流程

触发方式：

1. 夜间定时任务（默认 02:00）
2. CLI 手动命令 `/memory-update`

执行步骤：

1. 抽取近期历史
2. 提取用户洞察列表
3. 合并并重写 `user-insights.md` 正文

### 7.4 RAG 检索流程

1. 用户输入向量化
2. 检索向量库
3. 过滤已注入的上下文内容
4. 将相关内容注入 prompt

---

## 8. 配置项

`application.yml` 关键配置：

- `llm.api-key/base-url/model-name`
- `memory.max-slots/days-unaccessed/top-of-mind-limit/recent-messages-limit`
- `memory.async.*`
- `rag.enabled/min-similarity-score/max-search-results`
- `cli.theme/cli.show-footer`
- `scheduling.nightly-extraction-cron`

---

## 9. 测试建议

### 9.1 单元测试

1. `MemoryStorage` 迁移、单文档读写与窗口策略
2. `LlmClient.chatWithTools()` 工具轮转与异常回退
3. `ConversationCli` 的 `load_skill(name)` 工具暴露与参数校验
4. `MemoryManager` 队列升级与清理策略
5. `RagService` 检索排序与阈值过滤

### 9.2 集成测试

1. 一轮完整对话链路
2. 显式提取冲突入队
3. 夜间/手动隐式提取写入
4. RAG 检索注入是否生效

---

## 10. 已知差距与后续计划

1. 非临时模式下真实模型/API 的端到端工具调用回归：已提供手动 E2E 用例与脚本，尚未沉淀稳定基线结果
2. `model_set_context.json` 尚未接入 runtime 主链路
3. `memory_queues.json` 在单文档画像方案下是否退役仍未定案
4. `MemoryType` 目前仅 `USER_INSIGHT`，后续可扩展
5. 缺少系统化自动测试用例

### 10.1 毕设展示优先新增功能（建议优先级）

1. 任务闭环看板（高优先级）
   - 新增 `/tasks today|overdue|done` 视图与完成/延期操作
   - 形成“创建 -> 提醒 -> 完成 -> 周复盘”的完整闭环

2. 记忆冲突处理命令（高优先级）
   - 新增 `/memory-approve <slot>`、`/memory-reject <slot>`
   - 将现有治理展示从“可查看”升级为“可操作”

3. 飞书交互卡片化（中高优先级）
   - 周报与任务提醒升级为飞书卡片
   - 提供完成/延期/忽略按钮，增强产品化展示效果

4. 团队空间权限与共享策略（中优先级）
   - 在 `team:*` 作用域增加只读/可写成员策略
   - 增加团队共享任务池与个人任务隔离规则

5. 一键答辩演示模式（中优先级）
   - 新增 `/demo run`，串联“无记忆 -> 个人记忆 -> 团队记忆 -> 周报推送”
   - 提升现场演示稳定性和可复现性

### 10.2 真实 API E2E 执行方式

1. 设置 `OPENAI_API_KEY`（可选设置 `OPENAI_BASE_URL`、`OPENAI_MODEL`）
2. 执行 `./scripts/run-real-api-e2e.sh`
3. 查看 `logs/e2e/real-api-e2e.YYYY-MM-DD_HH-MM-SS.log` 中的回归结果

---

## 11. 变更规则

1. 四层架构改动必须先更新本文档再改代码。
2. 新增记忆类型时，必须同步更新：
   - `Memory` 模型
   - 存储层读写
   - prompt 注入逻辑
   - 提取 Schema/DTO
3. 文档结构固定保留：
   - `README.md`
   - `开发文档.md`
   - `开发实现process.md`
